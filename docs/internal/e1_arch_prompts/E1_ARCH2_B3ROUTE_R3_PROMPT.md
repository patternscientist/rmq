Make the title of this chat exactly: (E1-ARCH2-B3ROUTE-R3) Connect the bounded target to its proved arithmetic and close the historical route

Worker identity:
- Handle: E1-ARCH2-B3ROUTE-R3
- Requested title: `(E1-ARCH2-B3ROUTE-R3) Connect the bounded target to its proved arithmetic and close the historical route`
- Fresh or returning worker: FRESH governed architecture/proof worker. Predecessor `E1-ARCH2-B3ROUTE-R2` reported `INCOMPLETE` at commit `5973d5d549fc37575820aa6fb4cc648a0a33452e` and was audited (`docs/internal/audit_reports/2026-07-25_C06_e1_arch2_b3route_r2.md`). Its work is in your base and is yours to extend, but it is **not accepted**: every `PENDING` row in the inherited matrix is still `PENDING`, and no inherited theorem may be cited as accepted evidence.

Skill:
- Use $rmq-proof-sprint before starting and apply its completion gate.
- Workflow-governance ref: f0c7232a8a52b8d61ead5e96d72a8a849bc094b5. This is the latest commit that changes enforced workflow policy — skills, preflight scripts, gate, templates, or repository attributes. Later mainline commits that only add coordinator prose or decision-log entries do not advance it. Your base contains this ref; confirm that and do not substitute a different one because `main` has moved.
- Before substantive work, run `scripts/project_skill_preflight.ps1` against that exact ref with `rmq-proof-sprint` required and the RMQ skill names actually shown in this task's runtime catalog. It is expected to PASS. Stop on any absent/stale checkout skill, missing role skill, or ancestry mismatch; do not substitute another skill or continue best-effort.

Checkout contract:
- Task mode: WRITE.
- Exact base/target commit: 105a47bc3aeae10926dfd6866560eaf4f2be5d38, the governed R3 base. Its ordered parents are the R2 candidate `5973d5d549fc37575820aa6fb4cc648a0a33452e` and current governance mainline `e04922890d98c7a984393bd1512e1604a9be9c7d`. It therefore contains governance, accepted B1 `1727de15f2030bfb9296a9b31508bc00581aa33a`, accepted PREHIST `50c5f8ccf7be83a56b90a6c29142ac32860f0a27`, the accepted validation-local 56-module source port `c19061629ce8cf1e78992a99346170edd84b4971`, and R2's three unaccepted modules. Every accepted Core and `B3SourcePort` blob equals `c190616` exactly; verify that yourself before freezing.
- Known base condition: the decision logs carry duplicate `DD-20260723-001` and `WDD-20260723-001` entries from independent lineages, and `WDD-20260719-001` was already duplicated upstream. Resolve any of those IDs by lineage and line number, never by a whole-file first or last match, and allocate your own new IDs under today's date.
- Durable completion artifact: mode=WORKER_REPORT; path=docs/internal/E1_ARCH2_B3_HISTORICAL_ROUTE_MATRIX.md.
- Create branch exactly `codex/e1-arch2-b3-historical-route-r3` in a fresh worktree and report its absolute path. Do not touch, steer, or delete the R1 or R2 branches or worktrees.
- Require exactly two commits after the base. Commit 1 is the contract freeze and changes only `docs/internal/E1_ARCH2_B3_HISTORICAL_ROUTE_MATRIX.md`. Commit 2 is the implementation and final evidence; do not amend, squash, rewrite, or add a third commit.
- Final range may change only `RMQ/Validation/E1Architecture/B3HistoricalRoute/*.lean`, `scripts/e1_arch2_b3_historical_route_replay.ps1`, `docs/internal/E1_ARCH2_B3_HISTORICAL_ROUTE_MATRIX.md`, `docs/internal/DESIGN_DECISIONS.md`, and `docs/internal/WORKFLOW_DESIGN_DECISIONS.md`. Every non-owned blob must equal the exact base. Do not edit Core, the accepted B1 modules, the accepted `B3SourcePort` modules, public roots, or current-route work.

Roadmap contract:
- Node/join: E1 architecture gate B3 historical full-word route, consuming accepted B1/PREHIST and the accepted source-port/interface predecessor; its result feeds the later A4 selector alongside independently accepted B2 and B4 route verdicts.
- Local owned rung: one complete, checked full B3 historical bounded-route PASS or one quantifier-matched formal universal FAIL over the exact frozen allowed domain, with the arithmetic blocks' operational correspondence proved rather than assumed.
- Roadmap-node closure condition: B3 closes only after coordinator reconstruction of this exact two-commit candidate. A4 and E1 remain open until B2, B3, and B4 are all accepted and the selector is separately adjudicated.
- Goal: Connect the bounded target's arithmetic microprograms to the recursions already proved correct, then close the full historical route on the identical accepted B1 image using one shape-independent fixed ROM in B3-owned vocabulary and an explicit bounded execution that stutter-simulates the accepted historical source under truncated subtraction.
- Required theorem/file/tool: `HistoricalPASS acceptedB1 pinned` with a concrete witness and checked receipt, or `HistoricalFAIL acceptedB1 pinned` plus `historicalFail_iff_not_historicalPass` over the identical domain; plus `B3-HIST-17` operational block correspondence; implement under `RMQ/Validation/E1Architecture/B3HistoricalRoute/` with committed matrix and replay.
- Write scope: `RMQ/Validation/E1Architecture/B3HistoricalRoute/*.lean`; `scripts/e1_arch2_b3_historical_route_replay.ps1`; `docs/internal/E1_ARCH2_B3_HISTORICAL_ROUTE_MATRIX.md`; `docs/internal/DESIGN_DECISIONS.md`; `docs/internal/WORKFLOW_DESIGN_DECISIONS.md`.
- Lifecycle dependency order: accepted B1 and PREHIST precede the accepted source port; the owner's truncated-subtraction and ROM-typing decisions precede this freeze; this worker freezes the contract and seven port-deferred choices before implementation, proves the arithmetic blocks' operational correspondence, then constructs the ROM/read/width/simulation chain and produces committed replay evidence; coordinator audit follows worker completion; only a separately accepted B3 verdict may later feed A4. No decision depending on this task gates its own launch, and no B4/A4/integration/publication work is authorized here.
- Current-surface inventory: NOT_APPLICABLE; this task adds validation-local architecture evidence and may not edit a current public claim surface.
- Current-source-comment inventory: NOT_APPLICABLE; no governed source-comment repair is in scope.
- Dependency-surface inventory: NOT_APPLICABLE; this task consumes rather than restores, renames, splits, or migrates a public identity.
- Non-goals: Do not accept or modify B1, B2, B4, A4, public RMQ claims, main, CI, or E1 closure. Do not push, merge, publish, or clean other worktrees. Do not reopen the truncated-subtraction or ROM-typing decisions.
- Explicitly deferred work: A4 deterministic selection, B4 total-Nat route, any B2 successor, integration, publication, and public architecture synchronization are downstream and are not required for this B3 verdict itself.

Frozen architecture decisions — read before freezing anything:
- **Truncated subtraction** (`DD-20260724-001`). Target `.sub` is truncated natural subtraction, matching the accepted source ISA. The width obligation is "every arithmetic result is `< 2^w`", definitional for subtraction via `left - right ≤ left`. No ordered-subtraction premise anywhere. R2 measured the pinned run: 64 underflows in 727 reached subtractions, so an ordered premise is false on the accepted execution, not merely unproved.
- **ROM typing** (`DD-20260725-003`, new). The semantic ROM is typed in the **B3-owned target instruction vocabulary, not `E1Machine.Program`.** PREHIST's `semanticROM : E1Machine.Program` cannot express the flat physical read that `B3-HIST-03` — inherited from the same report — requires: `Instr.readMem` carries a segment and `execInstr` evaluates it as `store.readWord? segment address` with `decodeRead` option shifting, while the frozen construction choices require a raw flat fixed-cell read that must *not* reuse `decodeRead`. Freeze the B3-owned typing explicitly, cite `DD-20260725-003`, and do not claim `semanticROM : E1Machine.Program` as verbatim inherited. The source program remains the specification-side object your target stutter-simulates.
- Neither decision is available to you as an obstruction, and neither may be reopened. If you believe a third such conflict exists, report it exactly as R2 did — name it, do not resolve it, and do not claim authority to add a governed deviation.

Inherited status you must not overstate:
- R2 proved that the shape-dependent `mulConst`/`divConst` immediates admit a candidate lowering: `mulFuel_eq`, `divmodFuel_eq`, exact immediate inventories `[2,2,2,2]` and `[2,2,2,0,2,2]`, `LiteralClosed [0,1,2]`, and tag membership in the frozen twelve. All three modules re-elaborate cleanly (verified by the coordinator).
- **That is not the arithmetic-lowering question resolved.** `arithmeticLoweringAvailable` concludes only facts about two Lean-level `Nat` recursions. **Nothing connects execution of `mulByRegisterBlock` / `divByRegisterBlock` to those recursions.** The audited status is `UNRESOLVED-BUT-PROMISING`. You may not freeze, restate, or rely on "arithmetic lowering resolved" as a premise, and closing that gap is your first priority below.

Priority order — concentrate rather than spread:
1. **The operational lowering proof.** Prove that executing the multiplication and division blocks on the bounded target computes exactly `mulFuel` / `divmodFuel`, hence the `Nat` product and quotient, with the register invariants the blocks assume (in particular that the register used as an unconditional-branch condition really holds a nonzero value) established rather than assumed. This closes audit finding P2-1 and is the single most valuable result you can produce.
2. **Cheap contract hygiene, done early, not last.** Fix the replay's `REGISTRY-VERDICT-COUNTS` stage: it matches doubled backticks while the matrix uses single, so it reports `0/0` and fails. Re-freeze the matrix **with explicitly delimited editable regions** (`<!-- EVIDENCE-EDITABLE-BEGIN/END -->` and `<!-- STATUS-EDITABLE-BEGIN/END -->`, as the R1 matrix had) and a freeze-to-final byte comparison over everything outside them, so a locally frozen requirement cannot be softened undetected. R2 dropped those delimiters; the coordinator had to reconstruct the check by hand.
3. **ROM, codec, and explicit physical read lowering** under the frozen typing: one parameter-free semantic ROM, canonical width-indexed encoding with decode round-trip and injectivity, total fixed source-PC lowering, and each source logical read expanded into explicit descriptor count/offset/span, word-length, payload, sentinel, and dead reads of the one B1 image, retaining addresses, cells, order, and multiplicity. Never call `package.executor`.
4. **Reachable-width closure** with the subtraction case discharged with no ordering hypothesis, plus `B3-HIST-16` fidelity including a **kernel-checked** reachability assertion for source PC 73 — R2 established it only by compiled evaluation, with no declaration asserting it.
5. **Stuttering simulation, receipt, `P`/`Q`, and the verdict**, then the full 42-case semantic mutation campaign, which has never been executed in any round.

Acceptance contract:
- Frozen acceptance IDs: `B3-HIST-01-EXACT-B1-OBJECT`, `B3-HIST-02-FIXED-ROM-LITERAL-PROVENANCE`, `B3-HIST-03-EXPLICIT-PHYSICAL-READ-LOWERING`, `B3-HIST-04-DYNAMIC-WIDTH-CLOSURE`, `B3-HIST-05-SOURCE-STUTTERING-SIMULATION`, `B3-HIST-06-TRACE-CATEGORY-ROM-CORRESPONDENCE`, `B3-HIST-07-PARAMETERIZED-AND-PINNED-RECEIPT`, `B3-HIST-08-EXACT-PQ-VERDICT`, `B3-HIST-09-SEVEN-FROZEN-CHOICES`, `B3-HIST-10-ROUTE-DOMAIN`, `B3-HIST-11-RECEIPT-DEFINITIONALITY`, `B3-HIST-12-ENDPOINT-DOMAIN`, `B3-HIST-13-PINNED-VALUE-DEPENDENCY`, `B3-HIST-14-EDGE-COVERAGE`, `B3-HIST-15-DEFERRED-SCOPE`, `B3-HIST-16-TRUNCATED-SUBTRACTION-FIDELITY`, `B3-HIST-17-ARITHMETIC-BLOCK-OPERATIONAL-CORRESPONDENCE`, `COMPLETE-B3-HIST-COMMITTED-EVIDENCE`; inherited `E1-ARCH2-03-BOUNDED-WORD-IMAGE`, `E1-ARCH2-05-HISTORICAL-ROUTE`, `E1-ARCH2-07-SOURCE-STATE-SIMULATION`, `E1-ARCH2-08-COST-CATEGORY-INTEGRITY`; `INV-STORE-IDENTITY`, `INV-VALUE-DEPENDENCY`, `INV-SEMANTIC-NONVACUITY`, `INV-TRACE-EXECUTION`, `INV-STORE-AGREEMENT`, `INV-READ-BACKING`, `INV-WORD-WIDTH`, `INV-ADDRESS-WIDTH`, `INV-INSTRUCTION-ATOMICITY`, `INV-PROGRAM-ACCOUNTING`, `INV-ORACLE-INDEPENDENCE`, `INV-VALIDATION-REACH`, `INV-ALL-SIZE`, `INV-PROOF-SEPARATION`, `INV-NO-SYNTHETIC`, `INV-CATEGORY-SEPARATION`, `INV-PUBLIC-COMPOSITION`, `INV-GLOBAL-PHYSICAL-MACHINE`, `INV-WIDTH-SCALING`, `INV-CERTIFICATE-ANTI-BYPASS`, `INV-MUTATION-REPRODUCIBILITY`, and `ARCH-CONTRACT-NO-ASSUMED-CAPSTONE`.
- `B3-HIST-17-ARITHMETIC-BLOCK-OPERATIONAL-CORRESPONDENCE` is new and mandatory for `PASS`. Required: a theorem that running the bounded target from a state entering `mulByRegisterBlock base …` with registers holding `a`, `b` (and the loop's auxiliary registers initialized as the block requires) halts at the block exit with `R dst = a * b`, quantified over `a` and `b` rather than over pinned values, with fuel bounded by operand bit length; the same for `divByRegisterBlock` yielding `a / b` and `a % b` with `0 < b`; every register invariant the blocks rely on discharged, not assumed; and an independent expected-type consumer projecting both at concrete arguments so deleting either breaks it. A theorem about `mulFuel` alone does **not** satisfy this row.
- Before any implementation edit, create and commit the matrix from `docs/internal/templates/PROOF_ACCEPTANCE_MATRIX.md`. Under one strict raw-Git UTF-8 decoder, copy the inherited architecture requirement rows byte-for-byte from exact blob `9eb1625a6023eb36bd2ceefdcc44396eb768f3c6` and the B3 contract, exact predicates, mandatory mutation registry, and stop conditions from accepted PREHIST report blob `be80468ef049c0c94c7d1a7b3c5f8f69ccfd453f`. Add exact local rows for every construction leaf, the seven port-deferred choices, replay controls, and final receipt. Freeze all IDs, requirement cells, predicate text, registries, fixture inputs, chosen constants, and expected surfaces as PENDING before implementation.
- Three frozen texts differ deliberately from PREHIST and must be recorded in the matrix as governed deviations citing their decisions, never as verbatim inheritance: the `B3-HIST-04` evidence cell and `MUT-HIST-06G-SUBTRACTION-UNDERFLOW` (both `DD-20260724-001`), and the `semanticROM` typing in `B3-HIST-02`/`B3-HIST-10` (`DD-20260725-003`). Every other inherited row stays byte-for-byte.
- `MUT-HIST-06G` frozen replacement, exactly: "At `pinnedUnderflowSubPhase`, mutate the target `.sub` evaluator branch to produce the wrapping value `(2^width + left) - right` instead of the truncated `0` on underflow", expected verdict `REJECT`, failing surface `consumeSourceSimulation`. The wrapping value fits the declared width, so `consumeDynamicWidth` cannot be the rejecting surface and must not be named as one. Registry totals remain exactly 42 cases, 41 `REJECT`, one `ACCEPT`.
- Preserve every frozen row byte-for-byte from freeze to final outside the explicitly delimited evidence and status regions. Use a strict UTF-8 raw-Git comparison, reject missing/duplicate IDs, and include a mojibake negative control.
- Read and apply `.agents/skills/rmq-proof-sprint/references/COMPLETION_GATE.md`.

Frozen inputs, choices, and predicates:
- Accepted source-port matrix blob is `4f8b3c84fbac5d8ef766b5d3df1c289c097fbeb3`. Its ordered 56-module registry, 5,646-instruction program, twelve constructor tags, declaration/interface correspondence, pinned fixture, fixed branch families, and source PCs 1/5/7 are immutable inputs. Consume them; do not duplicate or edit them.
- Carry forward the seven port-deferred choices with their recorded rationale: `pinnedROMPC = 26`; `pinnedTargetBranchPC = 5`; `pinnedTargetArithmeticPhase = (26,0)`; `pinnedTargetSubPhase = (32,0)`; `pinnedUnderflowSubPhase = (73,0)`; `pinnedMainRunReadIndex = 0`; `pinnedProvenUnreadImageAddress = 693`.
- R2 determined by execution that PC 32's operands are `(1,1)` — the degenerate `left = right` case, where truncated and ordered subtraction coincide, so it does **not** discriminate the repaired semantics. It also recorded a strictly ordered witness at tick 54, PC 55, `.sub 22 31 24`, operands `(2,1)`. Freeze both, and state in the matrix which one carries the ordered-case obligation. Do not present the degenerate case as discriminating evidence.
- `pinnedUnderflowSubPhase` is source PC 73, the fourth instruction of `denseSegB`, numerically `.sub 19 22 23`, reached at tick 71 with `regs 22 = 5`, `regs 23 = 19`, truncating to `0`. R2 reproduced this independently of R1. You must additionally make its reachability kernel-checked or state plainly that you could not and why.
- `AllowedHistoricalCore` must use the exact accepted B1 package for every shape, exactly its `.image` and `.width`, one semantic ROM independent of width/shape/query/image contents, canonical width-specific encoding decoding to that identical ROM, and executable inputs limited to encoded ROM, exact image, left, and right. No runtime shape, proof callback, expected result, source program, sibling data object, or host-program inspection.
- Target instructions expand the frozen twelve historical constructors into explicit charged bounded microsteps. No new primitive, semantic helper callback, hidden recursion, `List.find?`, `Nat.log2`, shape projection, or theorem-only answer. Subtraction is monus, matching the source; that is not a new primitive.
- Use the exact endpoint guard and reference packet from PREHIST: `EndpointFits package left right` is `left < 2^package.width ∧ right < 2^package.width`; `ReferencePacket shape left right` is the independent high-level value for valid half-open ranges and `none` otherwise. Public endpoints are inputs; the conditional reference is proof-only and may not drive execution.
- `buildHistoricalReceipt` is a constructor from one actual recursive run; package/image/ROM/query/initial/fuel/final/physical trace/categories/ROM fetches/logical trace/output/source program/source store/source initial state are definitional projections. No proof field may supply an answer, trace, category list, fetch count, or source state.
- `ParameterizedComplete` quantifies every shape and every machine-representable endpoint pair — valid, reversed, empty, and out-of-bounds — producing fuel and one halted receipt whose output is `ReferencePacket`, and closing physical-read lowering, reachable width, source stuttering simulation, and trace/category/ROM correspondence on that same run.
- `PinnedComplete` uses the accepted fixture: values `[8,6,7,5,3,0,9,1,4,2,6,6]`, query `[0,12)`, independently expected `some 5`, width `24`, shared-size cell mutation `12` to `4`, source PCs 1/5/7, and the exact live/empty/dead/sentinel expressions from `PinnedFixture.lean`.
- Freeze `P`, `Q`, `HistoricalPASS`, `HistoricalFAIL` exactly as PREHIST states them, and prove `HistoricalFAIL ↔ ¬ HistoricalPASS` over the identical objects, guards, and quantifiers.

Mutation and replay contract:
- Inherit `MUT-HIST-01` through `MUT-HIST-12B` byte-for-byte from PREHIST blob `be80468ef049c0c94c7d1a7b3c5f8f69ccfd453f` without weakening, with the single governed exception `MUT-HIST-06G` above.
- **Execute the campaign.** It has never run in any round; `INV-MUTATION-REPRODUCIBILITY` is open. Every case needs its exact object, expected verdict, exact failing theorem or diagnostic, restoration, and a final clean-tree check. If you cannot execute all 42, execute as many as your evidence supports and report the exact executed and unexecuted sets — never report an identity check as a semantic result.
- Keep R2's repaired replay controls and their negative controls: `REPLAY-PROBE-TOOLCHAIN-ENV` (run Lean probes under `lake env`, retain the bare-`lean` failure control), `REPLAY-CLEAN-BASELINE-OUTSIDE-TREE`, `REPLAY-EXACT-REGISTRY`, `REPLAY-SELECTOR-NONVACUITY`, `REPLAY-SUBPROCESS-DEADLINE` with a root-plus-descendant sleeper self-test of at least ten seconds, and R2's three further harness fixes recorded in `WDD-20260724-003` — in particular the false clean-tree `PASS` caused by empty PowerShell event-based capture. Verify each control in the failing direction, not only the passing one.
- The full replay must operate on committed raw-Git blobs at exact HEAD, enforce the exact two-commit chronology and path scope, compare frozen row bytes outside the delimited regions, verify immutable source-port identities, run the mutations and expected accepts, restore every transform, and end with clean index/worktree/untracked state.

Forbidden shortcuts:
- No prose substitute for proof; no declaration-name inventory as execution evidence.
- **No syntactic property of an instruction block in place of its operational correspondence.** Immediate inventories, literal closure, tag membership, and list lengths do not establish that executing a block computes anything. This is the exact defect the R2 audit recorded as P2-1 and the same class as the rejected B2 R2 candidate's opcode-tag lists.
- No input-dependent code cell, sibling data object, runtime shape or proof value, target verdict callback, source-program inspection, expected-answer oracle, opaque logical load, or semantic helper disguised as one instruction.
- No proof-supplied trace/output/category/fetch field, post-hoc decorative read, synthetic event, membership-only trace claim, or theorem bypassing actual step/run evaluation.
- No `Nat`-only fit theorem in place of bounded reachable-state closure. Array bounds do not prove address width.
- No ordered-subtraction premise, and no fidelity theorem vacuous because underflow is assumed unreachable.
- No reuse of historical 11886 as a bounded instruction/fetch/category result, and no reuse of current 210 or local 33 across categories.
- No inherited `PENDING` row treated as satisfied because a predecessor built adjacent machinery.
- No universal FAIL from one failed encoding, timeout, open theorem, resource limit, large term, or one rejected implementation.
- Do not stage transcripts, archives, build products, scratch files, or unrelated changes.

Context:
- Read `AGENTS.md`, `docs/internal/RMQ_FINAL_ROADMAP.md`, accepted PREHIST blob `be80468ef049c0c94c7d1a7b3c5f8f69ccfd453f`, accepted architecture matrix blob `9eb1625a6023eb36bd2ceefdcc44396eb768f3c6`, and accepted source-port matrix blob `4f8b3c84fbac5d8ef766b5d3df1c289c097fbeb3`.
- Read `docs/internal/audit_reports/2026-07-25_C06_e1_arch2_b3route_r2.md` — the audit of your predecessor, including both findings you must not repeat.
- Read `docs/internal/E1_ARCH2_CONTRACT_REPAIR_PREP.md` for the owner decisions and the disjunction sweep, and `DD-20260724-001`, `DD-20260724-002`, `DD-20260725-003`, `WDD-20260724-001`, `WDD-20260724-003`.
- Read the inherited R2 modules `BoundedArithmetic.lean`, `BoundedTarget.lean`, `RouteDomain.lean` as your own starting point, and the accepted B1 package and `B3SourcePort` root, manifest, topology, correspondence, and pinned fixture as immutable consumers.
- Read `.agents/skills/rmq-proof-sprint/references/COMPLETION_GATE.md`; consult `KNOWN_FAILURE_MODES.md` only for applicable traps.

Completion:
- Work until the exact full B3 target closes or a valid quantifier-matched obstruction dossier forces coordinator architecture judgment. A green build, one receipt, or a candid gap is only a checkpoint.
- PASS requires a concrete allowed route, all parameterized and pinned theorems, exact same-object run, operational arithmetic correspondence, exercised truncated-subtraction fidelity, source simulation, public composition, the executed mutation campaign, and committed replay evidence.
- FAIL requires the exact universal `HistoricalFAIL` over every construction the frozen domain permits plus the checked iff bridge. Do not strengthen the premise or narrow the route type.
- If a local implementation or replay defect remains and another in-scope step is available, continue. If the unchanged twelve-constructor model forces a primitive/atomicity/cost choice, report OBSTRUCTED with the smallest checked implication and stop; do not edit Core or choose the architecture unilaterally. Subtraction and ROM typing are already decided and are not available obstructions.
- Exercise empty, singleton, size two, threshold-adjacent sizes, the pinned fixture, valid/reversed/empty/out-of-bounds queries, every reachable source constructor, live/empty/dead reads, both sub-phase witnesses, and all route branches.
- Log bounded-machine/ROM/lowering decisions in `DESIGN_DECISIONS.md` and replay/process decisions in `WORKFLOW_DESIGN_DECISIONS.md`. Every commit must independently satisfy the strict write-scope pairing rule: a commit touching a workflow-classified path carries its own `WORKFLOW_DESIGN_DECISIONS.md` entry, and one touching a code-classified path carries its own `DESIGN_DECISIONS.md` entry (`WDD-20260725-003`).
- Stage only owned files and make the exact freeze then implementation commits.

Verification:
- Development-loop checks: build the narrowest new module and direct expected-type consumer first; run bounded replay startup, one exact positive selector, and one representative mutation before the full registry. Run the toolchain-environment probe self-test in your first replay attempt, not the last.
- Final-required checks: focused `lake build` of the B3 historical-route root and independent consumer; one terminal green full committed replay; strict frozen-row/source-identity/history checks; hygiene; native-decision scan; exact range/scope/non-owned identity; restoration; clean state; `git diff --check`; `git diff --check HEAD^..HEAD` for each commit as CI does; `git diff --check 105a47bc3aeae10926dfd6866560eaf4f2be5d38..HEAD`; `powershell -ExecutionPolicy Bypass -File scripts\design_decision_check.ps1 -Strict -Base 105a47bc3aeae10926dfd6866560eaf4f2be5d38`; `powershell -ExecutionPolicy Bypass -File scripts\claim_drift_scan.ps1`.
- Conditional checks: no broad `lake build` and no `scripts/gate.ps1` unless a unique contradiction appears which a narrower named component cannot resolve.
- Use one heavy Lean/Lake process at a time. Record runtime, timeout rationale, exact tree, covered rows, and outcome. Never rerun an unchanged expensive command after a timeout. Note that `lake env lean <file>` elaborates a single module against built dependencies and is the cheap way to check one file.
- Run the full replay exactly once on the unchanged clean committed candidate after startup/selectors/mutation controls pass.

Report:
- Begin with exactly one status line. For candidate completion the first two lines must be exactly `Status: CANDIDATE_COMPLETE` and `I found no assigned or inherited acceptance criterion unmet; coordinator acceptance is still required.`
- Report handle/title, exact branch/worktree/base/freeze/HEAD/tree/parents, changed paths, source-port input identities, and clean state.
- Name the exact definitions/theorems for route domain, allowed core, codec/ROM, bounded target machine, **arithmetic block operational correspondence**, charged physical lowering, reachable width invariant, truncated-subtraction fidelity, stuttering simulation, receipts, P/Q, PASS/FAIL bridge, verdict, and expected-type consumers; quote their checked proposition shapes and object-composition chains.
- State explicitly whether `B3-HIST-17` is closed, and if not, exactly which register invariant or execution step remains unproved. Do not describe syntactic block properties as progress on it.
- Report the executed and unexecuted mutation sets exactly, and whether PC 73 reachability became kernel-checked.
- Report all three governed deviations with their citations, and the seven frozen choices with which sub-phase witness carries the ordered-case obligation.
- Include proof digestion: conceptual meaning, plain-English meaning, live assumptions, downstream A4 consumer, and skeptical-reviewer questions.
- State one of CANDIDATE_COMPLETE, OBSTRUCTED, BLOCKED, or INCOMPLETE. Request coordinator audit only; do not call the route accepted, select A4, merge, push, publish, or close E1.
