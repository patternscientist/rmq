# P1 RMQ Policy Hardening Worklog

Status: the original P1 contract and worker-reported closure are preserved as a
rejected-candidate snapshot. The frozen P1-R1 matrix and P1-R1 closure/ledger
below are the authoritative current evidence.

## Worker And Contract

- Handle/title: P1 / (P1) Harden RMQ claim and decision policy.
- Exact base: 0b8490cb1dee4f02e7c72a5e097c1fa78361c588.
- Workflow governance: 7e0e6089251147b02365bc5603ebd2347902018f.
- Branch: codex/p1-rmq-policy-hardening.
- Worktree:
  C:\Users\poin\.codex\visualizations\2026\07\19\019f7ccf-62d2-7841-b02a-506c37d2e167\p1-rmq-policy-hardening.
- Named target: make retired cost 207 impossible to present as current on a
  governed surface, and replace enumerated design-sensitive coverage with a
  default-sensitive classifier whose narrow opt-outs are durable evidence and
  decision records.
- Downstream join: the aggregate gate and emitted worker contracts must consume
  the production claim scanner, the production design classifier, and their
  replayable regressions before later public-claim migrations are certified.
- Hard obligation: close the category boundaries through the production final
  verdict, not copied regexes, remembered path lists, or count-only fixtures.
- Forbidden shortcuts: no copied detector, catch-all history allowance,
  current-surface hand list, prose-only evidence, Lean/public theorem change,
  live document migration, E1 overlap, or transcript/scratch artifact.
- Stop conditions: every frozen row closes; or a precise policy-architecture
  obstruction requires coordinator choice; or a named external dependency
  blocks further work; or the user redirects.

## Base-Derived Inventories

The exact-base registry was reconstructed from
docs/internal/CLAIM_DRIFT_POLICY.json currentFactSurfacePathRegex and git
ls-files. It contains exactly 18 paths:

1. artifact/CLAIMS.md
2. artifact/README.md
3. docs/digests/PROJECT_DIGESTION_CURRENT.md
4. docs/FAMILY_SUMMARY.md
5. docs/internal/CLAIM_DRIFT_POLICY.md
6. docs/internal/RMQ_FINAL_ROADMAP.md
7. docs/PAPER_CLAIM_CORRESPONDENCE.md
8. docs/PAPER_MAIN_THEOREM.md
9. docs/PAPER_MODEL_ADEQUACY.md
10. docs/PAPER_RELATED_WORK.md
11. docs/PAPER_THEOREM_MAP.md
12. docs/PUBLICATION_STRATEGY.md
13. docs/RELATED_WORK_AND_LIMITATIONS.md
14. docs/ROADMAP.md
15. docs/TRUST_AUDIT_PACKET.md
16. docs/WHAT_IS_PROVED.md
17. docs/WORD_RAM_REVIEW_PACKET.md
18. README.md

README.md and docs/FAMILY_SUMMARY.md already state 210. The rejected P1 worker
incorrectly concluded that the policy explanation was the only current-surface
repair. P1-R1 reconstruction found the registered
docs/PUBLICATION_STRATEGY.md adjacent-line live `207`; its factual `207` to
`210` correction and docs/internal/CLAIM_DRIFT_POLICY.md are the two expected
current-surface repairs. The registry itself remains exactly 18 paths.

## Frozen Requirement-To-Evidence Matrix

| ID | Exact frozen requirement | Intended production behavior and evidence needed | Direct consumer / composition chain | Anti-vacuity mutation | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| REQ-P1-CURRENT-COST-210-ENFORCEMENT | "on every registered current-fact surface, strict production scanning must reject prose that presents 207 as current/canonical/principled/uniform/modeled/query/charged-trace/trace-length/Costed.cost. The existing regression fixture r1r1-current-cost-207-control currently accepts this exact defect; replace that false control with a named rejection. Keep 210 accepted as the current bound." | The strict forbidden-retired-current-cost-bound policy includes 207 in the complete named role category; the real claim_drift_scan.ps1 -Strict returns nonzero and labels the term fail for every category representative, while a current 210 control exits zero. | CLAIM_DRIFT_POLICY.json -> claim_drift_scan.ps1 final allowance/strict verdict -> claim_drift_policy_regression.ps1 -> gate.ps1 and blocking CI strict scan. | Replace the false 207 control with a reject; add held-out role/order variants and current 210 accept control. | Open. |
| REQ-P1-HISTORICAL-COST-ALLOWANCE | "truthful, explicitly marked historical mentions of 207 remain accepted, as do frozen historical 76, 142, 328, and distinctly named live compatibility 352 roles. CLAIM-HISTORY-A07-COST or a more precise documented marker may carry the allowance, but an allowance word in an unrelated clause must not bypass a stale-current statement." | The existing production line allowance becomes an anchored controlled grammar: a marked historical-only clause, or the exact current-210-plus-retired-comparison shape already used by current surfaces, accepts; a marker or history word in a separate clause cannot authorize current 207. Advisory 328 and compatibility 352 remain non-strict and truthfully labeled. | Policy JSON allowance -> production scanner final verdict -> regression accept/reject controls -> gate strict scan. | Accept marked historical 207/76/142 and current-210-plus-history; reject "Historical context ...; current bound is 207" with the same marker; accept historical 328 and named compatibility 352. | Open. |
| REQ-P1-POLICY-TERM-TRUTH | "policy IDs, status strings, prose, and fixtures must not call 76 or 207 current. Preserve compatibility if renaming an internal term would break consumers; do not rename or delete any frozen public Lean identity." | Keep stable consumed policy IDs, change their status/pattern prose to call 76/142/207 retired or historical, document 210 as current, and rename the misleading 207 expected-accept fixture into a rejection without touching Lean. | Policy files and regression names -> scanner output/audit interpretation -> gate and worker evidence. | Static config checks fail if retired advisory/status text starts with or asserts current; repository search classifies every current-sounding 76/207 policy occurrence. | Open. |
| REQ-P1-DESIGN-COVERAGE-OPT-OUT | "replace the current proof/code-sensitive enumeration boundary with a documented opt-out/default-sensitive classifier. Newly invented Lean, public artifact/docs, validation, script, workflow, and repository paths outside an explicit neutral exclusion must require the appropriate DESIGN_DECISIONS.md and/or WORKFLOW_DESIGN_DECISIONS.md. Exclusions must be narrow semantic categories, not an expanding remembered list of sensitive names. The two decision-log files themselves and durable evidence-only/history/report classes need nonrecursive treatment." | design_decision_check.ps1 normalizes changed paths, classifies narrow neutral decision/evidence/history/report classes first, classifies workflow infrastructure by semantic roots, classifies Lean code as code even under tool roots, and defaults every other new repository path to code-sensitive. Correct changed decision logs satisfy the respective requirement without recursion. | Git base/worktree inventory -> production classifier -> strict exit -> CI and isolated-Git regression; template emits exact invocation. | Held-out unenumerated RMQ Lean, validation Lean, workflow script, public doc, and unknown repository path reject; worklog/audit/historical digest and decision-log-only controls accept. | Open. |
| REQ-P1-STRICT-BASE-NONVACUITY | "strict branch certification must not silently examine zero committed changes because -Base was omitted. Choose and document one compatible production behavior: require -Base in strict certification mode, or add an equally strong explicit mode that fails closed without a base. Update the worker template and completion gate so emitted prompts require -Strict -Base with the task's exact 40-character base commit. Preserve a deliberate non-strict local-worktree mode if useful." | Strict mode requires a syntactically exact, resolvable 40-character commit and never falls back; non-strict mode without Base inspects worktree/index/untracked paths as an advisory local mode. Template and completion gate require the exact-base command. | Worker prompt -> production design checker -> CI/event certification; completion gate audits emitted commands. | Strict without Base and strict invalid/short Base reject; non-strict without Base detects a held-out path and exits advisory zero rather than examining an implicit committed range. | Open. |
| REQ-P1-PRODUCTION-CLASSIFIER-REGRESSION | "add a named regression that executes the actual production scripts/design_decision_check.ps1 against isolated Git fixtures. Include category-level holdouts for an unenumerated RMQ Lean path, a new validation path, a new workflow script, an ordinary public doc, an explicitly neutral evidence/history path, missing code decision, missing workflow decision, present correct decisions, strict missing-base failure, absolute Windows paths if supported, and clean restoration. Do not validate a copied detector." | New design_decision_check_regression.ps1 creates isolated Git repositories, invokes the production checker in bounded child processes, asserts exit plus exact production diagnostic, verifies drive-qualified repository roots on Windows, and preserves the caller tracked tree. | Production checker -> new regression -> gate.ps1 exactly-once consumer. | Every listed category is a named case; missing decisions reject, correct decisions accept, and deleting or copying around the production invocation breaks expected diagnostics. | Open. |
| REQ-P1-GATE-WIRING | "wire the new regression into the aggregate gate exactly once with faithful exit propagation. The existing claim policy regression remains wired exactly once." | gate.ps1 invokes each regression once and immediately fails on its nonzero LASTEXITCODE; no second classifier or overwritten status. | Aggregate gate -> CI blocking repository gate. | Static invocation count equals one for each regression; design regression nonzero is propagated by its immediate Fail branch. | Open. |
| INV-P1-NO-PUBLIC-SEMANTIC-CHANGE | "do not edit README, FAMILY_SUMMARY, any other current fact surface, Lean code, theorem statements, frozen public identities, E1 matrices/worklogs, audit reports, roadmaps, legacy naming, or the M1 registry. Current main already states 210 consistently; do not manufacture a document migration." | Exact changed-path inventory contains only authorized policy/workflow files; no Lean or current public surface other than the authorized policy explanation changes. | Base..HEAD inventory -> coordinator integration review. | Compare changed paths to the frozen allowlist and rerun current-surface inventory. | Open. |
| INV-P1-E1-NONOVERLAP | "This task closes: the P1 workflow-policy rung only; it does not close E1, merge the campaign, change Lean semantics, or refresh legacy/M1 surfaces." | No E1, Lean, legacy, M1, campaign, merge, or public theorem path changes; worklog and report say candidate only. | Base..HEAD inventory and candidate declaration. | Search changed paths and report for unauthorized E1 closure/merge claims. | Open. |
| REPLAY-EXACT-REGISTRY | "REPLAY-EXACT-REGISTRY applies to the named policy mutation fixture inventory: duplicate IDs, missing required IDs, and verdict-count drift must fail." | claim_drift_policy_regression.ps1 carries an independent exact ordered ID sequence and pinned verdict totals, validates before execution, and runs cheap self-mutations proving duplicate, missing-middle, and verdict-flip rejection. | Frozen registry -> actual fixture objects -> production scanner executions -> final pinned totals. | duplicate-fixture-id, missing-required-fixture-id, and verdict-count-drift controls must each be rejected by the registry validator. | Open. |
| REPLAY-SELECTOR-NONVACUITY | "REPLAY-SELECTOR-NONVACUITY is NOT_APPLICABLE because no focused selector is part of this script contract." | No selector interface is introduced. | Not applicable. | Confirm both regressions run their full frozen/default case sets. | N/A by frozen contract. |
| REPLAY-SUBPROCESS-DEADLINE | "REPLAY-SUBPROCESS-DEADLINE applies only if the regression launches external Git/PowerShell children; every such stage needs a positive deadline, timeout failure, owned-process cleanup, and a cheap sleeper control." | Both regressions route external Git/PowerShell stages through bounded process helpers, classify timeout nonzero, terminate and wait for the owned process tree, and run a cheap sleeper timeout/cleanup control before semantic cases. | Bounded helper -> production child verdict -> regression exit -> gate. | sleeper-timeout-owned-cleanup must time out quickly, terminate, and leave no live owned child; semantic child timeout must fail. | Open. |
| INV-P1-INHERITED-CLAIM-CHECKS | "Preserve all inherited claim-policy attribution checks, 2^128 role checks, source/freshness/position checks, readWord-only checks, and tracked-tree restoration controls." | Existing fixtures and production paths remain in the exact registry; new validation is additive. | Claim regression exact registry -> production scanner -> aggregate gate. | Missing inherited middle fixture and changed final totals are rejected; existing path/context/restoration cases still execute. | Open. |
| CHK-P1-CLAIM-STRICT | "powershell -ExecutionPolicy Bypass -File scripts\claim_drift_scan.ps1 -Strict"; "powershell -ExecutionPolicy Bypass -File scripts\claim_drift_policy_regression.ps1" | Both final production commands exit zero; the first reports zero strict failures and the second reports exact full-registry verdict totals plus controls. | Final candidate policy surfaces and aggregate consumer. | Regression must still reject stale-current 207 even when the repository itself is clean. | Not run on candidate. |
| CHK-P1-DESIGN-STRICT | "powershell -ExecutionPolicy Bypass -File scripts\design_decision_check_regression.ps1"; "powershell -ExecutionPolicy Bypass -File scripts\design_decision_check.ps1 -Strict -Base 0b8490cb1dee4f02e7c72a5e097c1fa78361c588"; "powershell -ExecutionPolicy Bypass -File scripts\worker_prompt_preflight_regression.ps1 if the template changed" | All required final production/regression commands exit zero on the unchanged final tree. | Design classifier, template, completion gate, gate wiring. | Missing-base regression rejects while exact-base final branch certification inspects the committed range. | Not run on candidate. |
| CHK-P1-HYGIENE | "git diff --check"; "after committing git diff --check 0b8490cb1dee4f02e7c72a5e097c1fa78361c588..HEAD"; "exact changed-path inventory; clean status." | Working and committed ranges are whitespace-clean; only authorized files changed; post-commit worktree is clean. | Candidate commit and coordinator review. | Compare exact paths to write scope; no transcript/temp/cache staged. | Not run on candidate. |
| COMPLETE-P1-COMMITTED-EVIDENCE | "Stage only authorized files and commit the final unchanged candidate tree." | One final candidate commit contains the completed worklog, production changes, regressions, gate/template consumers, and workflow decision; no later content edit invalidates checks. | Candidate SHA -> coordinator reconstruction. | Post-commit diff check, exact inventory, and clean status. | Open. |

## Frozen P1-R1 Repair Contract

- Handle/title: P1-R1 / (P1-R1) Close the live-cost and neutral-path policy
  gaps.
- Exact base and workflow governance:
  b18f9d87e54230c4a3b23df5b387f654fc5687c2.
- Branch: codex/p1-rmq-policy-hardening-r1.
- Worktree:
  C:\Users\poin\.codex\visualizations\2026\07\20\019f7d23-fc9c-7823-9c13-f21306978b6e\p1-r1-worktree.
- Named target: repair the four independently reproduced production-classifier
  gaps while preserving the already-correct P1 registry, strict-base behavior,
  gate wiring, and inherited regressions.
- Downstream join: governed RMQ worker certification and the aggregate gate
  before future public-claim and proof work.
- Hard obligation: the production final verdict, rather than a copied helper or
  fixture-only classifier, must reject each frozen counterexample category and
  preserve the truthful controls on the same committed final tree.
- Forbidden shortcuts: no literal-path exceptions, copied production regexes,
  blanket evidence-directory allowances, line-wide marker exemptions, broad
  arrow suppression, registry weakening, fixture deletion, gate/template edit,
  Lean/E1/M1 change, or transcript-only evidence.
- Stop conditions: every original P1 and P1-R1 row closes on one committed,
  clean exact candidate; a checked obstruction negates the exact classifier
  contract; external state blocks a required command; or the user redirects.

The following matrix is frozen before production edits. `P` is the accepted
predicate and `Q` the rejected mutation predicate. Every verdict is the exit
code plus diagnostic of the real production script in strict mode, not a
copied regular expression. Every fixture repository must be removed and the
caller tracked-state snapshot restored after the case and after the campaign.

| ID | Exact frozen requirement | Production verdict and predicate boundary | Path/line context and anti-vacuity challenge | Expected-accept control | Restoration / status |
| --- | --- | --- | --- | --- | --- |
| REQ-P1R1-CROSS-LINE-207 | "first reproduce that the rejected P1 candidate's strict production scan exits zero while registered `docs/PUBLICATION_STRATEGY.md` says on adjacent lines that the current theorem gives checked charged-trace cap `207`. Repair that live document to 210 and make the production classifier reject an exact committed mutation with the semantic phrase and retired numeral split across adjacent lines. Do not solve only the literal file sentence; cover the declared current-cost category under bounded line context without joining unrelated distant prose." | Production strict scan rejects `Q`: a registered current surface has a bounded adjacent-line semantic unit pairing the current-cost subject/role with retired 76, 142, or 207. It accepts `P`: the same live role states 210, or retired numerals occur only in a truthful historical grammar. | Exact live path `docs/PUBLICATION_STRATEGY.md`; report both match lines. Challenge: split role/token in both orders across adjacent lines and prove a blank paragraph or unrelated distant prose is not joined. | Adjacent-line current 210; a separated historical retired-cost control. | Exact fixture ID `p1-registered-current-cost-cross-line-207-rejected`; restore mutated registered file and caller snapshot. Open. |
| REQ-P1R1-HISTORY-NONLAUNDERING | "production must reject at least `Historical note: 76 was retired and the current query bound is 207 <!-- CLAIM-HISTORY-A07-COST -->` and `The current bound is 210. Historical comparison: the current query bound is 207 <!-- CLAIM-HISTORY-A07-COST -->`. It must continue accepting explicitly historical 76, 142, 207, and 328 statements and the distinctly named live compatibility role 352. A marker or history word in one clause may not license a stale-current clause elsewhere." | Production strict scan rejects `Q`: any clause presents a retired token as current even when another clause contains history wording or the marker. It accepts `P`: each retired 76/142/207/328 occurrence is the subject of an explicit historical clause, or 352 is distinctly named live compatibility. | Registered current surfaces, clause-local grammar. Challenge both exact conjunction/compound-comparison launders and role/token order holdouts. | Explicit historical controls for 76, 142, 207, 328 and live-compatibility 352. | Named exact history-conjunction and compound-comparison fixtures; restore each fixture repository. Open. |
| REQ-P1R1-ARROW-GRAMMAR | "production must reject `The current query bound is 207 -> 210.` and other current-subject arrow laundering. It may accept only an explicitly truthful supersession/history grammar whose subject says the retired fact moved from 207 to current 210. Do not use a broad negative lookahead that suppresses all `207 -> 210` lines before semantic classification." | Production strict scan rejects `Q`: a current subject owns retired 207 even if followed by an arrow to 210. It accepts `P`: an explicitly retired/historical subject moved from 207 to current 210. | One bounded clause/line on a registered surface. Challenge current-subject `207 -> 210`, `207 => 210`, and an extra stale-current clause after a truthful arrow. | `The retired query bound moved from 207 to the current bound 210.` under the documented supersession/history grammar. | Named arrow reject and truthful supersession accept fixtures; restore fixture repository. Open. |
| REQ-P1R1-NEUTRAL-CANNOT-SHADOW-CODE | "the production design checker must reject code-bearing injections such as `docs/internal/audit_reports/P1NeutralBypass.lean`, the corresponding `.ps1` boundary, and a code-bearing file under `docs/digests/`, even though their directories are evidence-labelled. Legitimate Markdown audit reports and genuinely neutral evidence controls must remain accepted. Code identity takes precedence over neutral placement." | Production strict design check rejects `Q`: a code-bearing extension under any nominally neutral root lacks the appropriate design decision. It accepts `P`: a constrained non-code evidence file has a genuinely neutral role. | Exact audit-report `.lean` and `.ps1` paths plus a held-out digest code extension. Challenge extension case variants and neutral-root precedence. | Markdown audit report, worklog, and frozen historical digest controls. | Exact umbrella fixture `p1-neutral-evidence-path-cannot-shadow-code-or-current-surface` plus named `.ps1` and digest-code cases; delete isolated repositories and restore caller snapshot. Open. |
| REQ-P1R1-CURRENT-LOOKING-DIGEST | "the production design checker must reject an unregistered current-looking path such as `docs/digests/PROJECT_DIGESTION_CURRENT_V2.md`. Replace blanket digest neutrality with a constrained historical/evidence role rule. Preserve the explicitly current registered digest as sensitive and preserve legitimate frozen historical digest controls." | Production strict design check rejects `Q`: an unregistered current-looking digest is neutral only by directory placement. It accepts `P`: a digest has a constrained frozen-history/evidence role. The exact registered current digest remains sensitive. | `docs/digests/` filename-role boundary. Challenge `_CURRENT_V2`, alternate current-looking names, and the exact registered current digest. | Frozen dated/historical digest and exact Markdown audit evidence controls. | Exact umbrella fixture plus current-digest and historical-digest cases; delete isolated repositories and restore caller snapshot. Open. |
| REQ-P1R1-PRODUCTION-REGRESSIONS | "commit named, exact production-verdict fixtures `p1-registered-current-cost-cross-line-207-rejected` and `p1-neutral-evidence-path-cannot-shadow-code-or-current-surface`, plus the history-conjunction, compound-comparison, arrow, `.ps1`, and historical-control cases. The harness must use the real production scripts, pin exact registry integrity and verdicts, bound child processes, restore all owned fixture repositories, and fail on missing/duplicate/drifted cases." | Both committed runners invoke the real production scripts. `Q` is any missing, duplicated, reordered, verdict-drifted, timed-out, leaked, or misclassified case; `P` is the exact ordered registry with pinned verdict totals and diagnostics. | Sentence fixtures exercise registered relative and absolute/focused parser shapes; design fixtures exercise isolated Git repositories and exact paths. Challenge registry duplication, missing middle, verdict flip, timeout sleeper, and production diagnostic drift. | Every reject category has a paired truthful acceptance; existing inherited cases remain unchanged. | Positive child deadlines, owned-process cleanup, per-case fixture deletion, final caller snapshot equality. Open. |
| REQ-P1R1-EVIDENCE-TRUTH | "correct any worklog or WDD statement contradicted by the repaired implementation. Distinguish production compatibility with any resolvable Base from governed worker commands that emit an exact 40-character base. Describe Windows absolute-path testing only to the level actually exercised. `REPLAY-SELECTOR-NONVACUITY` remains not applicable because this regression contract has no user-facing focused selector; exact ordered registry integrity is still required." | Durable prose must equal implementation and measured command evidence. `Q` is a claim of literal-SHA-only production, broader Windows path coverage than exercised, category closure from counts, or a selector interface that does not exist. `P` accurately separates production Base compatibility, governed exact-SHA commands, exact registry, and exercised drive-qualified roots. | Worklog, WDD-20260719-011/012, claim policy Markdown, and final report. Challenge every assertion against script branches and named fixtures. | Truthful description of symbolic/resolvable Base behavior, actual Windows fixture root, and selector N/A. | Final diff/registry/results recorded on exact committed tree; no transcript staged. Open. |
| INV-P1R1-NO-LEAN-E1-M1 | "No Lean proposition, public theorem identity, payload bit, proof-only field, modeled tick, trace, allocation, machine state, Lean runtime, measured performance, E1 artifact, or M1 surface may change." | `P`: exact changed paths are within the authorized policy/prose/worklog/decision scope. `Q`: any Lean, E1, M1, theorem, payload, model, runtime, performance, gate, template, or unrelated path changes. | Exact base-to-HEAD changed-path inventory and public-surface inventory. Challenge the allowlist against every changed path. | The only current public prose repair is `docs/PUBLICATION_STRATEGY.md`; current-surface registry remains exactly 18. | `git diff --check`, exact range check, clean status. Open. |
| REPLAY-EXACT-REGISTRY | "exact ordered registry integrity is still required." | `P`: actual fixture IDs equal the independent ordered expected list and pinned reject/accept totals. `Q`: duplicate, missing, reordered, or verdict-flipped case. | Both production regression registries. Challenge duplicate, missing-middle, order drift, and verdict drift. | Full unchanged inherited registry plus all named P1-R1 cases. | Registry mutations are in-process only; fixture repositories and caller tree restore. Open. |
| REPLAY-SELECTOR-NONVACUITY | "`REPLAY-SELECTOR-NONVACUITY` remains not applicable because this regression contract has no user-facing focused selector." | No selector is introduced; both final commands run their complete exact registries. | Regression command surface. Challenge accidental focused-only final evidence. | Full default campaign. | N/A by frozen contract. |
| REPLAY-SUBPROCESS-DEADLINE | "The harness must use the real production scripts, pin exact registry integrity and verdicts, bound child processes, restore all owned fixture repositories, and fail on missing/duplicate/drifted cases." | `P`: every external Git/PowerShell child has a positive deadline and completed/cleaned verdict. `Q`: timeout, leaked owned child, unbounded launch, or restoration drift. | Both production regression runners. Challenge cheap sleeper timeout and semantic-child deadline. | Ordinary fast production child completes within the same helper. | Owned tree termination/wait, fixture deletion, caller snapshot equality. Open. |
| CHK-P1-CLAIM-STRICT | "on the frozen final tree, run `scripts/claim_drift_policy_regression.ps1` once; `scripts/claim_drift_scan.ps1 -Strict`." | Both exact commands exit zero; regression prints pinned totals and strict scan prints zero failures. | Frozen final content before evidence-only timing update. Challenge stale cross-line 207 fixture while repository live prose is 210. | Current 210 and truthful history controls accept. | Regression proves restoration; rerun only if a consuming source changes. Open. |
| CHK-P1-DESIGN-STRICT | "on the frozen final tree, run `scripts/design_decision_check_regression.ps1` once; `scripts/design_decision_check.ps1 -Strict -Base b18f9d87e54230c4a3b23df5b387f654fc5687c2`." | Both exact commands exit zero; regression prints pinned totals and exact-base checker covers the committed/working range. | Frozen final content. Challenge missing Base remains rejected in fixture; final governed command uses exact base. | Correct decision records and neutral evidence controls accept. | Regression proves restoration; rerun only if a consuming source changes. Open. |
| CHK-P1-HYGIENE | "on the frozen final tree, run exact registry/changed-path checks; `git diff --check`; `git diff --check b18f9d87e54230c4a3b23df5b387f654fc5687c2..HEAD`; and clean status. Record durations, exact HEAD, and verdict totals." | All exact checks exit zero and inventory is authorized. | Working and committed exact ranges. Challenge unauthorized/untracked file and registry count drift. | Exact 18 registered surfaces and intended changed paths only. | Post-commit status empty; no scratch/transcript/archive staged. Open. |
| COMPLETE-P1R1-COMMITTED-EVIDENCE | "Work until every original P1 and P1-R1 row is closed on one committed, clean exact candidate, or return a checked obstruction that negates the exact classifier contract." | One candidate commit contains the production fixes, exact replay evidence, truthful worklog, and minimal decision updates; all inherited P1 rows remain satisfied. | Base `b18f9d...` to candidate HEAD, first-parent/second-parent ancestry retained. Challenge post-commit range diff and exact parent check. | No merge, push, integration, worktree deletion, or unrelated change. | Stage only intended files; commit; prove clean status and committed-range diff. Open. |

## Explicit Deferrals

Merging main into claude/b1-b2-charged-fringe-tables; REQ-E1-07/09 notes;
claude/e1-cost-algebra; ProgramFits; legacy naming; M1 refresh; Lean semantics;
and public-document migration remain owner/dependency blocked and are not
required for this workflow-policy target.

## Verification Command Ledger

| Command | Role / covered rows | Unique failure mode | Planned tree / timeout basis | Final duration and result |
| --- | --- | --- | --- | --- |
| JSON parse plus exact policy ID/status checks | Development; current-cost, history, term truth | malformed policy or missing/duplicated term | Dirty development tree; 30 s | Pending. |
| Focused production claim fixtures for current 207/current 210/historical costs | Development; current-cost/history | allowance-bypass or role-category miss | Dirty development tree; 60 s per production child through bounded runner | Pending. |
| design_decision_check_regression.ps1 | Development then final; design opt-out, strict base, production regression, subprocess deadline | copied/enumerated detector, base vacuity, path-category miss, child leak | Frozen candidate; closest scripts are sub-minute to a few minutes; outer timeout 10 min, internal positive stage deadlines | Pending. |
| claim_drift_policy_regression.ps1 | Final; claim rows, exact registry, inherited checks, deadline | policy category/allowance/path/final-exit regression | Frozen candidate; base regression is roughly two minutes; outer timeout 10 min, internal positive stage deadlines | Pending. |
| claim_drift_scan.ps1 -Strict | Final; repository current-surface verdict | live strict violation absent from fixtures | Frozen candidate; observed base scan 22 s; timeout 2 min | Pending. |
| design_decision_check.ps1 -Strict -Base exact SHA | Final; committed-range design coverage | zero-range fallback or missing workflow entry | Frozen candidate; timeout 1 min | Pending. |
| worker_prompt_preflight_regression.ps1 | Final because template changes | emitted prompt omits exact strict base | Frozen candidate; timeout 5 min | Pending. |
| git diff --check and hygiene rg | Final hygiene | whitespace or forbidden source/trust token | Frozen candidate; timeout 1 min each | Pending. |
| scripts/gate.ps1 | Conditional only if focused regression cannot prove exact aggregate invocation/exit propagation | aggregate wiring/status overwrite | Unchanged final tree only; recent gate evidence is about 4-5 min, timeout 15 min | Pending trigger decision. |
| Post-commit diff check, exact paths, status | Final committed evidence | uncommitted or unauthorized material | Candidate commit; timeout 1 min | Pending. |

No Lean/Lake build is planned: no Lean/import/public-theorem path is authorized.

## Proof Digestion

Conceptual meaning, plain-English meaning, live assumptions, downstream
consumers, and skeptical-reviewer questions will be completed after the
production verdicts close. The live assumptions already frozen are that Git
returns repository-relative changed paths, the claim scanner remains the sole
production claim verdict, and workflow policy does not establish any
mathematical, payload, cost-model, machine, runtime, or performance claim.

## Rejected P1 Production-Design Snapshot

The following description records the rejected candidate's intended design.
Its blanket digest/audit neutrality, line-local current-cost match, and broad
arrow non-match are superseded by the frozen P1-R1 contract and evidence below.

The claim policy is a controlled line-language classifier, not unrestricted
natural-language understanding. It pairs the retired tokens 76, 142, and 207
with the frozen live-role vocabulary and delegates path, line, allowance,
strictness, and exit status to claim_drift_scan.ps1. Its historical grammar
admits a single marked historical clause, the existing current-210 comparison
line, the existing live-compatibility-352 comparison line, and the exact
supersession arrow 207 -> 210. A second stale-current clause remains visible to
the same production verdict.

The design checker is an opt-out classifier. Decision records, internal
worklogs/acceptance matrices, audit reports, historical digests other than the
current publication digest, and the digestion evidence log are neutral.
Workflow roots are .agents, .codex, .github, scripts, AGENTS.md, and
docs/internal. Lean remains code-sensitive under every root. Every other
non-neutral path defaults to code-sensitive. Strict mode requires a resolvable
base and never falls back; symbolic refs remain compatible with CI, while the
template and completion gate require each governed worker's exact
40-character base commit.

## Rejected P1 Worker-Reported Closure Snapshot

These were the rejected P1 worker's claimed closures, not coordinator-accepted
facts. WDD-20260719-012 and the P1-R1 reproduction reopened the affected rows.

| ID | Production evidence and anti-vacuity result | Status |
| --- | --- | --- |
| REQ-P1-CURRENT-COST-210-ENFORCEMENT | Production fixtures p1-current-cost-207-rejected, p1-canonical-query-cost-207-heldout, p1-uniform-modeled-trace-length-207-heldout, p1-costed-cost-207-heldout, and p1-number-first-current-charged-trace-207 all reject. p1-current-cost-210-control accepts. Repository strict scan reports 1,290 hits and zero failures. | Closed. |
| REQ-P1-HISTORICAL-COST-ALLOWANCE | Marked 207/76/142, historical 328, compatibility 352, and supersession-arrow controls accept. Both unrelated-clause marker/history bypasses and a second 207 after the arrow reject. README and PAPER_MODEL_ADEQUACY existing comparison forms receive allowed verdicts. | Closed. |
| REQ-P1-POLICY-TERM-TRUTH | Stable consumed IDs remain; the new 207 advisory status is historical-retired, the strict status calls 76/142/207 forbidden when presented as current, the false 207 control was replaced by a named reject, and no Lean identity changed. | Closed. |
| REQ-P1-DESIGN-COVERAGE-OPT-OUT | The production isolated-Git regression rejects held-out RMQ Lean, validation Lean, workflow script, public doc, unknown path, missing code decision, and missing workflow decision. Correct code/workflow decisions and every frozen neutral class accept. | Closed. |
| REQ-P1-STRICT-BASE-NONVACUITY | strict-missing-base and strict-unresolvable-base reject; nonstrict-local-worktree-mode detects the held-out Lean path and exits advisory zero; exact-base branch certification checks the complete changed range. | Closed. |
| REQ-P1-PRODUCTION-CLASSIFIER-REGRESSION | design_decision_check_regression.ps1 invokes the real checker in 18 isolated Git cases: 10 reject, 8 accept, drive-qualified Windows root checked, sleeper cleanup checked, and caller state restored. | Closed. |
| REQ-P1-GATE-WIRING | aggregate-gate-wiring proves exactly one design regression call, exactly one inherited claim regression call, and immediate design-regression LASTEXITCODE propagation. | Closed. |
| INV-P1-NO-PUBLIC-SEMANTIC-CHANGE | Exact changed-path inventory is the ten authorized paths only. No Lean, README, FAMILY_SUMMARY, other live claim surface, theorem, registry, roadmap, audit, or E1 file changed. | Closed. |
| INV-P1-E1-NONOVERLAP | Diff and report remain workflow-policy only and make no E1, merge, legacy, M1, or roadmap-closure claim. | Closed. |
| REPLAY-EXACT-REGISTRY | The claim runner pins 94 ordered sentence IDs plus 15 ordered context IDs. duplicate-fixture-id-control, missing-required-fixture-id-control, verdict-count-drift-control, exact-fixture-registry, exact-context-registry, and final pinned totals are production failures if perturbed. | Closed subject to the final unchanged-tree replay recorded in the worker response. |
| REPLAY-SELECTOR-NONVACUITY | No focused selector exists; both regressions execute their complete registries. | Not applicable by frozen contract. |
| REPLAY-SUBPROCESS-DEADLINE | Both runners require positive deadlines for Git/PowerShell stages, return 124 on timeout, terminate and wait for the owned process tree, and pass a cheap five-second sleeper under a 200 ms deadline. The initial synchronous-pipe deadlock was repaired by beginning asynchronous stdout/stderr draining before the wait. | Closed. |
| INV-P1-INHERITED-CLAIM-CHECKS | All inherited 2^128, removed-alias, source/freshness/position, readWord-attribution, absolute-path, path/line allowance, and tracked-state cases remain in the pinned production replay and pass. | Closed subject to final replay attestation. |
| CHK-P1-CLAIM-STRICT | Development production run: full claim regression exit 0 in 292.6 s with 62 reject, 32 accept, 15 context verdicts and clean restoration; strict repository scan exit 0 in 10.7 s with 1,290 hits and zero failures. Final unchanged-tree results are response-attested. | Closed when final reruns below pass. |
| CHK-P1-DESIGN-STRICT | Development production regression exit 0 in 37.8 s with 10 reject/8 accept and gate wiring; exact-base strict checker exit 0 in 3.3 s over 10 changed files; worker-prompt regression exit 0 in 39.9 s. Final unchanged-tree results are response-attested. | Closed when final reruns below pass. |
| CHK-P1-HYGIENE | Working-tree diff check is clean; final hygiene, committed-range diff, exact inventory, and clean status remain in the final sequence. | Closed when final and post-commit checks pass. |
| COMPLETE-P1-COMMITTED-EVIDENCE | The intended commit contains only the authorized production policy, regressions, gate/template consumers, workflow decision, and this worklog. Candidate SHA and clean post-commit evidence are response-attested. | Closed when the final commit and post-commit checks succeed. |

## Rejected P1 Observed Command Ledger

| Command | Tree state | Timeout | Duration / result |
| --- | --- | --- | --- |
| Project skill preflight against governance 7e0e6089... | Clean exact base 0b8490c... | 120 s | 7.7 s, exit 0; canonical checkout/working/runtime inventories agree. |
| JSON parse, policy-term uniqueness/status checks, PowerShell syntax creation | Dirty development tree | 30 s | Each exit 0; policy version 20, one strict retired-current term, scripts parse. |
| claim_drift_scan.ps1 -Strict | Functionally final policy/tree before evidence update | 120 s | 10.7 s, exit 0; 1,290 hits, zero strict failures. |
| claim_drift_policy_regression.ps1 | Functionally final policy/runner before exact context-ID pin | 600 s | 292.6 s, exit 0; 94 ordered sentence fixtures, 62 reject, 32 accept, 15 context verdicts, clean restoration. |
| design_decision_check_regression.ps1 | Functionally final classifier/gate before evidence update | 300 s | 37.8 s, exit 0; exact gate wiring, 10 reject, 8 accept, sleeper cleanup, clean restoration. |
| design_decision_check.ps1 -Strict -Base 0b8490cb1dee4f02e7c72a5e097c1fa78361c588 | Dirty complete implementation before evidence update | 60 s | 3.3 s, exit 0; 10 changed files, 0 code, 8 workflow, 2 neutral. |
| worker_prompt_preflight_regression.ps1 | Functionally final template | 300 s | 39.9 s, exit 0; all named cases pass. |
| claim regression -AbsoluteWindowsOnly | Development parser/deadline control | 120 s | 16.3 s before async-drain repair and 11.8 s after exact-context instrumentation; both exit 0 with two production path verdicts and clean restoration. |

The aggregate gate is skipped if the final design regression again proves the
exact invocation counts and immediate exit propagation. That focused proof is
the conditional trigger named in the frozen contract; running the broad Lean
gate would duplicate unrelated builds despite no Lean/import/theorem change.

## Rejected P1 Proof-Digestion Snapshot

Conceptually, omission is no longer an acceptance mechanism. A retired cost is
classified by its semantic role vocabulary, and a new repository path is
design-sensitive unless it belongs to a small documented evidence class.

In plain English: writing "the current bound is 207" in any registered live
surface now breaks the real gate, while honest history still works. Adding a
new proof, validator, public document, workflow script, or unknown repository
format now demands the right durable decision record even if nobody remembered
its filename in advance.

Live assumptions:

- Git diff and ls-files provide repository-relative paths; the checker also
  normalizes rooted paths under the repository.
- Regex-based claim policy remains a controlled tripwire, not a theorem about
  unrestricted prose semantics.
- Symbolic Base refs remain production-compatible for CI; governed worker
  prompts supply exact 40-character commits.
- Workflow evidence changes no mathematical or executable RMQ semantics.

Downstream consumers are scripts/gate.ps1, blocking CI's existing strict claim
and base-relative design steps, the worker prompt template, and the proof
completion gate.

Skeptical reviewer questions and answers:

- Could "historical" in a first clause hide current 207 later? No; both named
  unrelated-clause mutations reject through the production scanner.
- Could a new extension or directory evade the design gate? No; unknown
  non-neutral paths default to code-sensitive.
- Could the decision logs demand themselves recursively? No; they are exact
  neutral records while their presence satisfies the corresponding class.
- Could strict mode silently inspect only a clean worktree? No; missing or
  unresolvable Base exits nonzero before classification.
- Do the regressions test copied logic? No; every semantic case launches the
  production script, and the gate wiring check pins its aggregate consumer.

## P1-R1 Authoritative Closure Table

| ID | Production evidence, predicate boundary, control, and restoration | Status |
| --- | --- | --- |
| REQ-P1R1-CROSS-LINE-207 | Base reproduction: strict production scan exited 0 in 11.429 s while `docs/PUBLICATION_STRATEGY.md:153-154` paired the current checked charged-trace cap with `207`. The live path now says `210`. `p1-registered-current-cost-cross-line-207-rejected` and number-first adjacent-line holdout reject; `p1-cross-line-blank-paragraph-boundary-control` accepts. The pattern consumes at most one line boundary and never a blank paragraph. | Closed. |
| REQ-P1R1-HISTORY-NONLAUNDERING | `p1-history-conjunction-current-207-rejected`, `p1-compound-comparison-current-207-rejected`, and both inherited unrelated-clause bypasses reject. Historical 76/142/207/328 and live-compatibility 352 controls accept through the production verdict. | Closed. |
| REQ-P1R1-ARROW-GRAMMAR | `p1-current-subject-arrow-207-to-210-rejected`, the `=>` holdout, and the second-current-clause bypass reject. `p1-supersession-arrow-207-to-210-control` accepts only the explicit retired-from-207-to-current-210 grammar. The detector no longer suppresses arrow forms before allowance classification. | Closed. |
| REQ-P1R1-NEUTRAL-CANNOT-SHADOW-CODE | Valid base reproduction made audit `.lean`, audit `.ps1`, and digest `.ps1` each exit 0 while Markdown audit and dated digest controls accepted; all six temporary repositories restored in 14.933 s. After repair, the corresponding focused rejects and controls all passed in 26.327 s. The exact umbrella, audit-PS1, and digest-code production regression cases reject. | Closed. |
| REQ-P1R1-CURRENT-LOOKING-DIGEST | Base reproduction accepted `PROJECT_DIGESTION_CURRENT_V2.md`. The repair accepts only direct Markdown audit reports, non-`CURRENT` date-frozen digest names, and explicit `HISTORY`/`LOG` roles. The umbrella case, held-out current status, and exact registered current digest reject; both frozen-history controls accept. | Closed. |
| REQ-P1R1-PRODUCTION-REGRESSIONS | Claim runner pins 101 ordered sentence fixtures: 68 reject and 33 accept, plus 15 ordered context verdicts. Design runner pins 25 ordered cases: 15 reject and 10 accept. Both execute real production scripts, reject duplicate/missing/verdict-drift registry mutations, pass sleeper cleanup, remove owned fixture trees, and report caller tracked state unchanged. | Closed. |
| REQ-P1R1-EVIDENCE-TRUTH | Policy/WDD/worklog now distinguish any resolvable production `Base` from exact 40-character governed worker commands. Windows evidence is limited to drive-qualified absolute fixture/root paths actually exercised. No selector exists; complete default registries run. Original P1 closure text is explicitly historical and rejected. | Closed. |
| INV-P1R1-NO-LEAN-E1-M1 | Exact inventory reports the ten authorized policy/prose/worklog/decision files. No Lean, E1, M1, gate, template, completion-gate, theorem, payload, cost-model, trace, store, runtime, or performance path changes. | Closed. |
| REPLAY-EXACT-REGISTRY | Both runners compare actual ordered IDs with independent frozen sequences and independently pin reject identities/totals. Duplicate, missing-middle, and verdict-flip controls reject before semantic execution. | Closed. |
| REPLAY-SELECTOR-NONVACUITY | No user-facing focused selector was added; both long commands ran complete default registries. | N/A by frozen contract. |
| REPLAY-SUBPROCESS-DEADLINE | Every harness-owned Git/PowerShell child uses a positive deadline. Both sleeper controls classify timeout and clean the owned tree; both final campaigns prove caller-state restoration. | Closed. |
| CHK-P1-CLAIM-STRICT | Functionally frozen strict repository scan exited 0 in 12.389 s with 1,353 hits and zero failures. Complete claim regression exited 0 in 445.580 s with exact 101/68/33/15 totals and restoration. Post-evidence strict scan exited 0 in 8.535 s with 1,382 hits and zero failures. | Closed. |
| CHK-P1-DESIGN-STRICT | Complete design regression exited 0 in 76.281 s with exact 25/15/10 totals, aggregate-gate wiring, deadlines, absolute drive-qualified root, and restoration. The corrected ordinary-environment exact-base command exited 0 in 1.847 s over exactly 10 changed paths: 1 code, 6 workflow, 3 neutral. | Closed. |
| CHK-P1-HYGIENE | JSON and all four PowerShell scripts parse. Exact inventory reports 10 authorized changed paths and 18 registered current surfaces. Working diff check exits 0; hygiene scan finds zero trust-boundary tokens. The candidate report records the required post-commit exact-range check and clean status. | Closed when carried by the clean candidate commit reported with this matrix. |
| COMPLETE-P1R1-COMMITTED-EVIDENCE | This matrix, production fixes, exact replay registries, decision entries, and live correction are the intended candidate tree. The candidate report records its exact SHA, ancestry, committed-range diff check, and clean status; no merge or push is authorized. | Closed when carried by that reported clean candidate commit. |

## P1-R1 Command-Time Ledger

Measured execution is separated from reasoning/editing time. Reasoning and
editing were not instrumented and are not reported as measured duration.

| Command / activity | Tree and role | Measured duration / exact result |
| --- | --- | --- |
| Project skill preflight | Clean exact base/governance `b18f9d...` | 9.3 s, exit 0; expected/checkout/working/runtime skills all `rmq-audit-prompt,rmq-coordinator,rmq-proof-sprint`; required role present. |
| Fetch and base/parent verification | Pre-worktree | 2.1 s fetch, exit 0; exact base resolved; parents are rejected P1 `18d766...` then governance `af2db0...`. |
| Base live claim reproduction | Matrix-only dirty tree | 11.429 s, exit 0; 1,327 hits, zero failures despite `PUBLICATION_STRATEGY.md:153-154` live `207`. |
| Initial isolated design probe attempts | Development diagnostic, not classifier evidence | First attempt: 16.4 s, invalid because Git excludes access failed and cleanup initially hit read-only `.git`; validated temp target was restored in 2.0 s. Second attempt: 23.481 s, invalid because the production child ran from the caller rather than fixture repository; all fixture roots restored. Neither result closes a row. |
| Valid base design reproduction | Isolated Git, real production checker | 14.933 s, exit 0; four required rejects instead exited 0 (audit `.lean`, audit `.ps1`, digest `.ps1`, current-looking digest); two expected-accept controls exited 0; restoration true. |
| JSON/PowerShell parse check | Dirty implementation | 3.3 s, exit 0; JSON plus all four scripts parsed. |
| Focused claim boundary | Dirty implementation, before long suite | 36.841 s, exit 0; five rejects and four accepts exactly matched; fixture root restored. |
| Focused design boundary | Dirty implementation, before long suite | 26.327 s, exit 0; five rejects and two accepts exactly matched; fixture root restored. |
| Strict live claim scan | Functionally frozen production/prose | 12.389 s, exit 0; 1,353 hits, zero strict failures. |
| `scripts/design_decision_check_regression.ps1` | Functionally frozen production inputs | 76.281 s, exit 0; 25 ordered cases, 15 reject, 10 accept, exact registry mutations rejected, timeout cleanup and caller restoration pass. |
| `scripts/claim_drift_policy_regression.ps1` | Functionally frozen production inputs, run only after focused boundaries passed | 445.580 s, exit 0; 101 ordered fixtures, 68 reject, 33 accept, 15 context verdicts, exact registry mutations rejected, timeout cleanup and caller restoration pass. |
| Post-evidence `scripts/claim_drift_scan.ps1 -Strict` | Evidence-updated tree | 8.535 s, exit 0; 1,382 hits, zero strict failures. |
| First post-evidence exact-base design check with `GIT_CONFIG_GLOBAL=NUL` | Diagnostic only; invalid path-inventory environment | 1.453 s, exit 0, but disabling the user global config also disabled `core.autocrlf`, so Git reported 25 paths including 15 unchanged line-ending-sensitive scripts. Not used as exact-path evidence. |
| Corrected `scripts/design_decision_check.ps1 -Strict -Base b18f9d87e54230c4a3b23df5b387f654fc5687c2` | Ordinary production environment | 1.847 s, exit 0; exactly 10 changed files, 1 code, 6 workflow, 3 neutral. |
| Exact changed-path and current-surface inventory | Evidence-updated tree | 1.368 s, exit 0; 10 authorized changed paths, exact 18 registered current surfaces. |
| `git diff --check` | Evidence-updated working tree | 0.324 s, exit 0. |
| Required hygiene scan over `RMQ` and `lakefile.toml` | Evidence-updated working tree | 2.995 s, `rg` exit 1 meaning zero matches; wrapper exit 0. |
| Post-ledger `scripts/claim_drift_scan.ps1 -Strict` | Final pre-commit content tree | 11.082 s, exit 0; 1,382 hits, zero strict failures. |
| Post-ledger exact-base design check | Final pre-commit content tree | 1.928 s, exit 0; exactly 10 changed files, 1 code, 6 workflow, 3 neutral. |
| Post-ledger `git diff --check` | Final pre-commit content tree | Exit 0. |

The long claim replay was deliberately delayed until the direct adjacent-line,
history, arrow, code-shadow, digest-role, and expected-accept controls passed
and all production scripts, policy data/prose, live correction, regressions,
and decision entries were functionally frozen. It was not used as a discovery
loop. No Lean/Lake or aggregate gate run is planned: no Lean/import/theorem
surface or accepted gate wiring changed, and the design production regression
re-proves exact-once gate invocation plus immediate failure propagation.

## P1-R1 Proof Digestion

Conceptually, the claim classifier now reasons over the smallest document unit
that contained the real defect: one line plus at most one continuation. Its
allowances apply to the retired fact's clause, not to a marker or arrow token
somewhere else. The design classifier now decides executable identity and
constrained evidence role before it grants neutral placement.

In plain English, wrapping `207` onto the next line no longer hides a false
current cost, and putting code or a new current-looking digest under an
evidence-labelled directory no longer makes it evidence. Honest history,
Markdown audit reports, and frozen dated/history digests still work.

Live assumptions:

- `rg --multiline` preserves full matching lines and uses the policy's explicit
  single-boundary pattern; the rule remains a controlled language tripwire,
  not unrestricted semantic understanding.
- Git supplies repository-relative changed paths; rooted paths under the
  repository are normalized, and the Windows regression exercises a
  drive-qualified fixture root/path rather than claiming every Windows path
  spelling.
- Production strict design checks accept any resolvable Base; governed worker
  evidence uses an exact 40-character base.
- This workflow policy changes no mathematical, payload, cost-model, trace,
  store, machine, Lean-runtime, or measured-performance fact.

Downstream consumers are the inherited aggregate-gate calls to both production
regressions, the blocking strict repository claim scan, and governed exact-base
design certification before future RMQ worker claims are accepted.

Strongest skeptical question: can another grammatical form present a retired
value as current without using the frozen role vocabulary or can a novel code
format escape the extension set? The answer is deliberately scoped: finite
fixtures are lower bounds, the current-cost classifier owns exactly its
documented role/token language, and unknown non-neutral paths still default to
design-sensitive. A newly recognized code extension under a constrained
neutral class must be added as a category rule with a held-out production
mutation; this candidate does not claim unrestricted natural-language or file-
type inference.
