# W22 U3 Semantic Publication Topology Acceptance Matrix

Branch: `codex/rmq-u3-principled-allsize-cost`

Continuation base: `8859911c7e3ca4f759acfbe7b1041f5a2dd3040f`.

A05 finding-resolution report read directly, without merge:
`234bc47:docs/internal/audit_reports/2026-07-14_A05_u3_topology_resolution_audit.md`
from `C:\Users\poin\Documents\RMQ\.worktrees\a05-u3-topology-resolution-audit`.

Status: `CANDIDATE_COMPLETE`. The accepted U3 Lean theorem, canonical
reviewer payload, physical words, global trace, compatibility module, and
uniform charged-trace bound `76` are invariants of this repair.

## One Canonical Proposition

Every designated current-public document must teach the same proposition:

1. `RMQ.Headlines.succinctRMQCanonicalReviewerPayloadGlobalWordTraceTwoSidedProfile`
   names the canonical construction-facing object.
2. Its payload is the at-most `2*n + o(n)` canonical reviewer payload, its
   physical words erase exactly to that payload, and every successful read in
   the same canonical global trace has direct positional physical backing.
3. Valid half-open leftmost RMQ queries are exact through that trace.
4. Every emitted event is a genuine read/rank/select event, the synthetic
   marker is absent, and the non-synthetic certificate sum equals both trace
   length and the same `Costed.cost`.
5. That charged trace is uniformly bounded by `76` for every size and query.
6. The result is a charged-trace theorem. Controller operations remain outside
   the charged event model; it is not a conventional word-RAM, compiled Lean
   runtime, serialized-payload-query, or preprocessing theorem.

## Consolidated U3 / A05 / SC / FH Contract

| ID | Frozen requirement | Production evidence | Adversarial requirement | Status |
| --- | --- | --- | --- | --- |
| W22-SEM-01 | Replace the dated-as-current digest with one unambiguous `PROJECT_DIGESTION_CURRENT.md` written against the canonical proposition above. Remove or quarantine the dated competitor and update every live link. | One current digest path is registered in the role manifest and required by topology lint; no live link names the former dated current digest. | Removing a canonical clause from the current digest rejects. | Satisfied. |
| W22-SEM-02 | Rewrite current `FAMILY_SUMMARY.md` status prose so it teaches the canonical payload, trace, `76` cap, and model boundary. Move detailed retired route/cost exposition to one explicit compatibility/history document. | Current summary has no retired U3 numeric or dispatch proposition; the compatibility/history location is linked but not duplicated. | Injecting a retired public bound or dispatch sentence into the family summary rejects. | Satisfied. |
| W22-SEM-03 | Define one machine-readable document-role manifest shared by claim-drift and topology enforcement. Roles are `current-public`, `compatibility`, `audit-evidence`, and `exact-frozen-snapshot`. | Both production scripts load the same manifest; current-public paths and frozen path-and-line exceptions are not duplicated in either script. | A missing/duplicate role, missing manifest file, or misplaced frozen marker rejects. | Satisfied. |
| W22-SEM-04 | Make obsolete U3 cost and route propositions blocking on every designated current-public surface, independent of natural-language negation. | Role-aware strict claim policy and topology lint reject obsolete semantic patterns anywhere in a current-public document. | Exact mutations "public all-size bound is 4144", "Ready threshold costs 118", current Ready/non-Ready/zero-block dispatch, and current `2^128` activation all reject. | Satisfied. |
| W22-SEM-05 | Preserve truthful compatibility, historical, proof-only, and exact frozen-snapshot evidence without granting directory-wide exemptions. | Compatibility/history prose accepts only on a registered non-current role; the existing two exact frozen lines remain path-, line-, and count-sensitive. | Truthful compatibility/history mutations accept; the same text in a current-public path rejects; forged or moved frozen text rejects. | Satisfied. |
| W22-SEM-06 | Require positive semantic coherence, not merely absence of old names. | The canonical digest, `FAMILY_SUMMARY.md`, `README.md`, `artifact/CLAIMS.md`, and `PAPER_THEOREM_MAP.md` each contain `76`, canonical reviewer payload/global trace identity, and the charged-trace/controller boundary. All other designated current-public documents carry the same core proposition. | A canonical `76` statement in a current-public surface accepts; deleting any required semantic anchor rejects. | Satisfied for all 23 current-public paths. |
| W22-SEM-07 | Preserve SC symbol-resolution closure and FH exact-history closure. | Repository-wide removed-name search, generated Lean resolution under the correct import, exact frozen line registry, and compatibility-name checks remain blocking. | Dead/retired aliases, compatibility-as-current aliases, wrong-scope history, and forged markers reject. | Satisfied. |
| W22-SEM-08 | Record the reusable failure mode in workflow and code-design decisions: symbol resolution and retired-name closure do not establish proposition-level publication coherence. | Workflow and code decision record the rejected lexical-only, negation-based, duplicated-path-list, and competing-current-digest alternatives plus paper-exposition consequences. | A green name-resolution scan with a stale current proposition is explicitly non-complete. | Satisfied. |
| W22-SEM-09 | Run the requested topology, policy, strict claim/design, paper, axiom, hygiene, and aggregate gates, then manually inventory every designated current-public proposition after scripts pass. | Completed-tree command ledger plus manual path-by-path proposition inventory. | Any post-gate semantic edit invalidates the affected ledger and inventory. | Satisfied; ledger below. |
| W22-SEM-10 | Commit and push the same branch without merging the A05 audit branch. | Exact staged set, pushed commit, clean worktree, and remote-tip equality. | Lean/execution drift or an audit-branch merge is a scope failure. | Candidate tree frozen; final mechanical commit/push evidence is reported after this matrix is committed. |

## Semantic Occurrence Classification

This table classifies the accumulated sensitive vocabulary by proposition,
not merely by token. The document-role manifest supplies the path role; the
production policies enforce the current-public disposition.

| Occurrence | Semantic class | Current-public disposition | Allowed non-current role |
| --- | --- | --- | --- |
| `76` as the uniform bound on the canonical reviewer global trace | current | Required together with payload/trace identity and the model boundary. | May also appear in audit evidence and historical comparisons. |
| `328` as the former canonical transitional sum | historical | Forbidden, even when negated or called historical in the current document; link to compatibility history instead. | `compatibility`, `audit-evidence`, or an exact snapshot. |
| `118` as a fast/Ready threshold cost | compatibility | Forbidden as a current bound or route explanation. | `compatibility` or `audit-evidence`. |
| `4144` as a public all-size or route-split bound | compatibility | Forbidden as a current bound or route explanation. | `compatibility` or `audit-evidence`. |
| `196727` as an aggregate/large-regime query bound | compatibility | Forbidden as a current bound or route explanation. | `compatibility`, `audit-evidence`, or an exact snapshot. |
| `Ready`, non-Ready, and zero-block as the current reviewer dispatch | compatibility | Forbidden. The canonical reviewer trace is uniform and does not publish that dispatch. | `compatibility` or `audit-evidence`. |
| route split as the current reviewer topology | compatibility | Forbidden. | `compatibility` or `audit-evidence`. |
| large regime as a canonical U3 activation regime | compatibility | Forbidden. Unrelated current Fischer-Heun uses remain current and are classified by family/context rather than token alone. | `compatibility` or `audit-evidence`. |
| `2^128` as the retired activation expression | compatibility | Forbidden from the present publication story. | `compatibility` or `audit-evidence`. |
| symbolic `N = 2^128` used only to witness sparse-local nonvacuity | proof-only | Forbidden from the current execution narrative. | `audit-evidence` or an explicitly proof-only compatibility note. |
| registered June snapshot marker-plus-line occurrence | historical | Forbidden outside the exact registered path and line. | `exact-frozen-snapshot` only. |

## Manual Current-Public Proposition Inventory

This inventory was performed path by path after the production topology lint,
topology regression, and claim-policy regression passed. The final manifest has
23 current-public documents, and all 23 match all six positive patterns for the
canonical reviewer payload, canonical global trace, `76`, charged-trace model,
controller exclusion, and non-conventional-word-RAM boundary.

| Current-public document | Scope | Positive proposition | Sensitive-occurrence classification |
| --- | --- | --- | --- |
| `README.md` | RMQ publication | Complete | None. |
| `artifact/README.md` | RMQ publication | Complete | None. |
| `artifact/CLAIMS.md` | RMQ publication | Complete | None; the residual conditional U3-regime sentence found by the manual pass was migrated to compatibility history. |
| `docs/README.md` | publication index | Complete | None. |
| `docs/FAMILY_SUMMARY.md` | RMQ publication and family inventory | Complete | Twelve classified lexemes: four current non-U3 Fischer-Heun/LCA occurrences and eight proof-only source identifiers; exact classification follows below. No retired U3 numeric, activation, or route proposition remains. |
| `docs/PAPER_CLAIM_CORRESPONDENCE.md` | paper map | Complete | None. |
| `docs/PAPER_THEOREM_MAP.md` | paper map | Complete | None. |
| `docs/PAPER_MAIN_THEOREM.md` | paper exposition | Complete | None. |
| `docs/PAPER_MODEL_ADEQUACY.md` | paper model | Complete | None. |
| `docs/WHAT_IS_PROVED.md` | public theorem status | Complete | None. |
| `docs/ARTIFACT_REPRODUCIBILITY.md` | artifact publication | Complete | None. |
| `docs/RELATED_WORK_AND_LIMITATIONS.md` | publication context | Complete | None. |
| `docs/PAPER_RELATED_WORK.md` | paper context | Complete | None. |
| `docs/PUBLICATION_STRATEGY.md` | publication strategy | Complete | None. |
| `docs/TRUST_BASE.md` | public trust boundary | Complete | None. |
| `docs/AI_ASSISTED_DEVELOPMENT_NOTE.md` | public process note | Complete | None. |
| `docs/HUB.md` | other public family surface | Complete | None. |
| `docs/RANK_SELECT_FRONTIER.md` | other public family surface | Complete | None. |
| `docs/UNION_FIND_FRONTIER.md` | other public family surface | Complete | None. |
| `docs/REPOSITORY_STRATEGY.md` | repository-public strategy | Complete | None. |
| `docs/digests/RANK_SELECT_COMPRESSED_FID_2026_06_29.md` | other public family digest | Complete | None. |
| `docs/digests/UNION_FIND_TARJAN_ARCHITECTURE.md` | other public family digest | Complete | None. |
| `docs/digests/PROJECT_DIGESTION_CURRENT.md` | sole current RMQ digest | Complete | None. |

The twelve `FAMILY_SUMMARY.md` occurrences are proposition-classified rather
than blanket-banned:

- Current, unrelated-family occurrences: the Fischer-Heun/LCA phrase
  `large-regime` at lines 278, 563, and 565, plus Fischer-Heun definition
  `canonicalReady` at line 1439. These are not U3 activation or reviewer-route
  claims.
- Proof-only source identifiers: `Ready` inside the four declarations at lines
  2203-2206, the agreement declaration at line 2215, and the segment theorem at
  line 2395; `zeroBlock` inside the two structural replay declarations at lines
  2393-2394. They inventory checked source components and do not publish a
  current Ready/non-Ready/zero-block dispatch.
- Current-public occurrences of `118`, `328`, `4144`, `196727`, route split,
  and `2^128`: zero. Current-public U3 compatibility/history prose: zero.

## Non-Current Role Inventory And Exact History Location

The exact detailed compatibility location is
`docs/digests/SUCCINCT_RMQ_COST_COMPATIBILITY_HISTORY.md`. It classifies the
old `196727`, Ready-`118`, route-split `4144`, transitional `328`, large-regime,
zero-block, and `2^128` material as compatibility, historical audit evidence,
or the explicitly identified proof-only sparse-local witness. The append-only
`docs/DIGESTION_LOG.md` retains chronological historical occurrences under the
compatibility role. `docs/digests/COORDINATOR_COMPLETION_LOG.md` and
`docs/digests/RMQ_CAPSTONE.md` have compatibility role but no sensitive
occurrences.

`docs/ADD_PROVENANCE.md`, `docs/TRUST_AUDIT_PACKET.md`,
`docs/WORD_RAM_REVIEW_PACKET.md`, and `docs/internal/*.md` are audit-evidence,
not current publication. The two June exact snapshots contain no listed
cost/dispatch lexeme; each retains exactly one removed paper alias only through
its registered exact marker-plus-line value.

## Final Verification Ledger

| Check | Final candidate result |
| --- | --- |
| `powershell -ExecutionPolicy Bypass -File scripts/claim_drift_policy_regression.ps1` | Pass: 37 must-reject, 17 must-accept, and 19 path/context/bypass verdicts. |
| `powershell -ExecutionPolicy Bypass -File scripts/claim_drift_scan.ps1 -Strict` | Pass: 1,144 sensitive hits, zero strict failures. |
| `powershell -ExecutionPolicy Bypass -File scripts/paper_topology_lint.ps1` | Pass: 82 broad documentary and 48 paper identifiers resolved. |
| `powershell -ExecutionPolicy Bypass -File scripts/paper_topology_lint_regression.ps1` | Pass: baseline production lint, 15 reject and 5 accept verdicts, tracked state unchanged. |
| `powershell -ExecutionPolicy Bypass -File scripts/design_decision_check.ps1 -Base 8859911c7e3ca4f759acfbe7b1041f5a2dd3040f -Strict` | Pass: 42 changed files checked. |
| `lake build RMQPaper` | Pass. |
| `lake env lean scripts/headline_axiom_check.lean` | Pass: canonical anchors depend only on the accepted standard Lean axioms reported by the inventory. |
| Required hygiene scans | Pass: no forbidden proof escape or `Mathlib` import; no `native_decide` or `Lean.ofReduceBool`. |
| `git diff --check` | Pass. |
| `powershell -ExecutionPolicy Bypass -File scripts/gate.ps1` | `GATE PASS` in 594.2 seconds on the final semantic tree; includes full `lake build`, all target builds, all axiom inventories, both production regressions, strict scan, topology lint, hygiene, and diff check. |

The only post-gate edit was this evidence ledger and row-status update in an
audit-evidence document. No current-public proposition, role classification,
checker, Lean source, or execution surface changed. Strict policy, topology,
design, and diff checks are rerun after this evidence-only update.

## Preserved Invariants And Remaining Acceptance Boundary

- No Lean theorem, payload definition, route, trace, compatibility declaration,
  or numeric `76` proof changes in W22 unless a checker/import is genuinely
  broken.
- Payload bits, proof-only data, charged trace events, controller omissions,
  and executable Lean runtime remain distinct categories.
- The existing SC documentary symbol-resolution checks and FH exact frozen-line
  boundary remain mandatory, but are not treated as semantic coherence by
  themselves.
- `CANDIDATE_COMPLETE` is the strongest worker verdict. Coordinator
  reconstruction, A05 finding-resolution, and a fresh A06 blind exact-commit
  audit remain mandatory.
