# B2 Charged Fringe Acceptance Matrix (frozen before implementation)

Worker: B2-01 (branch `claude/b1-b2-charged-fringe-tables`, base `b6338ea`).
Contract source: `docs/internal/OPTION_B_CHARGED_FRINGE_DESIGN.md` (B1/B2 core)
plus the B2-01 delegation prompt. Requirement wording below is verbatim from
those sources. Frozen at matrix commit; after this commit only evidence,
status, and coordinator-approved amendments may change.

Accepted-route objects the rows refer to (verified in this worktree):

- silent fringe leaves: `localBPLeftFringeCandidateSeededCosted`
  (`RMQ/Core/SuccinctClose/RelativeRmmMacro/LocalBPDecoder.lean:1071`),
  `localBPRightFringeCandidateSeededCosted` (`:1087`), both `cost := 4` with
  values from `localBPSeededPrefixRangeMinExcess`/`...ArgMinPrefixPos`
  (`:937`/`:929`), per-position recursion at `:797`.
- trace twins: `ConcreteDirectoryRAM.lean:267/:302`; with-store twins:
  `ConcreteDirectoryRAMStoreParam.lean:4387/:4484`.
- accepted consumers: `canonicalCrossBlockCloseCostedWithRankSeed`
  (`ConcreteDirectoryRAM.lean:2320`, fringe calls at `:2332`/`:2350`),
  `crossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegments`
  (`:2358`), with-store `ConcreteDirectoryRAMStoreParam.lean:4614`;
  seed threading `localBPSeedFromRankCloseCosted`
  (`LocalBPDecoder.lean:557`) / `localBPSeedFromRankCloseTraceResult`
  (`ConcreteDirectoryRAM.lean:1530`).
- fringe cost constant: `canonicalEndpointFringeChargedTraceCost = 4`
  (`ConcreteDirectoryRAM.lean:2308`), consumed by
  `canonicalCrossBlockCloseCostedWithRankSeed_cost_le_principled` (`:2476`)
  and the 76-literal chain (`SuccinctFinalRAM.lean:8635-8669`,
  `SuccinctRMQClassic.queryCost_eq : queryCost = 76` at
  `SuccinctRMQClassic.lean:111`).
- table pattern: `SuccinctSpace.FixedWidthNatTable`
  (`RMQ/Core/SuccinctSpace/Tables.lean:23`), `readCosted` (`:86`),
  machine view `MachineChunkedTable.lean:219-238`.
- store universe: `ReviewerSource` (20 constructors,
  `RMQ/Core/SuccinctFinal/RAM/ReviewerPhysical.lean:24-45`), erasure fold
  `reviewerSources_erases` (`:725`) /
  `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` (`:786`),
  capacity `concreteBPNativeSuccinctRMQReviewerCapacity n = 400000*(n+1)`
  (`:1414`), `concreteBPNativeSuccinctRMQReviewerPhysicalWords_length_le_capacity`
  (`:1771`); segment map `:83-106` (segments 0-20;
  `SuccinctFinalModelAdequacy.lean:112-116` bakes `segment < 21`).
- public space statement: `SuccinctClassic.buildPayload`
  (`SuccinctRMQClassic.lean:130`), `overhead` (`:81`),
  `buildPayload_length : (buildPayload xs).length <= 2 * xs.length + overhead
  xs.length` (`:932`), `overhead_littleO` (`:926`).

| ID | Exact frozen requirement | Scope | Evidence needed (exact proposition/check) | Named consumer and identity/composition chain | Anti-vacuity challenge | Evidence obtained | Status / residual gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REQ-B2-01 | "build o(n)-bit half-word chunk lookup tables (min-excess/argmin/delta-excess per chunk value)" with "table correctness: entry fields agree with `bpExcessAtBits` / `bpPrefixRangeMinExcessBits` / argmin (`LocalBPDecoder.lean:1728-1756`, `EndpointFringe/PrefixRange/PrefixArgMin.lean:392`) on the chunk". | Local | Checked theorems: for every chunk value `v < 2^c` and in-range offsets, the delta / range-min / range-argmin entries decode (offset-encoding removed) to the `Succinct.rankPrefix`-level excess statistics of the `c`-bit pattern of `v`; plus a bridge to the `bpExcessAtBits` twins where truncation-free (excess-nonnegative) hypotheses hold. Entry lists realized as `SuccinctSpace.FixedWidthNatTable` instances. | Consumed by REQ-B2-02 fold-equivalence proof; identity chain: table `entries[i]?` = spec function of decoded `(v,a,b)` = rankPrefix stats of `natToBitsLE c v`. | Mutate one entry (e.g. off-by-one offset constant, argmin rightmost instead of leftmost) and name the correctness theorem that fails; verify the offset decode is proven, not defined-true (see REQ-B2-11). | Checked in `ChargedFringeChunks.lean`: `bpFringeChunkTable c : FixedWidthNatTable (bpFringeChunkEntries c) (bpFringeChunkEntryWidth c)`; `bpFringeChunkTable_readCosted_erase : (readCosted (slot c v a b)).erase = some (bpFringeChunkPacked c v a b)` for `v < 2^c, a <= c, b <= c`; unpack `bpFringeChunkPacked_arg/_min/_delta`; spec fields are rankPrefix statistics of `natToBitsLE c v` by definition with `bpFringeChunkExcessOffsetAt_add_false` (truncation-free) and `bpFringeChunkExcessOffsetAt_eq_add_bpExcessAtBits` (bpExcessAtBits bridge under nonneg). Anti-vacuity: the corruption witness (REQ-B2-10) replaces entry slot 5 (packed min field 1 -> 3) and the checked inequality `bpFringeChunkTable_corruption_changes_fringe_value` shows the downstream value changes; leftmost-vs-rightmost tie policy is pinned by `bpFringeChunkArgMin` being the keep-left scan consumed by the REQ-B2-02 equality (a rightmost argmin would break `bpFringeChunkFold_eq_localBPSeeded` on tied minima). | Closed at chunk level; the design's 'agree with bpExcessAtBits' wording is realized as the rankPrefix-level characterization plus the nonneg-conditional bpExcessAtBits bridge because bpExcessAtBits truncates below zero inside a chunk (recorded, DD-20260717-002). |
| REQ-B2-02 | "fringe-value equivalence: the charged chunked fringe candidate equals the accepted seeded fringe candidate for every valid invocation reachable from the accepted route (value-level identity, not just result agreement)" — accepted candidates being the values of `localBPLeft/RightFringeCandidateSeededCosted`. | Local+roadmap | Checked theorem(s): `(chunkedLeftFringe... ).value = (localBPLeftFringeCandidateSeededCosted shape blockSize leftClose seed).value` (and right twin) universally quantified over shape/blockSize/close/seed under hypotheses that hold at every accepted call site (window/seed validity discharged from `CartesianShape` BP facts at those sites), not on sampled inputs. | REQ-B2-08 wiring/substitution lemma; chain: chunked fold = generic scan merge = accepted per-position scan (`localBPSeededPrefixRangeMinExcess/ArgMinPrefixPos`). | Record accepted predicate P (value equality of the pair (minExcess, argmin) for all reachable invocations) vs mutation Q (equality only of min component, or only on even-length windows, or with rightmost tie policy); exhibit the checked theorem whose statement rejects each Q. | Checked: `bpChunkedLeftFringeCandidateSeededCosted_value_eq` and `..Right..` (`ChargedFringeChunks.lean`): `.value = (localBPLeft/RightFringeCandidateSeededCosted shape blockSize close seed).value`, universally quantified over shape/blockSize/close/seed under (hvalid, hcount, hstart, hcov); discharged from query-side facts at the accepted call site by `bpChunkedCrossBlockCloseCostedWithRankSeed_value_eq` (`ChargedFringeSubstitution.lean`) via `bpFringeWindowValid_localBPSeedExcess` + the same geometry lemmas the accepted route uses in `canonicalCrossBlockCloseCostedWithRankSeed_exact_of_query`. P = pair value equality (minExcess AND argmin) for all reachable invocations; Q1 (min-only) rejected: statement equates the full `Option (Nat x Nat)` value; Q2 (tested-inputs only) rejected: universally quantified, no sampling; Q3 (rightmost ties) rejected: keep-left `bpFringeScanBetter` on both sides of `bpFringeChunkFold_eq_localBPSeeded`. | Closed at Costed layer (P = Q, same quantifiers). TraceResult/WithStore-layer twins are the named wiring successor. |
| REQ-B2-03 | "literal read-count bound for the chunked fringe (all sizes, no guards)" — "a literal all-size read-count bound". | Local | Checked theorem: `(chunkedLeftFringe...).cost <= <literal Nat>` (and right twin) with no size hypothesis, no readiness guard; the literal is a numeral. | Feeds REQ-B2-08 cost re-derivation / successor wiring rung. | Confirm no hypothesis of the bound theorem restricts n; check tiny sizes (n = 0,1,2) are covered by the same statement. | Checked: `bpChunkedLeftFringeCandidateSeededCosted_cost_le` / `..Right.. : cost <= 37` (`ChargedFringeChunks.lean`), no size hypothesis, no guards; cost = 4 window ticks + `Nat.min (relHi/c + 1) 33` reads with `bpFringeChunkFoldCosted_cost` exact; cap proven identity on the reachable domain inside `bpFringeChunkFoldCosted_global_eq_localBPSeeded` (window <= 4W <= 32c). Consumer bound: `bpChunkedCrossBlockCloseCostedWithRankSeed_cost_le_principled` with `bpChunkedPrincipledBPCloseChargedTraceCostWithRankSeed rankCost = 2*rankCost + 2*37 + interior`. | Closed (literal 37 all sizes; n = 0,1,2 covered by the same unguarded statement). |
| REQ-B2-04 | "store extension: new `ReviewerSource` constructor(s) for the table(s), erasure extension (`reviewerSources_erases` fold, `ReviewerPhysical.lean:725,786`)". | Local+roadmap | New constructor(s) in `ReviewerSource`; sources list, segment map, region map extended; extended erasure theorem `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` (flatten of physical words = live payload) still checked with the new source(s) included; `ReviewerPhysical.lean` compiles. If extension breaks `SuccinctFinalModelAdequacy` (B4 scope per delegation), record exactly what breaks and why. | Reviewer physical store; chain: table payload words are the new source's words; flatten equality at the extended payload. | Remove the new source from the sources list and check the erasure/coverage theorem fails; add a dead duplicate source and check `nodup`/manifest theorems reject it. | CLOSED at wiring (commit M9): `ReviewerSource.fringeChunkTable` appended as the 21st constructor (`ReviewerPhysical.lean`); sources list, segment map (`21 => some .fringeChunkTable`, tail `_ + 22 => none`), leaf map (`21 => canonicalClose`), region 20, `consumer? = canonicalClose`, `legacyOuterSource? = none`; erasure case `bpFringeChunkTable_store_erases` folded into `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` (whole-manifest flatten = amended public payload, still checked); manifest `nodup` (21 sources), `region_injective`, operational liveness case (`Or.inr (by decide, .canonicalClose, 21, rfl, rfl)`), producer may-path case (`lcaFringeLeft` from the fringe mayRead witness), and the W19 successful-occurrence case (increasing-16 cross-block execution reads segment 21 successfully) all re-proved. Anti-vacuity: the dead-source mutation witness moved to the new first dead segment 22 and is still rejected (`concreteBPNativeSuccinctRMQFreshUnusedCanonicalSource_no_producer`); removing the new source breaks `concreteBPNativeSuccinctRMQReviewerPhysicalSources_all`/erasure; the extension landed in the same commit as the wiring so no dead counted region ever existed | Closed (C05 coupling executed; adequacy regenerated, not broken) |
| REQ-B2-05 | "per-source linear capacity bound feeding `PhysicalWords_length_le_capacity` (`ReviewerPhysical.lean:1771`; crude `2^(chunkBits)*w <= 4n`-style bounds suffice given `reviewerCapacity n = 400000*(n+1)`)". | Local | Checked per-source word-count bound for the table source(s) linear in n, composed into the re-proved `concreteBPNativeSuccinctRMQReviewerPhysicalWords_length_le_capacity`. | Same as REQ-B2-04; consumer `PhysicalWords_length_le_capacity` (omega over per-source bounds). | Check the bound covers all n (no `n >= n0` hypothesis); check the new source's words are actually included in the capacity sum (delete its summand and the omega fails). | CLOSED: per-source feed `concreteBPNativeSuccinctRMQReviewerFringeWords_length_le : words(.fringeChunkTable).length <= 64*(size+1)` (from `bpFringeChunkRowCount_le_linear`) composed into the re-proved `concreteBPNativeSuccinctRMQReviewerPhysicalWords_length_le_capacity` (21-source omega against `400000*(n+1)`); deleting the fringe summand breaks the closing omega | Closed |
| REQ-B2-06 | "table bits fold into the space overhead: amended `buildPayload`/`overhead` with the public shape `buildPayload.length <= 2*n + overhead n`, `overhead = o(n)` preserved (`SuccinctRMQClassic.lean:925-941`)" with "`overhead = o(n)`" via `LittleOLinear` and "exact physical erasure". | Local+roadmap | Amended `buildPayload` definition includes the table payload; re-checked `buildPayload_length` with amended `overhead`; re-checked `overhead_littleO : SuccinctSpace.LittleOLinear overhead`; table-payload erasure (store words flatten to counted payload component). | Public statement `SuccinctRMQClassic.buildPayload_length` / `overhead_littleO`; chain: buildPayload = reviewer payload ++ table payload (or equivalent), table component length bounded by proven o(n) function. | Check the o(n) proof does not exclude the table branch (dense-table trap in KNOWN_FAILURE_MODES); confirm table bits are counted in the same payload the public length theorem measures, not a sibling. | CLOSED: the public swap is realized by amending the reviewer payload itself — `concreteBPNativeSuccinctRMQCanonicalReviewerPayloadLayout` gains `fringePayload := (bpFringeChunkTable (bpFringeChunkBits shape.bpCode.length)).payload` appended last (exactly the committed candidate shape `bpChunkedBuildPayloadCandidate = buildPayload ++ tablePayload`), `concreteBPNativeSuccinctRMQCanonicalReviewerOverhead n += bpFringeTableOverhead n`; re-checked `concreteBPNativeSuccinctRMQCanonicalReviewerPayload_length_le : length <= 2*n + overhead n` and `..Overhead_littleO`; `SuccinctClassic.buildPayload`/`overhead` are unchanged abbrev/def over the amended reviewer objects, so the public statement shape `buildPayload_length`/`overhead_littleO` is preserved verbatim and the capstone conjunct `reviewerPayload = buildPayload` remains `rfl` (`FlatPayloadStoreNoSyntheticExecutionStory` unchanged); the counted table component is the same object the execution reads (fringe payload slice theorem `concreteBPNativeSuccinctRMQCanonicalReviewerPayload_fringe_slice` + read-backing disjunct) | Closed |
| REQ-B2-07 | "The seed threading must preserve `localBPWindowBits_alone_does_not_determine_base_excess` (`LocalBPDecoder.lean:609`): base excess comes from the directory seed exactly as today." | Local | The chunked fringe takes `seed` as a parameter with the same threading (`localBPSeedFromRank...` unchanged at consumers); theorem `localBPWindowBits_alone_does_not_determine_base_excess` unchanged and still checked; no new code path derives base excess from window bits alone. | Accepted consumers' seed plumbing (`ConcreteDirectory.lean:45/:76`, `ConcreteDirectoryRAM.lean:2320`, `:1530`). | Inspect the chunked path for any seed reconstruction from window bits; the equivalence theorem must be stated for the threaded seed parameter, not a recomputed one. | The chunked evaluators take `seed` as a parameter identically to the accepted leaves; `bpChunkedCrossBlockCloseCostedWithRankSeed` threads `localBPSeedFromRankCloseCosted` unchanged; `localBPWindowBits_alone_does_not_determine_base_excess` untouched and still checked (module unmodified); no code path in the new modules derives the seed from window bits (the fold receives `seed` as opaque `Nat`). | Closed. |
| REQ-B2-08 | "the B2 core rung is not closed until either (a) the chunked fringe is wired into the accepted whole-query route with the cost chain re-derived, or (b) the equivalence theorem is consumed by a committed bridging theorem that pins the future wiring (an exact-value substitution lemma at the accepted route's fringe call sites), with the wiring rung named as the immediate successor. Prefer (a) if time permits." | Roadmap | Either (a) amended `canonicalCrossBlockCloseCostedWithRankSeed` (+ trace/with-store layers) calling the chunked fringe, with the cost algebra re-derived to a new checked literal; or (b) a committed substitution theorem stating that replacing the fringe leaves by the chunked leaves at the exact accepted call sites preserves the crossBlockClose value (exact-value, per call site), plus the successor wiring rung named in the worklog/report. | The accepted route chain: fringe leaves -> `canonicalCrossBlockCloseCostedWithRankSeed` (`ConcreteDirectoryRAM.lean:2320`) -> `canonicalLcaCloseCostedWithRankSeed` -> whole-query program -> `SuccinctClassic.queryCosted`. | For (b): the substitution lemma must mention the accepted consumer's own definition (not a restatement); mutating the consumer expression (different blockSize argument, swapped left/right) must break the lemma's applicability. | CLOSED via clause (a): the chunked fringe is wired into the accepted whole-query route — `canonicalLcaCloseCostedWithRankSeed` cross-block branch := `bpChunkedCrossBlockCloseCostedWithRankSeed`, trace dispatcher `lcaCloseTraceResultWithRankSeedAllSizeStructural(+WithStore)` cross-block branch := the chunked structural consumers (dispatchers relocated to `ChargedFringeWiring.lean`, names preserved); whole-query program/`SuccinctClassic.queryCosted` consume the swapped dispatcher unchanged; cost chain re-derived (see REQ-B2-15). Trace/WithStore substitution twins at the named accepted call sites additionally committed: `bpChunkedCrossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegments(_refines/_value_eq)` against `ConcreteDirectoryRAM.lean` consumer, WithStore twin with `_eq_of_agree/_refines_of_agree/_store_parametric` against the `ConcreteDirectoryRAMStoreParam.lean` consumer | Closed (clause (a) delivered; Costed pinning superseded by actual wiring) |
| REQ-B2-09 | "FORBIDDEN SHORTCUTS: no sorry/admit/axiom/native_decide/partial/unsafe/implemented_by/Mathlib; no uncounted table storage; no readiness thresholds or size dispatch on the public route; ... no equivalence 'on tested inputs'; no relabeled silent scan". | Inherited hygiene | `rg -n "\b(sorry|admit|axiom|unsafe|opaque|implemented_by|partial|extern|noncomputable)\b|import Mathlib" RMQ lakefile.toml` shows no NEW hits in touched files; table payload counted (REQ-B2-06); no size dispatch introduced; scan-shape check in REQ-B2-10. | All touched modules. | Deliberately search for `if n <` / threshold guards introduced by this branch on the public route. | `rg` hygiene battery on all touched modules: no new `sorry/admit/axiom/unsafe/opaque/implemented_by/partial/extern/noncomputable/import Mathlib` hits and no `native_decide`/`ofReduceBool` (ledger); table payload counted (REQ-B2-06); no size/readiness dispatch introduced on the public route (the dispatcher branches only on same-block vs cross-block exactly as before); fold shape unchanged from the closed REQ-B2-10 row | Closed |
| REQ-B2-10 | "the per-position recursion over window positions must be gone from the charged path — a fixed fold over at most a literal number of chunks with constant-shape per-chunk work is the required shape"; "no decorative reads (every charged read's value must flow into the result — prove value dependency for at least one adversarial table-corruption case: corrupting a read table entry changes the fringe output on a witness input)". | Local | The charged chunked evaluator's recursion is structurally over a chunk list/counter bounded by a literal (visible in the definition), with per-chunk work not recursing over window positions; plus a checked corruption theorem: there exist a witness invocation and a corrupted table (one entry changed) such that the chunked fringe value differs from the uncorrupted value. | Charged fringe definition + REQ-B2-03. | The corruption witness must corrupt an entry that the actual charged read footprint touches; a corruption of an untouched entry changing nothing is not evidence. Record which read index is corrupted and the two differing output values. | Fold shape: `bpFringeChunkFoldCostedFrom` recurses on the chunk counter only (<= 33 by the cap), one `readCosted` + constant mixed-radix decode + one merge per chunk; no recursion over window positions on the charged path (the per-position scan appears only in spec/proof layer). Corruption: `bpFringeChunkTable_corruption_changes_fringe_value` (kernel-checked): honest fold value.2 = some (1,0); with entry at slot 5 (exactly the slot the witness invocation reads, `hslot` by decide) replaced by 22, value.2 = some (3,0). The corrupted read index is in the actual charged footprint and the conclusion is about the returned candidate value. | Closed at Costed layer. |
| REQ-B2-11 | "The offset-encoding of signed excess values in Nat tables must be proven, not assumed." | Local | Checked round-trip/exactness lemmas: encoded entry = c + (signed excess) as a proven identity over `Succinct.rankPrefix` quantities with explicit no-truncation side conditions discharged (not definitional tautologies about the encoder alone). | REQ-B2-01/REQ-B2-02 proofs consume the decode lemmas. | Attempt the truncation trap: a statement that holds vacuously because Nat subtraction clamps at 0 must be rejected by exhibiting the side condition; check the decode lemma is used at a site where the excess is actually negative relative to the chunk start (delta below offset). | Checked: `bpFringeChunkExcessOffsetAt_add_false : offsetExcess + rankFalse = c + rankTrue` (truncation-free identity over rankPrefix, not definitional about the encoder); `bpFringeChunkPacked_arg/_min/_delta` mixed-radix round-trips with explicit field bounds; negative-relative-excess case exercised: pattern `[false]` (v = 0, c = 1) has offset excess 0 at t = 1 (true excess -1), present in the corruption-witness table where minOffset 0 = c + (-1). | Closed. |
| REQ-B2-12 | Process: matrix committed before implementation (this commit); `docs/internal/B2_WORKLOG.md` maintained and committed at each milestone; `docs/internal/DESIGN_DECISIONS.md` updated for chunk boundary mechanism, table field encoding, store region placement. | Process | Git history shows matrix commit precedes implementation commits; worklog present at each milestone commit; DESIGN_DECISIONS entries exist for the three named choices. | Coordinator audit of branch history. | `git log` order check at final report. | Matrix commit ae99812 precedes all implementation commits (f6970da, 17993a4, cee061f, ...); worklog updated and committed at each milestone; DD-20260717-002 records chunk boundary mechanism + table field encoding; store region placement decision recorded in DD (coupling analysis). | Closed (verify at final git log). |
| INV-STORE-IDENTITY | Inherited: "the exact payload/store executed is the payload/store counted by the public space theorem". | Inherited | Table reads execute against the same table store whose payload is counted in amended buildPayload (flatten equality chain), not a sibling decoded structure. | REQ-B2-06 chain. | Two-payload trap: counted payload vs executed store words must be connected by a proved flatten/erasure equality at the consumer. | Whole-machine closure: executed fringe reads hit segment 21 of `concreteBPNativeSuccinctRMQGlobalReadStore`/`CanonicalReviewerReadStore` (= the fringe table words, `_fringeChunkTable` store lemmas); the same words are the 21st reviewer region whose flatten is the final component of the amended public payload (`ReviewerPhysicalWords_erases` + `CanonicalReviewerPayload_fringe_slice`); the read-backing predicate (`concreteBPNativeSuccinctRMQCanonicalReviewerReadBacked`) gained the fringe disjunct tying each successful segment-21 read to the counted payload slice, consumed by the regenerated adequacy field `successful_reads_backed_by_canonical_reviewer_payload` | Closed |
| INV-VALUE-DEPENDENCY | Inherited: "returned values and routing decisions depend on actual charged reads, not a semantic answer computed before the reads". | Inherited | The chunked fringe value is computed from the `Costed`/trace read results (bind chain), with the corruption theorem of REQ-B2-10 as the dependency witness; no proof-field or pre-read semantic value feeds the output. | Charged fringe definition. | Corruption theorem conclusion must be about the returned candidate value, not the trace log. | The charged fold's accumulator and candidates are computed from `(entry?.getD 0)` of each `readCosted` result inside the `Costed.bind` chain (`bpFringeChunkStepDecoded`); the spec entries list never short-circuits the read path (the value lemma PROVES agreement rather than defining it). Corruption theorem conclusion is about `.value.2` (the returned candidate), not the trace/log. | Closed at Costed layer. |
| INV-SEMANTIC-NONVACUITY | Inherited: equivalence/coverage predicates "derived from the operational construction they describe"; for mutation evidence "record the accepted predicate P, the rejected mutation predicate Q... Use the same predicate, or prove the checked implication P -> Q". | Inherited | For REQ-B2-01/02 mutation rows: P and Q recorded side by side with matching quantifiers/domains. | Matrix rows above. | See REQ-B2-02 challenge column. | P/Q records in REQ-B2-01/02 rows; the equivalence predicates are value-level equalities of operational computations (no defined-true predicate anywhere in the new modules); `bpFringeExpectedBest` is derived from the generic scan, not hand-labeled. | Closed for delivered claims. |
| INV-READ-BACKING | Inherited: "every successful read is backed positionally by the counted store". | Inherited | Table reads use `PayloadWordStore`/`FixedWidthNatTable` stores whose words flatten to the counted table payload (store `.erases`), following the `machineFootprint_successful_read_backed` pattern. | Table store construction. | Confirm no read path returns entries from the spec list instead of stored words. | Table reads go through `FixedWidthNatTable.readCosted` = `PayloadWordStore.readWordCosted` + decode; `bpFringeChunkTable_store_erases : flattenPayloadWords store.words = payload` (`ChargedFringeSpace.lean`) ties the words to the counted table payload. | Closed at component level; reviewer-store positional embedding is the wiring successor. |
| INV-WORD-WIDTH | Inherited: "stored and returned words fit one declared modeled machine word". | Inherited | Table word width lemma: every stored table word length <= the route's machine word budget (`SuccinctRank.machineWordBits shape.bpCode.length` or the reviewer word-bits bound at the store level), all sizes. | Store extension rows. | Check tiny n: width bound must hold when `machineWordBits` is small (n = 0,1). | `bpFringeChunkEntryWidth_le : width <= 3c + 6`; `bpFringeChunkEntryWidth_le_reviewerWordBits : width(c(2n)) <= concreteBPNativeSuccinctRMQReviewerWordBits n` for all n (`ChargedFringeSpace.lean`) — the packed word fits the declared reviewer machine word at every size, including tiny n. | Closed against the reviewer word declaration. |
| INV-ALL-SIZE | Inherited: "exactness covers all assigned sizes and edge cases without hidden readiness or compatibility dispatch". | Inherited | Equivalence and cost theorems quantified over all shapes/sizes; no `Ready` hypotheses on the new path. | REQ-B2-02/03. | Grep the new modules for readiness predicates; exercise smallest shapes in kernel-checked examples where informative. | All equivalence/cost theorems quantified over all shapes/sizes; no `Ready`/threshold predicates in the new modules; the only `Nat.min _ 33` cap is a uniform arithmetic formula proven identity on the reachable domain. | Closed for the delivered layers. |
| INV-NO-SYNTHETIC | Inherited: "synthetic events, decorative rereads, and post-hoc replay do not support the execution claim". | Inherited | If a trace layer is built: no `syntheticCostOnlyPrimitive` in the chunked fringe trace (`no_syntheticCostOnlyPrimitive` lemma); at Costed level: every charged unit corresponds to a store read in the definition. | Charged fringe trace lemmas. | REQ-B2-10 decorative-read challenge. | Costed layer: every cost unit in the chunked fringe is either one of the 4 accepted window ticks (mirroring the accepted `cost := 4`) or one `readCosted` (cost lemma exact, `bpFringeChunkFoldCosted_cost`); no `syntheticCostOnlyPrimitive` is constructible at the Costed layer; trace-layer no-synthetic lemmas belong to the wiring successor. | Closed at Costed layer. |
| INV-CATEGORY-SEPARATION | Inherited: "payload bits, proof fields, model ticks, machine state, Lean runtime, and measured performance remain distinct". | Inherited | Table spec lists (entries) are proof/spec-layer; charged reads decode stored bits; costs are model ticks; no crossover (spec entries never short-circuit the read path in the charged evaluator). | New module structure. | Inspect the charged evaluator for direct `entries[i]?` access. | Spec entry lists (`bpFringeChunkEntries`) are proof/spec layer; charged evaluator decodes stored bits only (`entry?.getD 0` arithmetic); costs are Costed ticks; no `entries[i]?` access in `bpFringeChunkStepDecoded`/`bpFringeChunkFoldCostedFrom`. | Closed. |
| CHK-01 | "`lake build` of the smallest root covering every module you touched". | Verification | Exit 0; root(s) named. | Touched modules. | Note any module intentionally left broken (B4-scope record). | B2-01 ledger plus B2-02: `lake build RMQ` green at M8 and at the M9 swap commit (full root; ledger durations in `B2_WORKLOG.md`) | Closed |
| CHK-02 | "`rg -n \"\\b(sorry|admit|axiom|unsafe|opaque|implemented_by|partial|extern|noncomputable)\\b|import Mathlib\" RMQ lakefile.toml` (pre-existing hits are fine — no NEW hits in your files)". | Verification | Diff of hits vs base commit: none added in touched files. | Hygiene. | — | No NEW forbidden-token hits in any touched file (B2-02 ledger) | Closed |
| CHK-03 | "`git diff --check`; after the final commit, `git diff --check b6338ea..HEAD`". | Verification | Exit 0, no whitespace errors. | Hygiene. | — | `git diff --check` clean at M9; `git diff --check b6338ea..HEAD` clean at final commit (ledger) | Closed |
| CHK-04 | "`powershell -ExecutionPolicy Bypass -File scripts\\design_decision_check.ps1 -Strict -Base b6338ea004d49461305ae1df70354a1710109352`". | Verification | Exit 0. | Process. | — | `design_decision_check.ps1 -Strict -Base b6338ea...` exit 0 (27 changed files) at M9 | Closed |

## B2-02 continuation rows (frozen before implementation)

Worker: B2-02 (same branch, continuing from B2-01 HEAD `fff3f2f`).
Contract source: the B2-02 delegation prompt plus coordinator ruling C05:
"the store/source extension is COUPLED to the trace wiring — a table source
may only enter the counted source universe in the same milestone where the
execution actually reads it"; "No sanctioned breakage: EVERY commit must keep
`lake build RMQ` green. Achieve this by building parallel definitions and
swapping consumers atomically within a commit."  C05 resolves the open
coordinator questions recorded by B2-01 in REQ-B2-04/05/06/08: the (b)-clause
Costed pinning is NOT accepted as final closure; the trace layers and the
actual wiring are required in-rung, and the coupled store/public swaps land in
the same atomic milestone as the wiring.  Rows below are frozen at this
commit; after this commit only evidence, status, and coordinator-approved
amendments may change.

Verified anchors for the new rows (this worktree, HEAD `fff3f2f`):
accepted trace fringe twins `localBPLeft/RightFringeCandidateSeededTraceResult`
(`ConcreteDirectoryRAM.lean:267/:302`), accepted trace consumer
`crossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegments` (`:2358`),
with-store twins/consumer (`ConcreteDirectoryRAMStoreParam.lean:4387/:4484`,
consumer `crossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegmentsWithStore`
at `:4614`), Costed route dispatcher `canonicalLcaCloseCostedWithRankSeed`
(`ConcreteDirectoryRAM.lean:3588`) and its trace twin
`lcaCloseTraceResultWithRankSeedAllSizeStructural` (`:3601`); generic charged
table trace read `FixedWidthNatTable.readTraceResultAtSegment`
(`InteriorRAM.lean:36`) with `_refines_readCosted` (`:45`),
`_no_syntheticCostOnlyPrimitive` (`:64`),
`fixedWidthNatTable_readTraceResultAtSegment_matchesReadStore`
(`InteriorRAM.lean:1852`); global segment layout `Segments.lean` (trace
segments 0-20 live for the reviewer store, `deadSegment = 29`, reviewer store
returns `none` for `21 <= segment`); reviewer universe
`ReviewerPhysical.lean:24-45` (20 constructors), segment map `:83-106`
(`0-20`), erasure fold `reviewerSources_erases` `:725` /
`concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` `:786`, capacity
`:1414/:1771`, word bits `:1418`; adequacy record
`SuccinctFinalModelAdequacy.lean` fields `cost_le` (`:38-42`),
`nonSyntheticWeight_sum_le_76` (`:67-69`), `canonical_segments_complete`
(`:112-116`, `<-> segment < 21`), `compatibility_tail_unreachable`
(`:143-146`, `21 <= segment -> none`); cost algebra
`CanonicalRMQChargedTraceCostAlgebra` and
`concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCost(_eq) = 76`
(`SuccinctFinalRAM.lean:8634-8671`); public consumers
`SuccinctRMQClassic.queryCost_eq : queryCost = 76` (`:111`),
`overhead` (`:81`), `buildPayload` (`:130`), capstone `rfl` conjunct
(`:615/:698`), `buildPayload_length`/`overhead_littleO` (`:925-941`);
`Headlines/RMQ.lean` (`:70` `queryCost = 76` conjunct, `:497/:520` abbrevs);
`Validation/SuccinctClassic.lean:266` (`queryCost == 76`),
`Validation/SuccinctClassicCostHarness.lean:118` (`canonicalBoundIs76`).

| ID | Exact frozen requirement | Scope | Evidence needed (exact proposition/check) | Named consumer and identity/composition chain | Anti-vacuity challenge | Evidence obtained | Status / residual gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REQ-B2-13 | "TraceResult-level chunked fringe (segment-parameterized, follow the interior directory's `AtSegments` pattern) with refinement to the Costed layer, `matchesReadStore`, no-synthetic, and footprint lemmas." | Local | New `WordRAM.TraceResult` chunked fringe evaluators (left/right) whose trace is the 4 accepted window-word reads plus one `readWord fringeSegment slot` event per visited chunk; checked `_refines` (`toCosted = bpChunkedLeft/RightFringeCandidateSeededCosted`), `_matchesReadStore` under a store-agreement hypothesis (`store.readWord? fringeSegment i = table words[i]?`-shaped), `_no_syntheticCostOnlyPrimitive`, and a trace/footprint characterization strong enough for the SuccinctFinalRAM `_trace_forall` regeneration. | REQ-B2-14 wiring; chain: fringe TraceResult -> `.toCosted` = the B2-01 Costed evaluator -> `_value_eq` substitution -> accepted candidates. | The trace must contain the actual chunk-slot read events (not `ofCosted` synthetic markers): the no-synthetic lemma plus the exact per-chunk `readWord` shape reject a cost-only replay; corrupting the backing store word at a read slot must change the refined value (inherited from REQ-B2-10 via refinement). | Checked (`ChargedFringeTrace.lean`): fold = `FlatStoreComputation` (`bpFringeChunkFoldComputation(From)`) with `_run_value` (= Costed fold on the table's own words), `_run_reads_length` (= chunk count), `_run_footprint` (= visited slot sequence); segment-parameterized trace `bpFringeChunkFoldTraceResultAtSegment(WithStore)` with `_toCosted = bpFringeChunkFoldCosted`; fringe candidates `bpChunkedLeft/RightFringeCandidateSeededTraceResultAtSegment(WithStore)` with `_refines : toCosted = bpChunkedLeft/RightFringeCandidateSeededCosted`, `_matchesReadStore` (store agreement at segment 0 for the four window reads + at the fringe segment for the table words), `_no_syntheticCostOnlyPrimitive`, slot-bounded `_trace_forall` (every fringe event is `readWord fringeSegment address words[address]?` with `address < rowCount`), `_eq_of_agree`, `_store_parametric`, `_window_mem`, `_fringe(_successful)_mayRead`. Anti-vacuity: the trace contains the literal per-chunk `readWord` events produced by `FlatStoreComputation.read` (reads_match_store), never `ofCosted` synthetic markers (`_no_syntheticCostOnlyPrimitive` checked); value flows from the same reads by `_run_value` + the M4 corruption witness through refinement | Closed |
| REQ-B2-14 | "Substitution twins at the trace/WithStore layers — accepted call sites `ConcreteDirectoryRAM.lean:2358` and `ConcreteDirectoryRAMStoreParam.lean:4614`" together with REQ-B2-08(a): "the chunked fringe is wired into the accepted whole-query route with the cost chain re-derived". | Roadmap | The accepted consumers `crossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegments` (`:2358`), its WithStore twin (`:4614`), and the Costed dispatcher `canonicalLcaCloseCostedWithRankSeed` (`:3588`) actually call the chunked fringe leaves (atomic swap, same commit as REQ-B2-17 store extension); re-checked `_refines`/`_eq_of_agree`/store-parametricity theorems for the swapped consumers; whole-query route (`SuccinctFinalRAM` whole-query program) consumes the swapped dispatcher unchanged. | Closes REQ-B2-08 clause (a); chain: chunked leaves -> swapped `canonicalLcaCloseCostedWithRankSeed` -> `concreteBPNativeLCACloseCanonicalInterpretedCosted` -> whole-query program -> `SuccinctClassic.queryCosted`. | The swap must preserve the exact public value on every valid query: evidenced by re-proved route exactness through `bpChunkedCrossBlockCloseCostedWithRankSeed_value_eq` (accepted P = pair value equality at the accepted call sites, adversarial Q = agreement only at the Costed layer or only on sampled inputs — rejected because the swapped consumers are the accepted definitions themselves and exactness is re-proved for the full route); a mutation that swaps left/right fringe leaves or alters blockSize must break the re-proved exactness. | Checked: swapped dispatchers in `ChargedFringeWiring.lean` (`canonicalLcaCloseCostedWithRankSeed`, `lcaCloseTraceResultWithRankSeedAllSizeStructural(+WithStore)` with `fringeSegment` parameter) re-prove the full surface: `_refines`, `_trace_forall`, `_matchesReadStore`, `_no_syntheticCostOnlyPrimitive`, `_eq_of_agree`, `_refines_of_agree`, `_store_parametric`, `canonicalLcaCloseCostedWithRankSeed_exact_of_query` (same statement as the retired route, proved through `bpChunkedCrossBlockCloseCostedWithRankSeed_value_eq` + the retained accepted exactness); `SuccinctFinalRAM`'s whole-query exactness (`concreteBPNativeSuccinctRMQCanonicalQueryInterpretedCosted_exact`) consumes it unchanged. P = exact-value equality of the swapped-route close for every valid-query invocation (same quantifiers as the accepted `_exact_of_query`); Q mutations (Costed-only agreement, sampled inputs, swapped left/right leaves) rejected because the swapped dispatcher is the definition the whole-query program executes and the exactness statement is unchanged | Closed |
| REQ-B2-15 | "Wire into `canonicalLcaCloseCostedWithRankSeed` and re-derive the whole-route cost chain with B2-01's committed algebra `2*rankCost + 2*37 + interior` (projected literal 142 — DERIVE it, never assert; if the derivation lands elsewhere, the derived value wins). Update every Lean consumer of the old 76 so the library stays green ... Freeze 76 as a historical constant following the `canonicalTransitionalQueryCost = 328` pattern — do NOT delete it." | Local+roadmap | The cost algebra field `endpointFringe` becomes the chunked literal (37); the new whole-route literal is obtained by `rfl`/`decide`-free derivation from `CanonicalRMQChargedTraceCostAlgebra.wholeQuery` (2*13 + (2*4 + 2*37 + 30) + 4 per the committed algebra; the checked derivation output is authoritative); every Lean consumer of 76 re-proved at the derived literal: `SuccinctFinalRAM.lean` (algebra + `_eq` + downstream cost bounds), `SuccinctFinalModelAdequacy.lean` (`cost_le`, weight-sum field), `SuccinctRMQClassic.queryCost_eq`, `Headlines/RMQ.lean` (`:70/:497/:520`), `Validation/SuccinctClassic.lean:266`, `Validation/SuccinctClassicCostHarness.lean:118` (executable `== 76` becomes the derived literal); a frozen historical constant records 76 (pattern `canonicalTransitionalQueryCost = 328`) without remaining the live bound. | Whole-route cost chain; chain: fringe cost lemma (<= 37, REQ-B2-03) -> swapped cross-block `_cost_le_principled` (B2-01, `bpChunkedPrincipledBPCloseChargedTraceCostWithRankSeed`) -> LCA close cost -> whole-query `cost_le` -> public `queryCost_eq`. | The literal must be derived: the `_eq` theorem must be proved from the algebra definition (`rfl`), not asserted against an independent numeral; check by mutating the fringe constant (37 -> 36) and observing the `_eq` right-hand side no longer type-checks against the derivation; the executable harness check must compute the same literal from the live route. | Checked: `concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCostAlgebra.endpointFringe := bpChunkedEndpointFringeChargedTraceCost (= 37)`; derived literal by `rfl`: `concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCost_eq : ... = 142` and `CloseCost_eq : closeLCA = 112` (wholeQuery = 2*13 + (2*4 + 2*37 + 30) + 4 = 142 — derivation matched the projection); consumers re-proved: whole-query principled cost, `nonSyntheticWeight_sum_le_142` (renamed from `_le_76`), adequacy `cost_le`/weight field, `SuccinctClassic.queryCost_eq : queryCost = 142`, headlines conjunct, `Validation/SuccinctClassic` boolean, harness `canonicalBoundIs142`, `RMQExamples #guard 142`; 76 frozen as `concreteBPNativeSuccinctRMQSilentFringeChargedTraceCost(_eq = 76)` / `SuccinctClassic.canonicalSilentFringeQueryCost_eq = 76` (328 pattern); the transitional 328 chain re-proved (close-bound hypotheses added to the transitional LCA cap because 2*37 exceeds the old 8-tick slack; 2r+104 <= 8+2r+240) | Closed (pending final harness run recorded in ledger) |
| REQ-B2-16 | Headline re-consumption: the public paper main theorem and validation surfaces consume the re-derived route (mission: "Update every Lean consumer of the old 76"; verification: "`claim_drift_scan.ps1` and `paper_topology_lint.ps1`" because "this rung changes the public surface"; doc sync "only what committed checks ... require to pass; full prose migration is B5"). | Public surface | `Headlines/RMQ.lean` paper main theorem restated with the derived literal and still checked; headline abbrevs re-pointed (weight-sum bound abbrev at `:497`); `claim_drift_scan.ps1` and `paper_topology_lint.ps1` exit 0 with at most minimal stale-constant doc fixes (structural failures stop the rung and are reported). | Paper/headline surface; chain: `queryCost_eq` -> `listIntSuccinctRMQPaperMainTheorem` conjunct -> headline abbrevs -> claim-drift/paper-topology gates. | The headline conjunct must state the new derived literal (not drop the numeric claim); a doc fix is acceptable only for stale constants, and any needed weakening of a public statement shape is a structural stop condition, not a doc fix. | Checked: `listIntSuccinctRMQPaperMainTheorem` conjunct `queryCost = 142` re-proved; headline abbrev renamed to `succinctRMQWholeQueryGlobalWordTraceResultNonSyntheticWeightSumLe142`; `claim_drift_scan.ps1` exit 0 (0 strict failures) and `paper_topology_lint.ps1` (ledger); minimal stale-constant doc sync only (README headline rows, FAMILY_SUMMARY cost algebra lines); no public statement shape weakened | Closed (verification ledger) |
| REQ-B2-17 | Segment-accounting decision (mission: "Decide and record segment accounting explicitly (`SuccinctFinalModelAdequacy.lean` bakes `segment < 21` at :116); extending segments means regenerating the affected adequacy fields — do it, do not break the module") coupled with REQ-B2-04/05/06 closure per C05: the `ReviewerSource` extension, erasure/capacity fold re-proof, and public `buildPayload`/`overhead` swap land in the same milestone where the execution reads the table. | Local+roadmap | Decision recorded (matrix + DD): fringe chunk table at global trace segment 21 = reviewer logical segment 21, appended as the last `ReviewerSource` constructor so the amended reviewer payload is exactly `old payload ++ table payload` (the committed candidate shape `bpChunkedBuildPayloadCandidate`); regenerated adequacy fields checked: `canonical_segments_complete : (∃ source, segmentSource? segment = some source) <-> segment < 22`, `compatibility_tail_unreachable` at `22 <= segment`, extended erasure fold `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases`, extended capacity `PhysicalWords_length_le_capacity` (using `bpFringeChunkRowCount_le_linear <= 64*(n+1)`), word-width/address-fits fields extended to the new segment; public `buildPayload`/`overhead` swap realized by amending `concreteBPNativeSuccinctRMQCanonicalReviewerPayload`/`...ReviewerOverhead` so `reviewerPayload = buildPayload` stays `rfl` (`SuccinctRMQClassic.lean:698`) and the public statement shape `buildPayload.length <= 2*xs.length + overhead xs.length` with `overhead = o(n)` is re-checked (candidate theorems `bpChunkedBuildPayloadCandidate_length` / `bpChunkedOverheadCandidate_littleO` supply the arithmetic). | Closes REQ-B2-04/05/06 and INV-STORE-IDENTITY at whole-machine level; chain: table store words -> new source words -> physical words flatten -> amended reviewer payload = amended public `buildPayload`; executed fringe reads at segment 21 -> segment map -> new source -> region -> flat physical backing. | Dead-source challenge (inherited from REQ-B2-04): the constructor enters the universe in the same commit where the accepted trace reads segment 21, so the manifest no-dead-source theorems (`ReviewerPhysical.lean:409-451`) and the producer may-path/liveness fields must be re-proved WITH the new source — removing the new source from the list must break the erasure/coverage theorem; adding a duplicate must break `nodup`. Two-payload trap: the counted table component must be the same object the execution reads (flatten equality at the reviewer consumer, not a sibling table). | Decision recorded (DD-20260717-004): fringe table = trace segment 21 = reviewer logical segment 21, constructor appended last; regenerated adequacy checked: `canonical_segments_complete <-> segment < 22` (`concreteBPNativeSuccinctRMQReviewerSegmentSource?_coverage`), `compatibility_tail_unreachable` at `22 <=` (`_legacyTail_none`), extended erasure/capacity/word-width/address folds (`ReviewerPhysical` compiles green; `SuccinctFinalModelAdequacy`/`SuccinctFinalSemanticProvenanceAdequacy` records re-proved, not broken); public swap evidence under REQ-B2-06. Dead-source challenge: extension and wiring in one commit; `FreshUnusedCanonicalSource` mutation moved to segment 22 and still rejected; duplicate source rejected by `nodup` (decide over 21 constructors) | Closed |
| REQ-B2-18 | "Provenance for the new table reads: occurrence/producer packets per the existing table-source pattern (the interior directory's sources are your template). Regenerating the mechanical `_trace_forall` inductions that your new events invalidate is IN scope wherever needed to keep touched modules and the library green; a full independent provenance audit pass is B4 and out of scope." | Local | The adequacy provenance fields (`every_emitted_read_has_listed_region`, `every_emitted_read_has_occurrence_provenance`, `every_emitted_read_has_eventValue_producer_provenance`) still checked with the fringe-table reads included, via the same packet constructions the interior table sources use; all `_trace_forall`-style inductions over the whole-query trace regenerated to cover the new `readWord 21 _ _` events. | `ConcreteBPNativeSuccinctRMQFinalTraceModelAdequacy`; chain: fringe read event -> segment 21 -> new source -> region/physical address -> flat physical trace membership. | The provenance packet must cover the actual emitted fringe events (position/instruction shape per the existing pattern), not merely assert segment membership; deleting the fringe case from the regenerated induction must break the adequacy theorem. | Checked: `ReviewerProducerReadPath.lcaFringeLeft/lcaFringeRight` constructors carry the read event's membership in the actual chunked fringe candidate component trace at the fringe segment (per the interior template); `lcaCloseGlobalWordTraceResult_producerReadPath` and the whole-query producer/occurrence provenance packets regenerated (`SuccinctFinalRAM` compiles); consumer-level `_trace_forall`s take candidate-trace-membership hypotheses so the packets stay component-exact; W19 successful-occurrence for the fringe source witnessed on the increasing-16 cross-block execution (`reviewerIncreasing_fringe_successful_claim`, using `_fringe_successful_mayRead`: every chunked fringe invocation reads a stored row successfully); all touched `_trace_forall` inductions regenerated; full independent provenance audit remains B4 as contracted | Closed for the contracted scope |
| REQ-B2-19 | C05 process invariant: "EVERY commit must keep `lake build RMQ` green. Achieve this by building parallel definitions and swapping consumers atomically within a commit." Plus: "no dead sources at any commit; no decorative reads". | Process | Per-commit `lake build RMQ` (or an equivalent full-root check at each commit boundary) exit 0, recorded in the worklog ledger; no commit introduces a counted source the execution does not read at that commit. | Coordinator audit of branch history. | `git log` + ledger cross-check at final report; the store-extension commit must be the same commit as the wiring swap. | Commit ledger: M7 (docs only), M8 additive modules with `lake build RMQ` green, M9 single atomic swap commit with `lake build RMQ` green (ledger durations in worklog); the store extension and the wiring landed in the same M9 commit, so no commit contains a counted-but-unread source | Closed (verify at final git log) |

## B3 continuation rows (rank/select leaf recharge; frozen before implementation)

Worker: B3-01 (same branch, continuing from B2 candidate HEAD `d1d645e`,
full SHA `d1d645ee221c1756f8c61c5ea222950f73e43c8c`).
Contract source: the B3-01 delegation prompt (Option B campaign,
`OPTION_B_CHARGED_FRINGE_DESIGN.md` staging entry "B3 rank/select leaves +
store consolidation").  Mission: "eliminate the remaining
`wordRank`/`wordSelect` events from the accepted route so the charged event
vocabulary collapses to `readWord` only."  Rows below are frozen at this
commit; after this commit only evidence, status, and coordinator-approved
amendments may change.

Verified anchors for the B3 rows (this worktree, HEAD `d1d645e`):

- word-primitive emitters: `RMQ.RAM.rankBoolWordPrefix` /
  `RMQ.RAM.selectBoolWord` (`RMQ/Core/RAM.lean:163/:168`, `Op.wordRank` /
  `Op.wordSelect` erased by `.toCosted`); trace constructors
  `Program.sampledRank`/`wordSelectFromOpt`
  (`RMQ/Core/WordRAM/ReadStoreEval.lean:137/:149`), register variants
  `NatProgram.sampledRank`/`twoLevelSampledRank`
  (`RMQ/Core/WordRAM/Register.lean:600/:620`, `wordRank` emissions at
  `:614/:638`).
- accepted-route consumers: rank seeds
  `SuccinctRank.TwoLevelPayloadLiveStoredWordRankData.rankCosted`
  (`RMQ/Core/SuccinctRank.lean:870`, bound `rankCosted_cost_le_four` `:911`,
  3 `readWord` + 1 `wordRank`), used by the route rank close
  (`concreteBPNativeRankCloseInterpretedCosted`,
  `SuccinctFinalRAM.lean:30`, over `builtRelativeSplitBPCloseRankData shape`
  at `SuccinctFinal.lean:832`, target `false`), the select long path
  (`longFlagRankData.rankCosted true`, `GenericSelect/Source.lean:1877`),
  and the sparse directory (`SparseExceptionDirectory.readCosted`,
  `GenericSelect/Directory.lean:83`, `rankData.rankCosted true`); selects
  `SparseExceptionSelectData.selectCosted` (`GenericSelect/Source.lean:1860`,
  `selectCosted_cost_le_thirteen` `:2010`) with dense leaf
  `denseTwoWordSelectCosted` (`GenericSelect/Primitives.lean:24`,
  at most 2 `readWord` + 2 `wordRank` + 1 `wordSelect`).
- cost algebra: `CanonicalRMQChargedTraceCostAlgebra`
  (`SuccinctFinalRAM.lean:8767`, fields
  selectClose 13 / rankClose 4 / endpointFringe 37 / interiorDirectory 30),
  derived literal
  `concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCost_eq
  : ... = 142` (`:8801`), frozen historical
  `...SilentFringeChargedTraceCost_eq = 76` (`:8824`).
- chunk-table infrastructure: `bpFringeChunkTable c` at reviewer/global
  segment 21 (`ChargedFringeChunks.lean`, `ChargedFringeTableFacts.lean`,
  `ReviewerPhysical.lean` constructor `.fringeChunkTable`); the packed entry
  of slot `(v, a, b)` carries `deltaOffset` (full chunk) and
  `rangeMinOffset`/`rangeArgMin` over `[a, b)`; for `a = b = t` the min
  field decodes to `bpFringeChunkExcessOffsetAt c v t = c + rankTrue(t) -
  rankFalse(t)` (empty-range argmin `= a`), so in-chunk prefix ranks at any
  offset `t <= c` decode from one existing-table read.
- word geometry: `machineWordBits n = Nat.log2 n + 1`
  (`SuccinctRank.lean:38`), `bpFringeChunkBits m = Nat.log2 m / 8 + 1`, so
  `machineWordBits m <= 8 * bpFringeChunkBits m` (same omega arithmetic as
  `four_machineWordBits_le_32_mul_bpFringeChunkBits`); every in-word
  rank/select word on the accepted route has length at most its structure's
  `wordSize` (`BoundedPayloadWordStore.word_length_le`,
  `WordStore.lean:552`) with `wordSize <= machineWordBits
  shape.bpCode.length` (`wordSize_le_machine` fields,
  `Source.lean:1698/:1712`, `Directory.lean:29`;
  `TwoLevelPayloadLiveStoredWordRankData.wordSize_le_machine`,
  `SuccinctRank.lean:766`).
- reviewer store universe after B2: 21 constructors (segments 0-20 plus
  `.fringeChunkTable` = 21), `canonical_segments_complete <-> segment < 22`,
  dead-tail at `22 <= segment`, dead-source anti-vacuity witness
  `concreteBPNativeSuccinctRMQFreshUnusedCanonicalSource_no_producer` at
  segment 22.
- docstring/comment anchors (coordinator-queued fixes): stale "76" at
  `RMQ/Headlines/RMQ.lean:27` and `RMQ/Core/SuccinctFinalRAM.lean:9543`;
  legacy flat-payload segment map
  `concreteBPNativeSuccinctRMQFlatPayloadSegmentSource?`
  (`RMQ/Core/SuccinctFinal/RAM/FlatPayload.lean:257`, `| 21 => some
  .closeSummaryMinRel` at `:280`) needs a comment noting the canonical
  reviewer/global store's segment 21 is now the fringe chunk table (the
  legacy map is a distinct segment-numbering universe).

| ID | Exact frozen requirement | Scope | Evidence needed (exact proposition/check) | Named consumer and identity/composition chain | Anti-vacuity challenge | Evidence obtained | Status / residual gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REQ-B3-01 | "Replace each in-word rank/select with charged reads into o(n)-bit chunk tables plus silent bounded arithmetic, following B2's exact pattern end to end"; "In-word rank at position p: full chunks below p via a popcount-per-chunk-value table (or derive popcount from B2's offset-encoded excess delta - decide and justify), plus one boundary lookup via a (chunkValue, offset) table - B2's `ChargedFringeChunks` boundary pattern"; "universal value-equivalence theorems under the accepted route's own hypotheses". | Local | Checked chunked in-word rank evaluator (one charged read per visited chunk of the word, silent mixed-radix decode + bounded arithmetic, no per-position recursion on the charged path) with: exact cost lemma and all-size literal cap (projected at most 8, cap proven identity on the reachable domain `word.length <= 8c`); universal value equivalence `(bpChunkedWordRankCosted (bpFringeChunkTable c) c target word limit).value = RMQ.RAM.boolRankPrefix target word limit` under hypotheses (`0 < c`, `word.length <= 8 * c`) that hold at every accepted call site via `word_length_le` + `wordSize_le_machine` + `machineWordBits <= 8 * bpFringeChunkBits`. | REQ-B3-05 substitution at the three rank-seed call sites; chain: table read at slot `(v_j, t_j, t_j)` -> min-field decode = `bpFringeChunkExcessOffsetAt c v t_j` -> per-chunk rank arithmetic -> chunk-decomposition lemma -> `boolRankPrefix`. | Truncation trap: the per-chunk decode `(minOff + t - c) / 2` must be proven exact (not Nat-clamped) via `bpFringeChunkExcessOffsetAt_add_false`; boundary trap: a `limit > word.length` invocation must not read `false` padding as data (effective-limit clamp inside the evaluator, proven equal to `boolRankPrefix` which also clamps); target trap: `target = false` must decode `t - rankTrue`, exercised at the route rank close (target false) AND the flag ranks (target true). | Checked (`ChargedWordChunks.lean`, commit 93ab753): `bpChunkedWordRankCosted_value : 0 < c -> word.length <= 8*c -> (bpChunkedWordRankCosted (bpFringeChunkTable c) c target word limit).value = RMQ.RAM.boolRankPrefix target word limit` (all targets/words/limits); `bpChunkedWordRankCosted_cost_le : cost <= 8` (no hypotheses); decode exactness `bpChunkRankOfEntry_packed` via `bpFringeChunkMinOffset_self` + `bpFringeChunkExcessOffsetAt_add_false` + `bpRankPrefix_true_add_false` (truncation-free: omega over the two additive identities, no Nat-clamp vacuity); effective-limit clamp `bpWordRankEffLimit = min limit word.length` makes the fold agree with `boolRankPrefix` without decoding padding (`bpWordChunkRank_step` handles `limit > word.length` by the clamped-min case split); both targets exercised (decode branches on `cond target`); geometry `machineWordBits_le_8_mul_bpFringeChunkBits`; decision popcount-from-delta (no new rank table) recorded in DD-20260717-005 | Closed (Costed layer at M2/M3; trace/wiring residual discharged by the M5 swap, REQ-B3-06 closed) |
| REQ-B3-02 | "In-word select of the k-th true: per-chunk popcounts to locate the containing chunk, then a (chunkValue, k) select table for the final chunk. Bound the loop by the literal chunk count exactly as B2 bounded 33." | Local | Checked chunked in-word select evaluator: per-chunk one existing-table read for the slice popcount, routing branch `k < count_j`, then exactly one new-select-table read for the containing chunk; le cost lemma with all-size literal (projected at most 9 = 8 chunk reads + 1 select read, cap identity on `word.length <= 8c`); universal value equivalence `(bpChunkedWordSelectCosted ... c target word occ).value = RMQ.RAM.boolSelectInWord target word occ` under (`0 < c`, `word.length <= 8 * c`). | REQ-B3-05 substitution at the dense select leaf; chain: popcount reads route the fold -> select-table read at slot `(v_j, k')` decodes the in-chunk position -> `j * c + pos` -> select split algebra -> `boolSelectInWord` (via `ram_boolSelectInWord_eq_select`). | Padding trap for `target = false`: the k-th false of the padded pattern must be proven inside the true slice because the routing count is the SLICE popcount (`k' < count over [0, t_j)`), not the padded-chunk popcount; exhaust trap: `occ >= total target count` must return `none` exactly as `boolSelectInWord`; sentinel entries for `k >= popcount(v)` must be unreachable on the honest route (proven, not assumed). | Checked (`ChargedWordChunks.lean`): `bpChunkedWordSelectCosted_value : 0 < c -> word.length <= 8*c -> (bpChunkedWordSelectCosted (bpFringeChunkTable c) (bpChunkSelectTable c target) c target word occ).value = RMQ.RAM.boolSelectInWord target word occ`; `_cost_le : <= 9` (8-chunk cap + 1 select read, cap `bpWordChunkCount_eq` identity on the reachable domain); padding trap discharged: the routing count is the SLICE popcount and the found-branch proof places the selected position inside the slice via `select_drop_take_eq_sub_of_select` + `select_append_left_of_some` (k-th target of the padded pattern = k-th of the slice for k below the slice count); exhaust case returns `none` exactly as `boolSelectInWord` (contradiction via `occurrence_lt_rankPrefix_of_select_lt`); sentinel unreachability is proven inside the found branch (`hkc : k <= c` from the decoded slice count) | Closed (Costed layer at M2; M4a trace fold with canonical select-address bound; wiring residual discharged by the M5 swap, REQ-B3-06 closed) |
| REQ-B3-03 | "table module with size/width/erasure/littleO facts" for the new select chunk table; "Reuse or extend B2's table infrastructure rather than duplicating it; if the same physical table can serve both (e.g. adding fields to the packed entry), weigh entry-width vs word-width bounds (`bpFringeChunkEntryWidth_le_reviewerWordBits` pattern) before choosing." | Local | New `bpChunkSelectTable c target : FixedWidthNatTable` (rows `2^c * (c+1)`, slot `v * (c+1) + k`, entry = in-chunk position of the k-th target bit, sentinel `c` when absent) with checked: entries/getElem decode, payload length identity, component erasure (`store_erases`), entry-width literal bound and width at most reviewer word bits at every size, row-count linear capacity feed (`<= K * (n+1)` shape), and `LittleOLinear` overhead following `bpFringeTableOverhead_littleO`. Rank tables: DECISION - no new rank table; popcounts derive from the existing `(v, a, b)` table's min field at `(v, t, t)` (recorded with alternatives in DESIGN_DECISIONS). | REQ-B3-07/08 reviewer store extension; chain identical to B2's `ChargedFringeTableFacts` -> `ReviewerPhysical` feed. | Dense-table trap: the o(n) proof must not exclude the select-table branch; width trap: width bound must hold at tiny n (n = 0, 1) against the reviewer word declaration; duplication challenge: adding a packed select field to the B2 entry was weighed with the width comparison recorded. | Checked (`ChargedWordChunks.lean`): `bpChunkSelectTable c target : FixedWidthNatTable (bpChunkSelectEntries c target) (bpChunkSelectEntryWidth c)` with `bpChunkSelectEntries_getElem`, `bpChunkSelectTable_readCosted_erase`, `_payload_length` (= rowCount * width), `_store_erases`, `_word_length`, `bpChunkSelectEntryWidth_le (<= c+2)`, `bpChunkSelectRowCount_le_two_pow`, `bpChunkSelectRowCount_le_linear (<= 16*(n+1))`, `bpChunkSelectTableOverhead_littleO`, `bpChunkSelectEntryWidth_le_machineWordBits_capacity`; reuse decision: NO new rank table (popcounts decode from the existing `(v,a,b)` entries at `(v,t,t)`), packed-select-field alternative weighed and rejected in DD-20260717-005 (needs the occurrence index in the row space anyway; would reopen closed width rows) | Closed (component facts at M2; reviewer-word-bits restatement + store extension landed with REQ-B3-07/08 at the M5 swap) |
| REQ-B3-04 | "The word-primitive INSTRUCTIONS (`rankBoolWordPrefix` etc.) stay defined - legacy/compatibility surfaces still use them; only the accepted route's emissions are replaced." | Local | `RMQ.RAM.rankBoolWordPrefix`/`selectBoolWord`, `Program.sampledRank`/`wordSelectFromOpt`, `NatProgram.sampledRank`/`twoLevelSampledRank` and their eval/evalR semantics remain defined and untouched (modules unmodified or additively extended only); the legacy Costed/trace evaluators (`rankCosted`, `selectCosted`, `denseTwoWordSelectCosted`, their trace twins) remain checked. | Legacy/compatibility surfaces (transitional 328 chain, older profiles). | Grep-level check: no deletion/rename of the primitive constructors or reference evaluators; `lake build RMQ` covers the untouched legacy consumers at every commit. | Checked at the swap commit: `RMQ.RAM.rankBoolWordPrefix`/`selectBoolWord`, `Program.sampledRank`/`wordSelectFromOpt`, `NatProgram.sampledRank`/`twoLevelSampledRank` and their eval/evalR semantics are unmodified (modules untouched); the retired register evaluators remain checked under NEW Register names (`concreteBPNativeSelectCloseRegisterInterpretedCosted`, `concreteBPNativeRankCloseRegisterInterpretedCosted`, Register word-trace twins) and the pre-canonical compatibility chain (`WholeQueryInstr.eval/evalLeafTrace/evalWordTrace(OfSizeGe)`, legacy LCA replays, legacy-store matches, the three `_refines_*CloseCosted` bridges) is rewired to them with statements verbatim (DD-20260718-002); the transitional 328 chain still checked (`concreteBPNativeSuccinctRMQCanonicalTransitionalQueryCost_eq` re-proved via 207 <= 328); `lake build RMQ` green at the swap commit | Closed |
| REQ-B3-05 | "substitution at the accepted call sites" - the three rank seeds (route rank close over `shape.bpCode` target `false`; select long-path flag rank target `true`; sparse-directory flag rank target `true`) and the two selects (route select close `sparseExceptionSelectData shape.bpCode false`; dense leaf) consume the chunked leaves with exact-value substitution "under the accepted route's own hypotheses". | Local+roadmap | Checked chunked twins with value equivalence at the Costed layer: `TwoLevelPayloadLiveStoredWordRankData.bpChunkedRankCosted` `_value_eq` (= `rankCosted` value, all targets/pos, under the structure's own `wordSize_le_machine`-derived bound); `bpChunkedDenseTwoWordSelectCosted` `_value_eq`; `SparseExceptionDirectory.bpChunkedReadCosted` `_value_eq`; `SparseExceptionSelectData.bpChunkedSelectCosted` `_value_eq` (= `selectCosted` value for every idx); each with derived literal cost bounds; plus the route-level swapped consumers (REQ-B3-06) re-proving the accepted exactness statements unchanged. | Accepted route chain: `concreteBPNativeSelectCloseInterpretedCosted` / `concreteBPNativeRankCloseInterpretedCosted` -> whole-query program -> `SuccinctClassic.queryCosted`; LCA rank seeds via `canonicalLcaCloseCostedWithRankSeed` rank parameter. | P = full value equality of the chunked twin with the accepted evaluator for EVERY invocation of the accepted consumers (same quantifiers; hypotheses only those provable from the structures' own fields), NOT result-agreement on valid queries only, NOT min-component-only. Q mutations (agreement only on sampled inputs; agreement only when the word is full-width; rank-only equality for select) rejected by the checked statements' quantifiers. | Checked at the Costed layer (`ChargedRankSelectLeaves.lean`, commit 63aa401): `bpChunkedRankCosted_value_eq : 0 < c -> data.wordSize <= 8*c -> forall target pos, (data.bpChunkedRankCosted c target pos).value = (data.rankCosted target pos).value` (covers all three rank-seed sites: route rank close, long-path flag rank, sparse-directory flag rank — the hypothesis is the structure's own field via `machineWordBits_le_8_mul_bpFringeChunkBits`); `bpChunkedDenseTwoWordSelectCosted_value_eq`; `SparseExceptionDirectory.bpChunkedReadCosted_value_eq`; `SparseExceptionSelectData.bpChunkedSelectCosted_value_eq : ... forall idx, value = (data.selectCosted idx).value`; inherited `_exact` theorems (`= Succinct.rankPrefix`, `= Succinct.select`); literal costs 11/27/12/35 derived. P = full value equality at EVERY invocation (quantifiers over all target/pos/idx; hypotheses only the structures' own word-size fields); Q1 (sampled inputs) rejected: universal quantification, no sampling; Q2 (full-width words only) rejected: `read_word_length_le` covers short trailing words, and the in-word lemmas clamp at `word.length`; Q3 (rank-only equality for select) rejected: the select statement equates the full `Option Nat` value | Closed (Costed layer at M3, P = Q same quantifiers; route-level swapped consumers landed at the M5 swap, REQ-B3-06 closed) |
| REQ-B3-06 | Wiring: the accepted route's emissions are replaced so that "the charged event vocabulary collapses to `readWord` only"; "all Lean consumers updated so `lake build RMQ` stays green at EVERY commit (parallel-then-swap atomic commits)". | Roadmap | The accepted route definitions (`concreteBPNativeRankCloseInterpretedCosted`, `concreteBPNativeSelectCloseInterpretedCosted`, their Global/AtSegment word-trace twins and WithStore twins, and the LCA rank-seed threading) actually call the chunked leaves; trace layers emit `readWord` events only (window/sample reads at their existing segments, chunk-table reads at segment 21, select-table reads at the new segment); re-proved `_refines`/`_matchesReadStore`/`_no_syntheticCostOnlyPrimitive`/`_eq_of_agree`/`_store_parametric` surface; whole-query exactness consumed unchanged. | Whole-query trace (`concreteBPNativeSuccinctRMQWholeQueryGlobalWordTrace*`), adequacy record, provenance packets. | The swap must be atomic per commit with the store extension (C05 pattern); a mutation swapping rank targets or table segments must break the re-proved exactness/matchesReadStore; no commit may contain a counted-but-unread source or an unread counted table. | Parallel layer delivered (commits 4cd74c1, 5eba561; accepted route untouched, store not extended): `ChargedRankSelectLeafTrace.lean` chunked leaf trace twins (WithStore, every event a genuine `readWord` of the supplied store) with `_toCosted_of_agree`/`_trace_forall`/`_matchesReadStore`/`_no_syntheticCostOnlyPrimitive`/`_store_parametric`; `ChargedRankSelectWiring.lean` chunked route Costed consumers (`concreteBPNativeChunkedSelectCloseCosted` value_eq/cost<=35/exact, `concreteBPNativeChunkedRankCloseCosted` value_eq/cost<=11/exact, all hypotheses discharged at shape level) and the chunked rank-close global word trace with `_refines` + rank-leg `_events_readWord` at the canonical store (segments 17/18/19/21); Checked (M5 swap commit): `concreteBPNativeSelectCloseInterpretedCosted := concreteBPNativeChunkedSelectCloseCosted`, `concreteBPNativeRankCloseInterpretedCosted := concreteBPNativeChunkedRankCloseCosted`; `concreteBPNativeSelectClose{,Global}WordTraceResult := concreteBPNativeChunkedSelectCloseGlobalWordTraceResult` (canonical store, chunk segment 21, select segment 22); `concreteBPNativeRankCloseWordTraceResultAtSegment := bpChunkedRankTraceResultWithStore` at the seed store `base..base+2, base+4` with `_canonical_eq` at base 17; LCA rank-seed threading receives the swapped rank close; `_refines(_interpretedCosted)`/`_matchesReadStore`/`_no_syntheticCostOnlyPrimitive`/`_eq_of_trace_read_agreement` (compositional StoreTraceLocal chain in `SuccinctFinalStoreParam`)/`_store_parametric` re-proved; whole-query exactness consumed unchanged; every event a `readWord` (REQ-B3-10); atomic with the segment-22 store extension per C05; `lake build RMQ` green at every commit | Closed |
| REQ-B3-07 | "new ReviewerSource constructor(s) APPENDED last (segment(s) 22+, dead-source anti-vacuity witness moved to the new first dead segment at identical strength, new segments proven live by actual reads)". | Local+roadmap | New `ReviewerSource` constructor for the select chunk table appended after `.fringeChunkTable` (segment 22); segment map `22 => some .selectChunkTable`, tail `_ + 23 => none`; adequacy fields regenerated (`canonical_segments_complete <-> segment < 23`, `compatibility_tail_unreachable` at `23 <=`); manifest `nodup` (22 sources), region injectivity, operational liveness + producer may-path + successful-occurrence witness for the new source over an actual execution; `FreshUnusedCanonicalSource` dead-source witness moved to segment 23 at identical strength. | Reviewer physical store; same chain as REQ-B2-17. | Removing the new source breaks erasure/coverage; adding a duplicate breaks `nodup`; the constructor enters the universe in the same commit as the wiring (no dead counted region at any commit); the liveness case must cite an actual read of segment 22 by the accepted execution, not a label. | Checked (`ReviewerPhysical.lean`, M5 swap commit): `ReviewerSource.selectChunkTable` appended after `.fringeChunkTable`; `concreteBPNativeSuccinctRMQReviewerSegmentSource? 22 = some .selectChunkTable`, tail `_ + 23 => none`; adequacy `canonical_segments_complete <-> segment < 23`, `compatibility_tail_unreachable` at `23 <=`; manifest nodup over the 23 sources; capacity via `bpChunkSelectRowCount_le_linear`, width via `bpChunkSelectEntryWidth_le_machineWordBits_capacity`; dead-source witness (`FreshUnusedCanonicalSource`) moved to segment 23 at identical strength; liveness: `counted_producer_may_path` covers `.selectChunkTable` via `concreteBPNativeChunkedInWordSelect_selectTable_mayRead` (genuine segment-22 read of the canonical store); successful-occurrence witness over an actual execution: `reviewerSingleton_selectChunkTable_successful_read` (the singleton whole-query select dense route fires a SUCCESSFUL segment-22 read at slot 0), consumed by the `.selectChunkTable` case of `concreteBPNativeSuccinctRMQReviewerSource_small_successful_closed_valid_occurrence` | Closed |
| REQ-B3-08 | "erasure/capacity/payload-overhead extension preserving the exact public statement shape" (public `buildPayload.length <= 2*n + overhead n`, `overhead = o(n)`, capstone `reviewerPayload = buildPayload` conjunct stays `rfl`). | Local+roadmap | Amended reviewer payload layout gains the select-table payload appended last; re-checked `concreteBPNativeSuccinctRMQCanonicalReviewerPayload_length_le` and `..Overhead_littleO`; extended erasure fold `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases`; extended capacity `PhysicalWords_length_le_capacity` with the new per-source linear feed; word-width/address folds extended; public `SuccinctClassic.buildPayload`/`overhead` statement shapes verbatim-preserved; read-backing predicate gains the select-table disjunct tying successful segment-22 reads to the counted slice. | Public statement `buildPayload_length`/`overhead_littleO`; INV-STORE-IDENTITY chain. | Two-payload trap: counted select-table component must be the same object the execution reads (flatten equality at the reviewer consumer); o(n) proof must include the new branch; deleting the new capacity summand must break the closing omega. | Checked (`FlatPayload.lean` + `SuccinctRMQClassic.lean`, M5 swap commit): reviewer payload layout gains `(bpChunkSelectTable c false).payload` appended last (`_selectChunk_slice`); overhead += `bpChunkSelectTableOverhead`; `concreteBPNativeSuccinctRMQCanonicalReviewerPayload_length_le` and `..Overhead_littleO` re-checked with the new branch inside the o(n) proof; erasure fold `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases`, capacity `PhysicalWords_length_le_capacity` (new linear summand), word-width/address folds extended; public `SuccinctClassic.buildPayload`/`overhead` statement shapes verbatim; capstone `reviewerPayload = buildPayload` conjunct stays `rfl`; read-backing predicate gains the segment-22 disjunct tying successful select-table reads to the counted slice (two-payload trap: the executed store maps segment 22 to the same `bpChunkSelectTable` object counted by the layout) | Closed |
| REQ-B3-09 | "cost re-derivation with named component algebra - new select/rank component bounds derived, whole-route literal derived (never asserted), the 142 frozen as historical exactly as 76/328 were". | Local+roadmap | `CanonicalRMQChargedTraceCostAlgebra` fields `selectClose`/`rankClose` become the chunked derived constants (projected 35 and 11; the checked derivation output is authoritative); new whole-route literal derived by `rfl` from `wholeQuery` (projection `2*35 + (2*11 + 2*37 + 30) + 11 = 207`); every Lean consumer of the live 142 re-proved at the derived literal (`SuccinctFinalRAM` algebra/`_eq`/weight-sum rename, adequacy `cost_le`/weight field, `SuccinctClassic.queryCost_eq`, headlines conjunct + abbrev, `Validation/SuccinctClassic`, cost harness boolean, `RMQExamples` `#guard`); 142 frozen as a historical constant (silent-in-word-rank-select route) following the 76/328 pattern, with the 76/328 chains still checked. | Whole-route cost chain: chunked rank component bound -> chunked select component bound -> LCA close cost -> whole-query `cost_le` -> public `queryCost_eq`. | The literal must be derived (`rfl` from the algebra), never asserted; mutating a component constant must break the `_eq` derivation; the executable harness must compute the same literal from the live route. | Checked (M5 swap commit): `concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCostAlgebra` fields `selectClose := 35`, `rankClose := 11` (derived from `bpChunkedSelectCosted_cost_le`/`bpChunkedRankCosted_cost_le` via the M5-prep wrappers); DERIVED literal `concreteBPNativeSuccinctRMQPrincipledAllSizeChargedTraceCost_eq : ... = 207 := by rfl` (the checked derivation confirms the projection `2*35 + (2*11 + 2*37 + 30) + 11`); every live 142 consumer re-proved (`SuccinctFinalRAM` algebra/`_eq`/`nonSyntheticWeight_sum_le_207` rename, adequacy fields, `SuccinctClassic.queryCost_eq = 207`, headlines conjunct + `SumLe207` abbrev, `Validation/SuccinctClassic.canonicalBoundOK`, harness `canonicalBoundIs207`, `RMQExamples` `#guard 207`); 142 frozen as `concreteBPNativeSuccinctRMQSilentWordRankSelectChargedTraceCost(_eq/Algebra)` with public `canonicalSilentWordRankSelectQueryCost_eq = 142` following the 76/328 pattern; 76/328 chains still checked; the harness executable computes the same literal from the live route | Closed |
| REQ-B3-10 | "After the swap, prove the new vocabulary fact: a theorem that every event of the accepted whole-query trace is a `readWord` (this is the B-campaign's signature result - name it clearly, it will be paper-facing)." | Public surface | Checked theorem over the accepted whole-query global word trace (the same object the cost/adequacy theorems name): every trace event is a `readWord segment index word?` (structural constructor claim), universally quantified over shape/left/right exactly as the accepted trace theorems are; clearly named + headline abbrev. | Paper/headline surface; consumed as the B-campaign signature alongside `queryCost_eq`. | The statement must constrain the actual whole-query trace object (not a component, not a relabeled sibling); a vocabulary predicate defined as `True` or as membership in a hand-listed event set is rejected - use the `TraceEvent` constructor shape; the retired route (emitting `wordRank` at rank seeds) must falsify the same statement conceptually, recorded via the frozen historical constants. | Checked (M5 swap commit): `concreteBPNativeSuccinctRMQWholeQueryGlobalWordTraceResult_readWord_only : forall shape left right, forall event, event in (concreteBPNativeSuccinctRMQWholeQueryGlobalWordTraceResult shape left right).trace -> event.isReadWord` — the SAME accepted whole-query trace object named by the cost/adequacy theorems, universally quantified exactly as they are; the predicate is the structural `TraceEvent.isReadWord` constructor claim (not `True`, not a hand-listed event set); paper-facing headline abbrev `succinctRMQWholeQueryGlobalWordTraceResultReadWordOnly` registered in `Headlines/RMQ.lean`, `scripts/headline_axiom_check.lean`, and the `paper_topology_lint.ps1` required-anchor list; the retired route emissions are recorded via the frozen `canonicalSilentWordRankSelectQueryCost` constants | Closed |
| REQ-B3-11 | Process: "extend the frozen matrix FIRST in its own commit"; "keep `B2_WORKLOG.md` (rename-in-place to B_WORKLOG.md if cleaner - record the decision) current per milestone"; "quote exact propositions in evidence rows with P/Q adversarial pairs for equivalence rows"; DESIGN_DECISIONS entries for each design choice with alternatives. | Process | This matrix commit precedes implementation; `docs/internal/B3_WORKLOG.md` created (decision: separate file, `B2_WORKLOG.md` left frozen - recorded in DD) and updated per milestone; DD entries for: rank-popcount derivation vs new table, select-table shape/target, segment placement, worklog file naming, fold caps and effective-limit clamp. | Coordinator audit of branch history. | `git log` order check at final report. | Matrix extension committed first (a32713c) before any implementation commit; `docs/internal/B3_WORKLOG.md` created (separate-file decision in DD-20260717-005) and updated per milestone with verification ledger; DD entries: DD-20260717-005 (design snapshot), DD-20260718-001 (M4b register-presentation retirement), DD-20260718-002 (M5 swap: seed-store `+4` layout, Register legacy split, deleted/restored legacy theorems, nav rank-leg rewire, kernel-safety witness pattern); equivalence rows carry quoted propositions with P/Q adversarial pairs (REQ-B3-01/02/05); `git log` order verified at the final report | Closed |
| REQ-B3-12 | "a value-dependency corruption witness for at least one new table" (inherited B2 wording: corrupting a read table entry changes the output on a witness input; the corrupted entry must be in the actual charged read footprint). | Local | Checked corruption theorem for the select chunk table: a witness invocation of the chunked in-word select over the honest table vs a one-entry-corrupted table returns different VALUES, where the corrupted slot is exactly a slot the witness invocation reads (slot equality by `decide`), and the conclusion is about the returned candidate value, not the trace. | Charged select evaluator; INV-VALUE-DEPENDENCY. | Corrupting an untouched entry changing nothing is not evidence; record the corrupted slot, honest value, and corrupted value. | Checked (`ChargedWordChunks.lean`): `bpChunkSelectTable_corruption_changes_select_value` — honest `bpChunkedWordSelectCosted ... 1 false [false] 0` returns `some 0`; with `bpChunkSelectCorruptedTableWitness` (entry slot 0 set to 1) it returns `some 1`; slot 0 is exactly the slot the witness invocation reads (`hslot : bpChunkSelectSlot 1 (bpFringeWindowChunkValue 1 [false] 0) 0 = 0` by decide; the read fires because the routing count of the honest rank read is 1 > 0, `hfound`); conclusion is about `.value` (the returned candidate), not the trace; proof routes through `bpChunkedWordSelectCostedFrom_step_found` + `readCosted_erase`, never reducing through table widths | Closed |
| REQ-B3-13 | "fix stale '76' docstrings at `RMQ/Headlines/RMQ.lean:27` and `RMQ/Core/SuccinctFinalRAM.lean:9543`; add a clarifying comment at the legacy `FlatPayloadSegmentSource?` segment-21 label (`RMQ/Core/.../FlatPayload.lean:280`) noting the canonical store's segment 21 is now the fringe chunk table. Do NOT do broader doc migration (B5)." | Docs | The two docstrings state the current derived literal (the B3 derived literal once the swap lands in this rung; 142 if fixed before the swap commit); the FlatPayload legacy map carries the clarifying comment; no other prose migration. | Coordinator-queued B2 reconstruction audit findings. | The docstring fix must match whatever literal is live at the commit where it lands (no stale intermediate). | Commit fcd491e: `RMQ/Headlines/RMQ.lean` and `RMQ/Core/SuccinctFinalRAM.lean` two-sided-profile docstrings now state the live literal `142` (live at that commit; if the B3 swap re-derives a new literal in this rung the same docstrings are re-synced in the swap commit); clarifying comment added above `| 21 => some .closeSummaryMinRel` in `FlatPayload.lean` noting the canonical reviewer/global store's segment 21 is the fringe chunk table; no other prose touched; `lake build RMQ` green, `claim_drift_scan.ps1` 0 strict failures | Closed at this commit; re-sync to the B3 derived literal is part of the REQ-B3-09 swap commit |
| REQ-B3-14 | FORBIDDEN (verbatim): "sorry/admit/axiom/native_decide/partial/unsafe/implemented_by/Mathlib; dead sources at any commit; asserted constants; deleting/renaming frozen public identities; breaking `lake build RMQ` at any commit; weakening any closed B2 row; size dispatch on the public route." | Inherited hygiene | `rg` hygiene battery with no NEW hits in touched files; every commit's build green (ledger); no public identity deleted/renamed; every closed B2 row's evidence object unchanged or strengthened; no size/readiness dispatch introduced. | All touched modules. | Deliberate search for threshold guards on the public route introduced by this rung; diff review of frozen identities. | Hygiene rg battery over the candidate tree: no NEW hits of the forbidden tokens and no `native_decide`/`ofReduceBool` in touched files (M6 ledger); `lake build RMQ` green at every commit (ledger); no frozen public identity deleted or renamed — the only deletions are non-frozen internals recorded in DD-20260718-002 (private StoreParam pullback helpers, the superseded nav bridge `concreteBPCloseNavigationRankCloseInterpretedCosted_refines_rankCloseCosted`, the superseded legacy-store rank matches noted at the B3-03 checkpoint), and every closed B2 row evidence object is unchanged or restated over the Register names at identical strength; no size/readiness dispatch introduced on the public route (deliberate grep over touched modules: none) | Closed |
| INV-B3-STORE-IDENTITY | Inherited INV-STORE-IDENTITY: "the exact payload/store executed is the payload/store counted by the public space theorem". | Inherited | Select-table reads execute against segment 22 of the canonical global/reviewer read store whose words flatten into the amended counted payload; rank-recharge reads execute against the SAME segment-21 table already counted by B2 (no sibling table). | REQ-B3-08 chain. | Two-payload trap as in B2's INV-STORE-IDENTITY row. | Select-table reads execute against segment 22 of `concreteBPNativeSuccinctRMQGlobalReadStore`, which maps 22 to the SAME `(bpChunkSelectTable c false).store.words` counted by the amended reviewer payload slice (`_selectChunk_slice` + read-backed disjunct); rank recharges read the SAME segment-21 fringe chunk table already counted by B2 (no sibling table); flatten equality at the reviewer consumer preserved (capstone `rfl` conjunct) | Closed |
| INV-B3-VALUE-DEPENDENCY | Inherited INV-VALUE-DEPENDENCY: "returned values and routing decisions depend on actual charged reads". | Inherited | Chunked rank folds accumulate decoded read values; the select fold's routing branch (`k < count_j`) is computed from the decoded read, and the final position from the select-table read; corruption witness (REQ-B3-12); no spec-entry access on the charged path. | Charged evaluator definitions. | Inspect for `entries[i]?` access in the charged path; the routing decision must come from the read, not a precomputed rank. | Chunked rank folds accumulate decoded read values (`bpWordRankStepDecoded ... entry?`); the select fold routing branch `k < bpChunkRankOfEntry ... (entry?.getD 0)` is computed from the decoded read and the final position from the select-table read; corruption witness closed at REQ-B3-12; no spec-entry access on the charged path (inspection of the swapped evaluators) | Closed |
| INV-B3-SEMANTIC-NONVACUITY | Inherited INV-SEMANTIC-NONVACUITY (P/Q parity for every equivalence/liveness claim). | Inherited | P/Q records in REQ-B3-01/02/05 rows; liveness of the new source proven from actual reads (REQ-B3-07); vocabulary predicate structural (REQ-B3-10). | Matrix rows above. | See per-row challenges. | P/Q records closed in REQ-B3-01/02/05; liveness of `.selectChunkTable` proven from an actual segment-22 read of an actual closed valid execution (REQ-B3-07 W19 witness); the vocabulary predicate is structural (`TraceEvent.isReadWord`, REQ-B3-10) | Closed |
| INV-B3-READ-BACKING | Inherited INV-READ-BACKING: "every successful read is backed positionally by the counted store". | Inherited | New-table reads via `FixedWidthNatTable.readCosted`/`FlatStoreComputation` with `store_erases` ties to the counted payload; regenerated read-backing adequacy disjunct for segment 22. | Table store construction + adequacy. | No read path returns spec entries instead of stored words. | Select-table reads route through `FixedWidthNatTable` stores with `store_erases` tying words to the counted payload; the read-backing adequacy predicate gains the regenerated segment-22 disjunct (REQ-B3-08); no read path returns spec entries (charged evaluators decode stored bits only) | Closed |
| INV-B3-WORD-WIDTH | Inherited INV-WORD-WIDTH + INV-ADDRESS-WIDTH: stored/returned words and every executed address fit the declared machine word at all sizes. | Inherited | Select-table entry width at most reviewer word bits at every n (tiny n included); slot addresses < rowCount with rowCount inside the address capacity fold; chunk-table addresses already covered by B2. | Store extension rows. | Tiny-n check (n = 0, 1). | `bpChunkSelectEntryWidth_le_machineWordBits_capacity` bounds the select-table entry width inside the reviewer word declaration at every n (tiny n included; the capacity fold covers n = 0, 1); slot addresses bounded by `bpChunkSelectSlot_lt_rowCount` with `bpChunkSelectRowCount_le_linear` inside the address capacity fold; chunk-table addresses covered by the B2 folds | Closed |
| INV-B3-ALL-SIZE | Inherited INV-ALL-SIZE: "exactness covers all assigned sizes and edge cases without hidden readiness or compatibility dispatch". | Inherited | All equivalence/cost theorems quantified over all shapes/sizes/words; the only caps are uniform arithmetic formulas proven identity on the reachable domain (B2's `Nat.min _ 33` pattern); no `Ready`/threshold predicates. | REQ-B3-01/02/05. | Grep for readiness predicates; smallest-size kernel-checked coverage where informative (c = 1 witness inputs). | All swapped equivalence/cost theorems quantified over all shapes/left/right/pos/idx; the only caps are the uniform `Nat.min _ 8`/`Nat.min _ 9` chunk-count formulas proven identity on the reachable domain (`bpWordChunkCount_eq` under `word.length <= 8c` from the structures own fields); grep for readiness predicates over touched modules: none; smallest-size kernel evidence: the c = 1 witnesses (REQ-B3-12 corruption witness, the W19 singleton at chunk bits 1) | Closed |
| INV-B3-NO-SYNTHETIC | Inherited INV-NO-SYNTHETIC: no synthetic events, decorative rereads, or post-hoc replay. | Inherited | Trace layers built from `FlatStoreComputation` reads (no `ofCosted` markers); `_no_syntheticCostOnlyPrimitive` re-proved for every swapped consumer; every charged unit is a store read or an accepted window/sample tick. | Trace lemmas. | REQ-B3-12 decorative-read challenge. | `_no_syntheticCostOnlyPrimitive` re-proved for every swapped consumer (select/rank legs, LCA composition, whole query); trace layers are read-atom folds with no `ofCosted` markers; `nonSyntheticWeight_sum_eq_trace_length`/`_sum_eq_cost` re-proved and bounded by the derived 207 (`_sum_le_207`) | Closed |
| INV-B3-CATEGORY-SEPARATION | Inherited INV-CATEGORY-SEPARATION. | Inherited | Spec entry lists proof-layer only; charged evaluators decode stored bits; costs are model ticks. | New module structure. | Inspect charged evaluators for spec access. | Spec entry lists remain proof-layer only (`bpChunkSelectEntries` consumed by proofs; the charged path reads `bpChunkSelectTable.store.words` bits); charged evaluators decode stored bits; costs are model ticks (inspection of the chunk modules and the swapped route) | Closed |
| CHK-B3-01 | "`lake build RMQ`" green at every commit; final battery also "`lake build RMQPaper RMQExamples`". | Verification | Exit 0 per commit (ledger with durations). | Touched modules. | - | Exit 0 per commit (ledger); candidate tree: full `lake build RMQ` green (fresh compile of every swapped module; final synchronous verification `Build completed successfully`, exit 0); `lake build RMQPaper RMQExamples` exit 0, 46 s (M6 ledger) | Closed |
| CHK-B3-02 | "hygiene rg (no NEW hits)". | Verification | Forbidden-token and native-decide scans over touched files: no new hits. | Hygiene. | - | rg forbidden-token scan over `RMQ`+`lakefile.toml`: 0 hits (hence no NEW hits); `native_decide|ofReduceBool` over `RMQ`: 0 hits (M6 ledger) | Closed |
| CHK-B3-03 | "`git diff --check` + `git diff --check d1d645e..HEAD`". | Verification | Exit 0. | Hygiene. | - | `git diff --check` clean at the candidate working tree; `git diff --check d1d645e..HEAD` clean at the final HEAD (the only transient hits were the committed B3-03 recovery patch `docs/internal/B3_M5_WIP.patch`, deleted in the bookkeeping commit; patch files inherently carry patch-context whitespace) | Closed |
| CHK-B3-04 | "`design_decision_check.ps1 -Strict -Base d1d645e`" (full SHA `d1d645ee221c1756f8c61c5ea222950f73e43c8c`). | Verification | Exit 0. | Process. | - | `design_decision_check.ps1 -Strict -Base d1d645ee221c1756f8c61c5ea222950f73e43c8c` exit 0 (35 changed files; code decisions in DD-20260717-005/DD-20260718-001/DD-20260718-002, workflow decision WDD-20260718-001), 3 s | Closed |
| CHK-B3-05 | "`claim_drift_scan.ps1`". | Verification | Exit 0 (0 strict failures). | Public surface. | - | `claim_drift_scan.ps1` exit 0 (762 hits, 0 strict failures), 10 s | Closed |
| CHK-B3-06 | "`paper_topology_lint.ps1` (mutex-held; stale-doc-constant fixes allowed, structural failures stop-and-report)". | Verification | Exit 0 after at most stale-constant doc sync. | Public surface. | - | `paper_topology_lint.ps1` exit 0 (mutex-held, 94 s; PASS, 82 documentary identifiers / 48 paper identifiers resolved incl. the new `ReadWordOnly` anchor). First run failed only on the stale `SumLe142` documentary rows in the claim registries - stale-doc-constant sync applied per the allowed policy (commit 25a310b); no structural failure | Closed |
| CHK-B3-07 | "cost harness executable". | Verification | `lake build rmq_succinct_classic_cost_harness` + `lake exe` exit 0 with the derived literal boolean true. | Executable validation. | - | `lake exe rmq_succinct_classic_cost_harness` exit 0, 55 s: every reported window `agrees=true`, `canonicalBoundIs207=true` (the executable computes the derived literal from the live route), `underCanonicalBound=true`; closing line `all reported windows agree with reference List Int RMQ semantics` | Closed |
