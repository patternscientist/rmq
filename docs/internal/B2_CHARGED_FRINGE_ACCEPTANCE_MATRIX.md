# B2 Charged Fringe Acceptance Matrix (frozen before implementation)

Worker: B2-01 (branch `claude/b1-b2-charged-fringe-tables`, base `b6338ea`).
Contract source: `docs/internal/OPTION_B_CHARGED_FRINGE_DESIGN.md` (B1/B2 core)
plus the B2-01 delegation prompt. Requirement wording below is verbatim from
those sources. Frozen at matrix commit; after this commit only evidence,
status, and coordinator-approved amendments may change.

Accepted-route objects the rows refer to (verified in this worktree):

- silent fringe leaves: `localBPLeftFringeCandidateSeededCosted`
  (`RMQ/Core/SuccinctClose/RelativeRmmMacro/LocalBPDecoder.lean:1071`),
  `localBPRightFringeCandidateSeededCosted` (`:1087`), both `cost := 4` with
  values from `localBPSeededPrefixRangeMinExcess`/`...ArgMinPrefixPos`
  (`:937`/`:929`), per-position recursion at `:797`.
- trace twins: `ConcreteDirectoryRAM.lean:267/:302`; with-store twins:
  `ConcreteDirectoryRAMStoreParam.lean:4387/:4484`.
- accepted consumers: `canonicalCrossBlockCloseCostedWithRankSeed`
  (`ConcreteDirectoryRAM.lean:2320`, fringe calls at `:2332`/`:2350`),
  `crossBlockCloseTraceResultWithRankSeedAllSizeStructuralAtSegments`
  (`:2358`), with-store `ConcreteDirectoryRAMStoreParam.lean:4614`;
  seed threading `localBPSeedFromRankCloseCosted`
  (`LocalBPDecoder.lean:557`) / `localBPSeedFromRankCloseTraceResult`
  (`ConcreteDirectoryRAM.lean:1530`).
- fringe cost constant: `canonicalEndpointFringeChargedTraceCost = 4`
  (`ConcreteDirectoryRAM.lean:2308`), consumed by
  `canonicalCrossBlockCloseCostedWithRankSeed_cost_le_principled` (`:2476`)
  and the 76-literal chain (`SuccinctFinalRAM.lean:8635-8669`,
  `SuccinctRMQClassic.queryCost_eq : queryCost = 76` at
  `SuccinctRMQClassic.lean:111`).
- table pattern: `SuccinctSpace.FixedWidthNatTable`
  (`RMQ/Core/SuccinctSpace/Tables.lean:23`), `readCosted` (`:86`),
  machine view `MachineChunkedTable.lean:219-238`.
- store universe: `ReviewerSource` (20 constructors,
  `RMQ/Core/SuccinctFinal/RAM/ReviewerPhysical.lean:24-45`), erasure fold
  `reviewerSources_erases` (`:725`) /
  `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` (`:786`),
  capacity `concreteBPNativeSuccinctRMQReviewerCapacity n = 400000*(n+1)`
  (`:1414`), `concreteBPNativeSuccinctRMQReviewerPhysicalWords_length_le_capacity`
  (`:1771`); segment map `:83-106` (segments 0-20;
  `SuccinctFinalModelAdequacy.lean:112-116` bakes `segment < 21`).
- public space statement: `SuccinctClassic.buildPayload`
  (`SuccinctRMQClassic.lean:130`), `overhead` (`:81`),
  `buildPayload_length : (buildPayload xs).length <= 2 * xs.length + overhead
  xs.length` (`:932`), `overhead_littleO` (`:926`).

| ID | Exact frozen requirement | Scope | Evidence needed (exact proposition/check) | Named consumer and identity/composition chain | Anti-vacuity challenge | Evidence obtained | Status / residual gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REQ-B2-01 | "build o(n)-bit half-word chunk lookup tables (min-excess/argmin/delta-excess per chunk value)" with "table correctness: entry fields agree with `bpExcessAtBits` / `bpPrefixRangeMinExcessBits` / argmin (`LocalBPDecoder.lean:1728-1756`, `EndpointFringe/PrefixRange/PrefixArgMin.lean:392`) on the chunk". | Local | Checked theorems: for every chunk value `v < 2^c` and in-range offsets, the delta / range-min / range-argmin entries decode (offset-encoding removed) to the `Succinct.rankPrefix`-level excess statistics of the `c`-bit pattern of `v`; plus a bridge to the `bpExcessAtBits` twins where truncation-free (excess-nonnegative) hypotheses hold. Entry lists realized as `SuccinctSpace.FixedWidthNatTable` instances. | Consumed by REQ-B2-02 fold-equivalence proof; identity chain: table `entries[i]?` = spec function of decoded `(v,a,b)` = rankPrefix stats of `natToBitsLE c v`. | Mutate one entry (e.g. off-by-one offset constant, argmin rightmost instead of leftmost) and name the correctness theorem that fails; verify the offset decode is proven, not defined-true (see REQ-B2-11). | (pending) | Open |
| REQ-B2-02 | "fringe-value equivalence: the charged chunked fringe candidate equals the accepted seeded fringe candidate for every valid invocation reachable from the accepted route (value-level identity, not just result agreement)" — accepted candidates being the values of `localBPLeft/RightFringeCandidateSeededCosted`. | Local+roadmap | Checked theorem(s): `(chunkedLeftFringe... ).value = (localBPLeftFringeCandidateSeededCosted shape blockSize leftClose seed).value` (and right twin) universally quantified over shape/blockSize/close/seed under hypotheses that hold at every accepted call site (window/seed validity discharged from `CartesianShape` BP facts at those sites), not on sampled inputs. | REQ-B2-08 wiring/substitution lemma; chain: chunked fold = generic scan merge = accepted per-position scan (`localBPSeededPrefixRangeMinExcess/ArgMinPrefixPos`). | Record accepted predicate P (value equality of the pair (minExcess, argmin) for all reachable invocations) vs mutation Q (equality only of min component, or only on even-length windows, or with rightmost tie policy); exhibit the checked theorem whose statement rejects each Q. | (pending) | Open |
| REQ-B2-03 | "literal read-count bound for the chunked fringe (all sizes, no guards)" — "a literal all-size read-count bound". | Local | Checked theorem: `(chunkedLeftFringe...).cost <= <literal Nat>` (and right twin) with no size hypothesis, no readiness guard; the literal is a numeral. | Feeds REQ-B2-08 cost re-derivation / successor wiring rung. | Confirm no hypothesis of the bound theorem restricts n; check tiny sizes (n = 0,1,2) are covered by the same statement. | (pending) | Open |
| REQ-B2-04 | "store extension: new `ReviewerSource` constructor(s) for the table(s), erasure extension (`reviewerSources_erases` fold, `ReviewerPhysical.lean:725,786`)". | Local+roadmap | New constructor(s) in `ReviewerSource`; sources list, segment map, region map extended; extended erasure theorem `concreteBPNativeSuccinctRMQReviewerPhysicalWords_erases` (flatten of physical words = live payload) still checked with the new source(s) included; `ReviewerPhysical.lean` compiles. If extension breaks `SuccinctFinalModelAdequacy` (B4 scope per delegation), record exactly what breaks and why. | Reviewer physical store; chain: table payload words are the new source's words; flatten equality at the extended payload. | Remove the new source from the sources list and check the erasure/coverage theorem fails; add a dead duplicate source and check `nodup`/manifest theorems reject it. | (pending) | Open |
| REQ-B2-05 | "per-source linear capacity bound feeding `PhysicalWords_length_le_capacity` (`ReviewerPhysical.lean:1771`; crude `2^(chunkBits)*w <= 4n`-style bounds suffice given `reviewerCapacity n = 400000*(n+1)`)". | Local | Checked per-source word-count bound for the table source(s) linear in n, composed into the re-proved `concreteBPNativeSuccinctRMQReviewerPhysicalWords_length_le_capacity`. | Same as REQ-B2-04; consumer `PhysicalWords_length_le_capacity` (omega over per-source bounds). | Check the bound covers all n (no `n >= n0` hypothesis); check the new source's words are actually included in the capacity sum (delete its summand and the omega fails). | (pending) | Open |
| REQ-B2-06 | "table bits fold into the space overhead: amended `buildPayload`/`overhead` with the public shape `buildPayload.length <= 2*n + overhead n`, `overhead = o(n)` preserved (`SuccinctRMQClassic.lean:925-941`)" with "`overhead = o(n)`" via `LittleOLinear` and "exact physical erasure". | Local+roadmap | Amended `buildPayload` definition includes the table payload; re-checked `buildPayload_length` with amended `overhead`; re-checked `overhead_littleO : SuccinctSpace.LittleOLinear overhead`; table-payload erasure (store words flatten to counted payload component). | Public statement `SuccinctRMQClassic.buildPayload_length` / `overhead_littleO`; chain: buildPayload = reviewer payload ++ table payload (or equivalent), table component length bounded by proven o(n) function. | Check the o(n) proof does not exclude the table branch (dense-table trap in KNOWN_FAILURE_MODES); confirm table bits are counted in the same payload the public length theorem measures, not a sibling. | (pending) | Open |
| REQ-B2-07 | "The seed threading must preserve `localBPWindowBits_alone_does_not_determine_base_excess` (`LocalBPDecoder.lean:609`): base excess comes from the directory seed exactly as today." | Local | The chunked fringe takes `seed` as a parameter with the same threading (`localBPSeedFromRank...` unchanged at consumers); theorem `localBPWindowBits_alone_does_not_determine_base_excess` unchanged and still checked; no new code path derives base excess from window bits alone. | Accepted consumers' seed plumbing (`ConcreteDirectory.lean:45/:76`, `ConcreteDirectoryRAM.lean:2320`, `:1530`). | Inspect the chunked path for any seed reconstruction from window bits; the equivalence theorem must be stated for the threaded seed parameter, not a recomputed one. | (pending) | Open |
| REQ-B2-08 | "the B2 core rung is not closed until either (a) the chunked fringe is wired into the accepted whole-query route with the cost chain re-derived, or (b) the equivalence theorem is consumed by a committed bridging theorem that pins the future wiring (an exact-value substitution lemma at the accepted route's fringe call sites), with the wiring rung named as the immediate successor. Prefer (a) if time permits." | Roadmap | Either (a) amended `canonicalCrossBlockCloseCostedWithRankSeed` (+ trace/with-store layers) calling the chunked fringe, with the cost algebra re-derived to a new checked literal; or (b) a committed substitution theorem stating that replacing the fringe leaves by the chunked leaves at the exact accepted call sites preserves the crossBlockClose value (exact-value, per call site), plus the successor wiring rung named in the worklog/report. | The accepted route chain: fringe leaves -> `canonicalCrossBlockCloseCostedWithRankSeed` (`ConcreteDirectoryRAM.lean:2320`) -> `canonicalLcaCloseCostedWithRankSeed` -> whole-query program -> `SuccinctClassic.queryCosted`. | For (b): the substitution lemma must mention the accepted consumer's own definition (not a restatement); mutating the consumer expression (different blockSize argument, swapped left/right) must break the lemma's applicability. | (pending) | Open |
| REQ-B2-09 | "FORBIDDEN SHORTCUTS: no sorry/admit/axiom/native_decide/partial/unsafe/implemented_by/Mathlib; no uncounted table storage; no readiness thresholds or size dispatch on the public route; ... no equivalence 'on tested inputs'; no relabeled silent scan". | Inherited hygiene | `rg -n "\b(sorry|admit|axiom|unsafe|opaque|implemented_by|partial|extern|noncomputable)\b|import Mathlib" RMQ lakefile.toml` shows no NEW hits in touched files; table payload counted (REQ-B2-06); no size dispatch introduced; scan-shape check in REQ-B2-10. | All touched modules. | Deliberately search for `if n <` / threshold guards introduced by this branch on the public route. | (pending) | Open |
| REQ-B2-10 | "the per-position recursion over window positions must be gone from the charged path — a fixed fold over at most a literal number of chunks with constant-shape per-chunk work is the required shape"; "no decorative reads (every charged read's value must flow into the result — prove value dependency for at least one adversarial table-corruption case: corrupting a read table entry changes the fringe output on a witness input)". | Local | The charged chunked evaluator's recursion is structurally over a chunk list/counter bounded by a literal (visible in the definition), with per-chunk work not recursing over window positions; plus a checked corruption theorem: there exist a witness invocation and a corrupted table (one entry changed) such that the chunked fringe value differs from the uncorrupted value. | Charged fringe definition + REQ-B2-03. | The corruption witness must corrupt an entry that the actual charged read footprint touches; a corruption of an untouched entry changing nothing is not evidence. Record which read index is corrupted and the two differing output values. | (pending) | Open |
| REQ-B2-11 | "The offset-encoding of signed excess values in Nat tables must be proven, not assumed." | Local | Checked round-trip/exactness lemmas: encoded entry = c + (signed excess) as a proven identity over `Succinct.rankPrefix` quantities with explicit no-truncation side conditions discharged (not definitional tautologies about the encoder alone). | REQ-B2-01/REQ-B2-02 proofs consume the decode lemmas. | Attempt the truncation trap: a statement that holds vacuously because Nat subtraction clamps at 0 must be rejected by exhibiting the side condition; check the decode lemma is used at a site where the excess is actually negative relative to the chunk start (delta below offset). | (pending) | Open |
| REQ-B2-12 | Process: matrix committed before implementation (this commit); `docs/internal/B2_WORKLOG.md` maintained and committed at each milestone; `docs/internal/DESIGN_DECISIONS.md` updated for chunk boundary mechanism, table field encoding, store region placement. | Process | Git history shows matrix commit precedes implementation commits; worklog present at each milestone commit; DESIGN_DECISIONS entries exist for the three named choices. | Coordinator audit of branch history. | `git log` order check at final report. | (pending) | Open |
| INV-STORE-IDENTITY | Inherited: "the exact payload/store executed is the payload/store counted by the public space theorem". | Inherited | Table reads execute against the same table store whose payload is counted in amended buildPayload (flatten equality chain), not a sibling decoded structure. | REQ-B2-06 chain. | Two-payload trap: counted payload vs executed store words must be connected by a proved flatten/erasure equality at the consumer. | (pending) | Open |
| INV-VALUE-DEPENDENCY | Inherited: "returned values and routing decisions depend on actual charged reads, not a semantic answer computed before the reads". | Inherited | The chunked fringe value is computed from the `Costed`/trace read results (bind chain), with the corruption theorem of REQ-B2-10 as the dependency witness; no proof-field or pre-read semantic value feeds the output. | Charged fringe definition. | Corruption theorem conclusion must be about the returned candidate value, not the trace log. | (pending) | Open |
| INV-SEMANTIC-NONVACUITY | Inherited: equivalence/coverage predicates "derived from the operational construction they describe"; for mutation evidence "record the accepted predicate P, the rejected mutation predicate Q... Use the same predicate, or prove the checked implication P -> Q". | Inherited | For REQ-B2-01/02 mutation rows: P and Q recorded side by side with matching quantifiers/domains. | Matrix rows above. | See REQ-B2-02 challenge column. | (pending) | Open |
| INV-READ-BACKING | Inherited: "every successful read is backed positionally by the counted store". | Inherited | Table reads use `PayloadWordStore`/`FixedWidthNatTable` stores whose words flatten to the counted table payload (store `.erases`), following the `machineFootprint_successful_read_backed` pattern. | Table store construction. | Confirm no read path returns entries from the spec list instead of stored words. | (pending) | Open |
| INV-WORD-WIDTH | Inherited: "stored and returned words fit one declared modeled machine word". | Inherited | Table word width lemma: every stored table word length <= the route's machine word budget (`SuccinctRank.machineWordBits shape.bpCode.length` or the reviewer word-bits bound at the store level), all sizes. | Store extension rows. | Check tiny n: width bound must hold when `machineWordBits` is small (n = 0,1). | (pending) | Open |
| INV-ALL-SIZE | Inherited: "exactness covers all assigned sizes and edge cases without hidden readiness or compatibility dispatch". | Inherited | Equivalence and cost theorems quantified over all shapes/sizes; no `Ready` hypotheses on the new path. | REQ-B2-02/03. | Grep the new modules for readiness predicates; exercise smallest shapes in kernel-checked examples where informative. | (pending) | Open |
| INV-NO-SYNTHETIC | Inherited: "synthetic events, decorative rereads, and post-hoc replay do not support the execution claim". | Inherited | If a trace layer is built: no `syntheticCostOnlyPrimitive` in the chunked fringe trace (`no_syntheticCostOnlyPrimitive` lemma); at Costed level: every charged unit corresponds to a store read in the definition. | Charged fringe trace lemmas. | REQ-B2-10 decorative-read challenge. | (pending) | Open |
| INV-CATEGORY-SEPARATION | Inherited: "payload bits, proof fields, model ticks, machine state, Lean runtime, and measured performance remain distinct". | Inherited | Table spec lists (entries) are proof/spec-layer; charged reads decode stored bits; costs are model ticks; no crossover (spec entries never short-circuit the read path in the charged evaluator). | New module structure. | Inspect the charged evaluator for direct `entries[i]?` access. | (pending) | Open |
| CHK-01 | "`lake build` of the smallest root covering every module you touched". | Verification | Exit 0; root(s) named. | Touched modules. | Note any module intentionally left broken (B4-scope record). | (pending) | Open |
| CHK-02 | "`rg -n \"\\b(sorry|admit|axiom|unsafe|opaque|implemented_by|partial|extern|noncomputable)\\b|import Mathlib\" RMQ lakefile.toml` (pre-existing hits are fine — no NEW hits in your files)". | Verification | Diff of hits vs base commit: none added in touched files. | Hygiene. | — | (pending) | Open |
| CHK-03 | "`git diff --check`; after the final commit, `git diff --check b6338ea..HEAD`". | Verification | Exit 0, no whitespace errors. | Hygiene. | — | (pending) | Open |
| CHK-04 | "`powershell -ExecutionPolicy Bypass -File scripts\\design_decision_check.ps1 -Strict -Base b6338ea004d49461305ae1df70354a1710109352`". | Verification | Exit 0. | Process. | — | (pending) | Open |
