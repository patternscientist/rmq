{
 "attacks": [
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-2 ORACLE IN THE INSTRUCTION SEMANTICS. Add one constructor `| loadCanonicalWord (dst src : Nat)` to `BuildInstr`, whose `exec` arm computes `bitsToNatLE ((concreteBPNativeSuccinctRMQReviewerPhysicalWords (SuccinctClassic.cartesianShape (decodeInput (s.store.wordSegments[1]!))))[s.regs src]!)` and stores it in `regs dst`. `builder` = a closed 6-instruction loop: `loadCanonicalWord rW rI; writeMem 0 rI rW; incr rI; brNZ`.",
   "survives": true,
   "notes": "FATAL, and it is the fake Contract A itself claims to kill. Passes every row: O1 (builder is one closed term); O2 (the constructor's arguments are `Nat` only, so A's frozen structural side condition \u2014 no `Word`/`List Bool`/`List Int`/`CartesianShape`/function-typed ARGUMENT \u2014 is satisfied; `category := .arith` and `literalOperands := []` supply the wildcard-free arms); O3 (staticBits is defined over the PROGRAM, not the interpreter \u2014 moving the oracle from `builder` into `BuildInstr.exec` carries unbounded information at zero staticBits cost); O4; O5 (the oracle reads the input through `s.store`, so eval is a function of `(regs, readWord?)` exactly as O5 demands); O6/O7/O9 (the writes are real, addressed, distinct, last-write-wins, replayable); O8 (its write disjunct is `\u2203 index word, event = writeWord 0 index word`, a tautology); O10; O11/O12 (`built xs` IS the canonical store, so the bridge is `rfl`); O13; O14 (emits no events so `charged_iff_memory` holds with `.arith`, and `unchargedStepBound` is a DECLARED Nat per constructor with no theorem tying it to what `exec` actually computes \u2014 declare it `1`). Contract A has NO import-closure obligation anywhere. Root cause: A's entire anti-oracle apparatus constrains the PROGRAM and the CONSTRUCTOR SIGNATURES; nothing constrains the INTERPRETER. A's \u00a72 item 4 asserts the honest-shaped-trace fake 'is killed only by O1+O2+O3' \u2014 that is false, because `builder` never needs to contain `p`; `exec` does. Verified: `RMQ/Core/WordRAM.lean:1` is `import RMQ.Core.RAM` and nothing else, so the semantic namespace is reachable from any file that imports it, and A places `BuildMachine.lean` under `RMQ/Core/WordRAM/` with no closure constraint."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-2 same construction, attempted inside `RMQ/Core/Builder/Machine.lean`.",
   "survives": false,
   "notes": "EXCLUDED by REQ-BLD-13. The transitive import closure is pinned to exactly `{RMQ.Core.Cost, RMQ.Core.RAM, RMQ.Core.WordRAM}`; verified `RMQ/Core/WordRAM.lean:1 = import RMQ.Core.RAM` (sole import), so `RMQ.Cartesian`, `RMQ.SuccinctFinal`, `RMQ.GenericSelect`, `RMQ.SuccinctClose` are not nameable in Machine.lean or Program.lean. Lean import acyclicity means `Bridge.lean` (which does import the semantics) cannot inject into `builtStore`. REQ-BLD-01 (Nat-only fields) assists but would not alone have stopped it. This one row is the single largest quality gap between the two contracts."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-4 PAYLOAD SMUGGLED IN THE 'ENCODED INPUT'. `encodeInput xs := #[fatWord] ++ (per-element words)` sized to exactly `2*xs.length + 1` words, where `fatWord : List Bool` is the flattened canonical payload; `decodeInput` projects words 1..2n and ignores word 0. The builder then reads word 0 once and copies bit-ranges out.",
   "survives": true,
   "notes": "FATAL and independent of FK-2 \u2014 it needs no ISA change at all. VERIFIED ROOT CAUSE: `abbrev Word := List Bool` (RMQ/Core/WordRAM.lean:16) is UNBOUNDED in length, and A's O4 pins only the word COUNT (`O4_input_size : ...size = 2 * xs.length + 1`) and invertibility (`O4_faithful : decodeInput ... = xs`). Neither bounds word WIDTH, and neither forces the encoding to be pointwise. So `2n+1` cells can carry arbitrarily many bits. O3 is untouched because the payload lives in the INPUT, not the program. O5 holds (it is in the store). O6\u2013O12 all hold. A's own annotation on O4_input_size \u2014 'pins its length so it cannot BE the payload' \u2014 is wrong: it pins the number of cells, not their contents."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-4 same fat-word smuggle in `encodeInput`.",
   "survives": false,
   "notes": "EXCLUDED by REQ-BLD-08, which is materially tighter than A's O4: `(encodeInput xs).readWord? inputSegment i = (xs[i]?).map (encodeInt wordBits)` fixes both the width (`wordBits`) and the pointwise dependence, and `encode_is_pointwise : \u2200 i, cell i depends only on xs[i]?` is a separate stated row. A fat cell is not `encodeInt wordBits (xs[i])`; a payload-bearing cell is not pointwise. Widening `wordBits` does not help the adversary because `encodeInt wordBits x` still depends only on `x`. REQ-BLD-06's `initial_clean : \u2200 seg \u2264 29, ... = none` additionally closes the pre-seeded-footprint variant."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-5 PAYLOAD SMUGGLED IN THE PROGRAM (per-size unrolled constant emission). `builderProgram n` is a decision tree over the Catalan(n) shapes of size n: it reads the input to select a branch, then emits `const rV w_i; const rA i; writeMem 0 rA rV` for the branch's literal payload words. Cheap realistic variant: bake the shape-independent `fringeChunkTable` / `selectChunkTable` sources (ReviewerPhysical.lean sources 21\u201322) into the program as literal constants for all n below a threshold, and for all n above it.",
   "survives": true,
   "notes": "FATAL. REQ-BLD-09 requires the program to be a function of `n` ONLY and bounds its length by `progBound n` \u2014 but the row says verbatim 'with `progBound` derived' and B NEVER constrains `progBound`. `progBound n = Catalan(n) * |payload(n)|` is 'derived'. REQ-BLD-02's `FieldsFit wordBits` caps each FIELD, not the NUMBER of instructions, so a program of any length passes. REQ-BLD-13 does not bite: the program GENERATOR is ordinary Lean and can reimplement the enumeration inside the closure, and the cheap table-baking variant needs no enumeration at all. B has NO pigeonhole/static-information row \u2014 no analogue of A's O3. REQ-BLD-14b (\u2203 xs xs' of equal length with different built stores) is a single-witness existential and is satisfied by the decision tree. This is B's fatal defect and the exact mirror-image of A's."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-5 same per-size unrolled program.",
   "survives": false,
   "notes": "EXCLUDED, decisively, and this is A's one genuine advantage. O1 forbids the family outright: `builder : WordRAM.BuildProgram` is a declaration with NO parameters, one closed constant for every input at every size, with `builder.size` literal-pinned by `rfl`. O3 then makes it a theorem rather than an inspection: `builder.staticBits = K` (literal) and `K < |canonicalReviewerPayload(shape)|` for `n \u2265 n\u2080`. Given O1+O2 this is non-vacuous and cheap to prove. A's Group A is strictly stronger than B's REQ-BLD-09 on program uniformity and must be transplanted into any frozen contract."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-3 UNBOUNDED WORK IN ONE CHARGED INSTRUCTION. Add `| bigStep (dst src : Nat)` whose `bstep` arm re-implements (inside the firewall, as copied source) the whole payload computation and returns word `s.regs src`. Program = `[bigStep rW rI; writeMem 0 rI rW; incr; brNZ]`. Practical cheap variant: one `chunkTable (dst idx : Nat)` instruction whose arm computes an entire precomputed table in one tick.",
   "survives": true,
   "notes": "SURVIVES. REQ-BLD-01 permits any constructor with `Nat` fields and a single non-recursive `bstep` match; REQ-BLD-03 charges it exactly one tick under category `.arithmetic`; REQ-BLD-04 is satisfied because the value genuinely is in a register at the emitting step; REQ-BLD-13 is a NAME-REACHABILITY check, not a CONTENT check, so a copied re-implementation passes. Nothing in B bounds per-instruction work. Consequence is asymmetric and worth stating precisely: the PROVENANCE half of B's claim survives intact (the store really is the machine's own output, computed from the input), but the COST half is destroyed \u2014 B's manuscript sentence 'the run charges B(n) machine instructions in total, one per executed instruction across seven exhaustive categories' becomes true-but-vacuous. Contract A's O14 gestures at this with `unchargedStepBound \u2264 unchargedCap`, but that quantity is a DECLARED Nat per constructor with no theorem connecting it to `exec`, so A does not cover it either. NEITHER CONTRACT HAS THE ROW THAT KILLS THIS."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-3 same unbounded-work instruction.",
   "survives": true,
   "notes": "SURVIVES a fortiori \u2014 and under A it is strictly cheaper than under B, because A's missing firewall means the 'unbounded work' is a one-line call to the existing semantic function rather than a re-implementation (i.e. FK-2 subsumes it). A's O14_uncharged_cap is the row that was meant to stop this and it has no teeth: `BuildInstr.unchargedStepBound` is author-supplied data with no `bstep`-reflection theorem behind it."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-8 ADDRESSES COUNTED, VALUES ORACULAR. Emit writes at exactly the right addresses, pairwise distinct, count matching `(target shape).length`, with the WORDS supplied by a semantic call.",
   "survives": true,
   "notes": "SURVIVES. A's O8 has two disjuncts; the write disjunct is `\u2203 index word, event = TraceEvent.writeWord 0 index word`, which is TRUE OF EVERY WRITE EVENT \u2014 it is a tautology, not an obligation. A therefore has no constraint whatsoever relating a written word to the machine's register state at the emitting step. O11 forces the values to be CORRECT, but 'correct' is precisely what an oracle supplies. A is missing B's REQ-BLD-04 outright."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-8 same.",
   "survives": false,
   "notes": "EXCLUDED as an independent attack by REQ-BLD-04: `\u2200 e \u2208 writeLog, \u2203 seg addrReg valReg, e = \u27e8seg, s'.regs addrReg, encodeWord wordBits (s'.regs valReg)\u27e9` where `s'` is the emitting step's pre-state. The written word must be the encoding of a register the machine actually held. It is not sufficient in isolation (FK-3 fills that register oracularly), but it is the correct write-side mirror of `run_readLog_readWord_shape` (E1Machine.lean:398-404) and it is the row A should have written."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-1 THE AUDITOR'S FAKE VERBATIM: `{payload := buildPayload xs, finalStore := canonicalStore xs, trace := replicate p.length dummyWrite}`.",
   "survives": false,
   "notes": "EXCLUDED, but only by unstatability, not by refutation. Every Group-B/C row is a proposition about `runBuild xs = BuildProgram.eval builder (init xs) fuelBuild`, a function application, so a hand-built record is not the subject of any obligation. A's O13_oracle_rejected is NOT a Lean proposition \u2014 its body is the prose placeholder `<p writes the payload with no input read>` \u2014 so A cannot host the negative control that a blind auditor checks first."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-1 same.",
   "survives": false,
   "notes": "EXCLUDED AND REFUTED \u2014 strictly better than A. `Produces (init) (log) (final)` is stated over PLAIN DATA (a store, a `List WriteEvent`, a store), which is a deliberate and correct design choice: it makes the auditor's fake EXPRESSIBLE and therefore falsifiable in Lean. REQ-BLD-14a requires `\u00ac Produces emptyStore (List.replicate k dummyWrite) (canonicalAsStore fixedShape)` as a checked theorem; it fails at the `Nodup` conjunct for k \u2265 2 and independently at the third conjunct (a populated canonical cell with no matching event). A predicate stated only over `BRunResult` could not host this control."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-6 BRIDGE PROVED BY `rfl`/`simp` AGAINST THE SEMANTIC DEFINITION, run decorative.",
   "survives": true,
   "notes": "SURVIVES in conjunction with FK-2/FK-4: once the oracle is installed, `ReadStore.ofStore (built xs) = targetStore shape` is literally `rfl` and the run is a decoration around a semantic call. A never discusses bridge-proof shape at all \u2014 it has no analogue of B's BR-F1..BR-F6 table, no pin forbidding an intermediate adapter, and no argument for why a `rfl` bridge is harmless."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-6 same.",
   "survives": false,
   "notes": "EXCLUDED, and B \u00a73 is the best-argued section in either document. Its reasoning is correct: constraining PROOF METHOD is unenforceable and confuses tactic hygiene with soundness; what must be constrained is what the LHS is allowed to BE. BR-F1 pins it syntactically to `(brun (builderProgram n) (fuelBound n) (initialState xs)).final.mem`; BR-F2 permits only `WordRAM.ReadStore.ofStore` (verified at WordRAM.lean:91-93) as adapter and forbids any store-valued `def` in Bridge.lean; BR-F3/REQ-BLD-12 require agreement DISCHARGED not hypothesised; REQ-BLD-05/06/07 give the LHS a semantics-free characterisation; REQ-BLD-13 denies the machine the answer. Given the FK-5 repair, a `rfl` bridge is genuinely harmless under B."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-7 REPLAY BY SHARED DEFINITION. Define `WordRAM.replayWrites` and the machine's own store update from the same `Store.writeWord`, so O7 is a near-trivial induction.",
   "survives": true,
   "notes": "SURVIVES as a WEAKENING, not a full fake \u2014 but both contracts materially overstate replay. If the write log is emitted exactly when the store changes, `replayWrites init trace = final` is a structural induction of a few lines and carries no honesty content: it excludes only a machine that mutates memory outside its log. A \u00a72 item 3 presents O7 as the decisive killer ('O7 kills the auditor's fake verbatim'); that is true of the record-forgery fake and false as a general anti-oracle property. The real content in that region is O6 (last-write-wins, no unwritten cells), not O7."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-7 same.",
   "survives": true,
   "notes": "SURVIVES as the same weakening. REQ-BLD-05 is cheap for the same reason. B partially compensates by folding replay into `Produces` alongside `Nodup` and the no-inheritance conjunct (REQ-BLD-06), which is where the actual content lives. Neither contract should advertise replay as the anti-oracle; both should say plainly that replay is necessary, nearly free, and insufficient."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-9 PADDING AS A FREE PARAMETER. Define `paddingBits shape := (\u03a3_{e \u2208 writeLog} e.word.length) - (canonicalReviewerPayload shape).length` and discharge REQ-BLD-07's third conjunct by `rfl`.",
   "survives": true,
   "notes": "SURVIVES. REQ-BLD-07 says `paddingBits` must be 'derived, never asserted' but never says derived FROM WHAT. Deriving it from the writeLog makes `builder_write_bits_eq_payload_plus_padding` a tautology, and the bit-accounting row \u2014 the one B correctly identifies as the auditor's conjunct (b) restated \u2014 becomes vacuous. Ironic given that B's own \u00a77 names this row as 'riskiest obligation to STATE'. B diagnosed the trap and then left the door open."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-9 bit/word granularity mismatch.",
   "survives": false,
   "notes": "EXCLUDED BY AVOIDANCE, and A deserves credit here. O9_count is `(runBuild xs).trace.countP isWriteWord = (target shape).length` \u2014 word count against word-LIST length, both in words \u2014 and A's claim sentence says 'the number of charged writes equals the number of emitted payload words'. A never asserts the auditor's bit-level conjunct and therefore never has to fudge padding. Duplicate/overwrite inflation is separately excluded by O9_nodup, which A correctly freezes as a row rather than leaving as a later lemma."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-11 DESCOPE-AND-STILL-CLAIM. Follow A \u00a76's own 'coordinator scoping recommendation': build one `concreteBPNativeSuccinctRMQReviewerSourceWords shape source` and keep every obligation 'stated unchanged'.",
   "survives": true,
   "notes": "SURVIVES AS A DOCUMENTATION DEFECT THAT WOULD PRODUCE A FALSE CLAIM. VERIFIED: `concreteBPNativeSuccinctRMQReviewerPhysicalWords shape := (concreteBPNativeSuccinctRMQReviewerPhysicalRegions shape).flatten` (RMQ/Core/SuccinctFinal/RAM/ReviewerPhysical.lean:634-637) over `concreteBPNativeSuccinctRMQReviewerPhysicalSources`, a 22-element list (:51-73, with `..._length = 22` at :279). If `built xs` holds one source, O11 and hence O12 are FALSE, not narrower \u2014 A's assertion that 'every obligation above is stated unchanged, only with `target shape` replaced by the one-source word list' is wrong, because O12's `queryWithStore` needs the whole flat array. Contract B is honest about exactly this ('There is no partial credit on the bridge'; `agreesOnFootprint` quantifies over all of 0..29). A's \u00a76 paragraph must be struck before freezing or it will be executed as written."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-13 O12 CITES A THEOREM THAT DOES NOT EXIST. A claims O12 is 'No new query mathematics', discharged by instantiating the accepted store-parametric surface at SuccinctFinalStoreParam.lean:3283-3294 / :3303-3315 / :3261-3277.",
   "survives": true,
   "notes": "NOT AN ADVERSARIAL FAKE BUT A CONTRACT ERROR OF THE SAME SEVERITY. VERIFIED BY EXHAUSTIVE GREP: the flat-physical surface at 85f3eb9 has `_value_eq_suppliedStoreEvaluator` (:3303), `_refines_logical` (:3536), `_eq_of_orderedFootprint` (:3450), `_matchesReadStore` (:3419), `_successful_read_backed` (:3632), footprint and address-width rows \u2014 and NO `_exact` (value = `queryCosted`/`scanWindow`) and NO `_cost_le`/`\u2264 210` theorem, in SuccinctFinalStoreParam.lean or anywhere in RMQ/**; the headline entries for the flat route are only RMQ/Headlines/RMQ.lean:262, 267, 277, 282, 287, 317, 395. Separately, A's O12_value is stated as `= (SuccinctClassic.queryCosted xs left right).value` under `ValidRange`, whereas the accepted headline is `succinctRMQFinalFullModelSoundnessExactOfFootprintGlobal` (RMQ/Headlines/RMQ.lean:693-709) concluding `= some (RMQ.scanWindow shape.representative left len)` under `hshape : shape \u2208 Cartesian.shapesOfSize n`, `0 < len`, `left + len \u2264 n`. O12 as written is not an instantiation of anything."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-12 THE `TraceEvent` WIDTH-CERTIFICATE ARM. Give `concreteBPNativeTraceEventNatEnvelope` (SuccinctFinalRAM.lean:4089) a `writeWord` arm that reports only the address, as A's own blast-radius table specifies ('must include the write address').",
   "survives": true,
   "notes": "SURVIVES as an unflagged second instance of the defect class A correctly flags for `matchesStore`. The envelope and `...TraceEventReadAddressFitsInBits` (:4115) are the width certificates underpinning the reviewer-word-bit theorems; a write event carries a WORD as well as an address, and an arm that reports only the address leaves the written data outside the certificate \u2014 the width theorems then hold vacuously on the build route while looking green. A identified the `matchesStore := True` hazard and then reproduced it one row later in its own blast-radius table."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-14 UNBOUNDED SCRATCH. Use segments > 29 (outside `initial_clean`'s range and outside the footprint) as unbounded working memory; the 'final memory of the run' then contains arbitrarily much beyond the payload.",
   "survives": true,
   "notes": "SURVIVES as a claim-scope hole rather than a soundness hole. B does state a non-claim ('the input segment persists in the final memory outside the footprint; the space theorems continue to be about the counted payload') and offers 'either add a scratch/input accounting theorem or state the exclusion' \u2014 but leaves it optional. Since the public sentence says 'the final memory of a run', an unbounded-scratch final memory makes the sentence read as a space claim it does not support. Contract A is worse: its `init` has exactly two segments (O4_shape) but nothing forbids `builder` from writing fresh segments, and A has no scratch story or non-claim at all."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-15 FUEL AS AN ORACLE. `fuelBound n := 2^(2^n)`, so `builder_halts` and fuel-insensitivity are trivially true and `buildCostBound n` is the only real cost row \u2014 itself unconstrained beyond being 'derived'.",
   "survives": false,
   "notes": "EXCLUDED AS A SOUNDNESS ATTACK (the store is still genuinely built), and both contracts already state the matching non-claim \u2014 B: 'B(n) is not claimed optimal or tight'; A item 1: 'No optimality or asymptotic build claim.' Recorded here only so the coordinator does not later read either contract as licensing an efficiency statement: neither `chargedBuildCost`/`fuelBuild` (A, O10) nor `buildCostBound`/`fuelBound` (B, REQ-BLD-10) is required to be polynomial, let alone comparable to the payload."
  },
  {
   "contract": "A (extend WordRAM)",
   "strongest_fake": "FK-16 VACUOUS AGREEMENT (both stores empty on the footprint).",
   "survives": false,
   "notes": "EXCLUDED. O11 is an equality against the concrete `targetStore shape`, and O9_count ties the write count to `(target shape).length`, so an empty build fails both. A does not name this attack; B does (BR-F5), but A's rows happen to close it."
  },
  {
   "contract": "B (separate model, theorem-bridged)",
   "strongest_fake": "FK-16 same.",
   "survives": false,
   "notes": "EXCLUDED and explicitly anticipated: BR-F5 names it ('Agreement is an equation between `Option Word` functions and is trivially true for two empty stores'), closed by REQ-BLD-07's per-segment length equality plus REQ-BLD-14b sensitivity."
  }
 ],
 "best": "CONTRACT B IS STRONGER, but neither is freezable as written: each has exactly one fatal hole, and they are mirror images, so the correct frozen artifact is B repaired with A's Group A plus one obligation neither contract contains.\n\nWHY B WINS (six independent reasons, all verified at 85f3eb9):\n1. FIREWALL. REQ-BLD-13 pins the machine's transitive import closure to {Cost, RAM, WordRAM}; `RMQ/Core/WordRAM.lean:1` is `import RMQ.Core.RAM` and nothing else, so the semantic namespaces are structurally unreachable and FK-2 dies. Contract A has no closure obligation anywhere, and FK-2 is a one-constructor kill of A that A's own \u00a72 claims is impossible.\n2. INITIAL-STATE DISCIPLINE. REQ-BLD-08 pins `encodeInput` pointwise at fixed width; A's O4 pins only cell count and invertibility, and `abbrev Word := List Bool` (WordRAM.lean:16) is unbounded, so FK-4 kills A with no ISA change at all.\n3. WRITE-VALUE PROVENANCE. REQ-BLD-04 forces the written word to be the encoding of a register held at the emitting step; A's O8 write disjunct (`\u2203 index word, event = writeWord 0 index word`) is a tautology.\n4. REFUTABILITY. `Produces` is stated over plain data, so the auditor's fake is expressible and REQ-BLD-14a refutes it as a checked theorem. A's O13_oracle_rejected is prose (`<p writes the payload with no input read>`), not a proposition \u2014 it cannot host the negative control a blind auditor checks first.\n5. BLAST RADIUS. B touches no accepted inductive. A's `TraceEvent` extension costs 17 wildcard-free arms, 39 case-splits, a definitional change to `DecidableEq`, a full rebuild, a fresh blind audit of the extension commit, and installs TWO instances of the project's recurring defect class inside the repair (`matchesStore (writeWord \u2026) := True`, which A flags, and the width-envelope arm at SuccinctFinalRAM.lean:4089, which A does not).\n6. THE SEAM IS REAL. B's target `concreteBPNativeSuccinctRMQWholeQueryStoresAgreeOnFootprint` (SuccinctFinalStoreParam.lean:678-684) is ALREADY the hypothesis of `succinctRMQFinalFullModelSoundnessExactOfFootprintGlobal` (Headlines/RMQ.lean:693-709), so REQ-BLD-12 is a genuine discharge. A's O12 cites a theorem that does not exist: exhaustive grep shows the flat-physical surface has no `_exact` and no `_cost_le`/`\u2264210` theorem anywhere in RMQ/**.\n\nWHAT A HAS THAT B LACKS \u2014 one axis, and it is decisive against B's own fatal hole: PROGRAM UNIFORMITY. A's O1 (`builder : WordRAM.BuildProgram`, a declaration with no parameters \u2014 one closed constant for every input at every size, `builder.size` literal-pinned) plus O3 (`staticBits = K` literal, `K < |payload(shape)|` for `n \u2265 n\u2080`) kills FK-5. B's REQ-BLD-09 is a size-indexed family whose length bound `progBound n` is left entirely unconstrained ('with `progBound` derived'), so a per-size unrolled constant-emission program \u2014 or, cheaply and realistically, baked constant tables for the two shape-independent chunk sources \u2014 satisfies every B row.\n\nTHE HOLE IN BOTH: neither contract bounds the work performed by one charged instruction. A's O14_uncharged_cap uses `BuildInstr.unchargedStepBound`, author-declared data with no theorem tying it to `exec`. B charges every instruction one tick and stops there. So FK-3 (one instruction that computes an arbitrary amount) survives both. Under repaired B it costs the adversary a full re-implementation and leaves provenance intact, but it makes the cost half of B's manuscript sentence ('charges B(n) machine instructions in total') true-but-vacuous.\n\nNET: B + A's O1/O3 + a reflected-ISA row (R2 below) is the only combination in which the auditor's attack and all eleven variants above are excluded.",
 "required_repairs": [
  "R1 [B, FATAL \u2014 kills FK-5]. Replace REQ-BLD-09 with A's O1 verbatim: `builderProgram : Program` is a single closed constant with no parameters and `builderProgram.length = <literal>` by `rfl`; the input length arrives in a register, read from a pinned header cell of the input segment (add `(encodeInput xs).readWord? inputSegment 0 = some (encodeInt wordBits xs.length)` to REQ-BLD-08 and shift the element cells by one). If a size-indexed family is kept instead, the row is NOT optional and must read: `theorem builderProgram_information_gap : \u2200 n, n\u2080 \u2264 n \u2192 \u2200 shape \u2208 Cartesian.shapesOfSize n, (builderProgram n).length * instrBits < (concreteBPNativeSuccinctRMQCanonicalReviewerPayload shape).length`, with `instrBits := tagBits + maxFieldCount * wordBits` and `n\u2080` a literal. Without one of these two forms, a per-size decision tree emitting literal payload constants satisfies all sixteen of B's rows.",
  "R2 [A AND B, FATAL \u2014 kills FK-3, and gives A's O14 its only teeth]. REFLECTED ISA WITH A LITERAL PER-INSTRUCTION WORK CAP. The interpreter must be data, not a Lean function: `inductive Prim | constZero | succ | add | sub | mulConst (c : Nat) | divConst (c : Nat) | lt | le | eq | getBit | setBit`; `def Prim.constants : Prim \u2192 List Nat` (constructor-exhaustive, no wildcard); `def BInstr.semantics : BInstr \u2192 List Prim` (constructor-exhaustive, no wildcard); and THREE theorems, of which the first is the one that stops `semantics` from being a decoration: `theorem bstep_reflects : \u2200 i s, bstep i s = interpretPrims i.semantics s`; `theorem semantics_cap : \u2200 i : BInstr, (i.semantics).length \u2264 primCap` with `primCap` a literal; `theorem prim_const_cap : \u2200 i : BInstr, \u2200 p \u2208 i.semantics, \u2200 c \u2208 p.constants, c < 2 ^ wordBits`. Note explicitly in the row that `Prim` must contain NO table-lookup constructor and no constructor with an unbounded `Nat` payload, and that adding one must break `Prim.constants`'s wildcard-free match at compile time (precedent: `Instr.FieldsFit` design note, RMQ/Core/WordRAM/E1Machine.lean:492-497). With R2 in place, A's `unchargedStepBound` is replaced by the computed `(i.semantics).length` and R1's bit budget can be extended to `isaBits + progBits < payloadBits`, closing the static-information argument over the interpreter as well as the program.",
  "R3 [B, kills FK-9]. REQ-BLD-07's third conjunct is currently satisfiable by `rfl` if `paddingBits` is derived from the writeLog. Delete the bit-sum equation and replace it with a sequence equality that has no fittable parameter: `theorem builder_writes_canonical_words : \u2200 s, s \u2264 29 \u2192 ((brun \u2026).writeLog.filter (\u00b7.segment = s)).map WriteEvent.word = canonicalSegmentWords shape s` (in emission order), plus `theorem builder_write_width : \u2200 e \u2208 (brun \u2026).writeLog, e.word.length = wordBits`. Bit count and padding then follow as corollaries and `paddingBits` is never a free parameter. Keep B's diagnosis that the auditor's conjunct (b) is not literally provable \u2014 that diagnosis is correct and is the single best call in either document \u2014 but land it as this equality, not as an equation with a derived slack term.",
  "R4 [A, FATAL \u2014 kills FK-4]. Add to O4: `theorem O4_width : \u2200 xs i, (((init xs).store.wordSegments[1]!)[i]!).length = wordBits xs.length` and `theorem O4_pointwise : \u2200 xs i, ((init xs).store.wordSegments[1]!)[i]? = (xs[i]?).map (encodeInt (wordBits xs.length))`. `abbrev Word := List Bool` (RMQ/Core/WordRAM.lean:16) is unbounded, so O4_input_size's cell count bounds nothing; A's annotation 'pins its length so it cannot BE the payload' is false as written.",
  "R5 [A, FATAL \u2014 kills FK-2]. Add an import-closure row to Contract A, mirroring REQ-BLD-13: the transitive import closure of `RMQ/Core/WordRAM/BuildMachine.lean` must be exactly `{RMQ.Core.Cost, RMQ.Core.RAM, RMQ.Core.WordRAM}`, CI-enforced by a script that fails the build (not a reviewer note). Without it, one `Nat`-only constructor whose `exec` calls `concreteBPNativeSuccinctRMQReviewerPhysicalWords` satisfies O1\u2013O14 in full.",
  "R6 [A, kills FK-8]. Replace O8's second disjunct \u2014 currently `\u2203 index word, event = TraceEvent.writeWord 0 index word`, a tautology \u2014 with B's REQ-BLD-04 shape: `\u2203 addrReg valReg, event = TraceEvent.writeWord 0 ((preState pos).regs addrReg) (encodeWord wordBits ((preState pos).regs valReg))`, where `preState pos` is the replay-prefix state O8 already constructs for the read disjunct.",
  "R7 [A, blocking before freeze]. Strike \u00a76's descoping paragraph. VERIFIED: `concreteBPNativeSuccinctRMQReviewerPhysicalWords shape = (concreteBPNativeSuccinctRMQReviewerPhysicalRegions shape).flatten` over a 22-element source list (RMQ/Core/SuccinctFinal/RAM/ReviewerPhysical.lean:634-637, :51-73, `..._length = 22` at :279). A one-source build makes O11 and O12 FALSE, not narrower. The sentence 'every obligation above is stated unchanged' will be executed as written by a worker and will produce a green artifact standing for an unestablished property \u2014 the exact defect class this contract exists to repair. Adopt B's honest formulation instead ('there is no partial credit on the bridge').",
  "R8 [A, blocking before freeze]. Re-state O12 against a theorem that exists. There is no flat-physical `_exact` and no flat-physical `_cost_le`/`\u2264 210` theorem at 85f3eb9 (grep over RMQ/**; the flat route's headline entries are only RMQ/Headlines/RMQ.lean:262, 267, 277, 282, 287, 317, 395). Route instead: O11 gives `ReadStore.ofStore (built xs) = concreteBPNativeSuccinctRMQReviewerPhysicalReadStore shape` (SuccinctFinalRAM.lean:9807-9813); compose with the accepted `concreteBPNativeSuccinctRMQReviewerPhysicalStoreAdapter_canonical` (SuccinctFinalStoreParam.lean:3261-3277) to reach `concreteBPNativeSuccinctRMQGlobalReadStore shape`; then instantiate `succinctRMQFinalFullModelSoundnessExactOfFootprintGlobal` (Headlines/RMQ.lean:693-709), whose real signature is `hshape : shape \u2208 Cartesian.shapesOfSize n`, `0 < len`, `left + len \u2264 n`, concluding `= some (RMQ.scanWindow shape.representative left len)` \u2014 not `queryCosted xs left right` under `ValidRange` as O12 currently states. Declare in the row that this chain contains TWO adapters (`ofStore`, then `PhysicalStoreAdapter`), which B's BR-F2 would forbid; if A is kept, BR-F2's analogue must be written to permit exactly these two named adapters and no others.",
  "R9 [A]. The blast-radius table's arm for `concreteBPNativeTraceEventNatEnvelope` (RMQ/Core/SuccinctFinalRAM.lean:4089) says 'must include the write address'. That is insufficient: a write event carries a WORD as well as an address, and an address-only arm leaves the written data outside the width certificate that `...TraceEventReadAddressFitsInBits` (:4115) and the reviewer-word-bit theorems rest on. Specify the arm as including both the address and the word's bits, and freeze it as a second recorded coordinator amendment alongside the `matchesStore` decision A already flags.",
  "R10 [BOTH]. Add a scratch-memory row. B: `theorem builder_scratch_clean : \u2200 seg idx, (brun \u2026).final.mem.readWord? seg idx \u2260 none \u2192 seg \u2264 29 \u2228 seg = inputSegment`, or freeze the total-memory exclusion as an explicit non-claim row rather than as optional prose. A: A has no scratch story at all \u2014 `init` pins two segments (O4_shape) but nothing forbids `builder` writing fresh ones, so 'the final store' is unbounded. Without this row the public sentence 'the final memory of a run' reads as a space claim neither contract supports.",
  "R11 [BOTH]. Both contracts advertise replay as the anti-oracle; it is not. If the write log is emitted exactly when the store changes, A's O7 and B's REQ-BLD-05 are short structural inductions that exclude only a machine mutating memory outside its log. Rewrite A \u00a72 item 3 and B's \u00a72 row 05 to say plainly: replay is necessary, nearly free, and insufficient; the anti-oracle content lives in program uniformity (R1), the interpreter firewall (R5/REQ-BLD-13), the reflected work cap (R2), no-inheritance (A's O6 / B's REQ-BLD-06 third conjunct), and the pointwise input encoding (R4/REQ-BLD-08).",
  "R12 [BOTH]. Add a negative control that a CURRENT-repo-shaped oracle FAILS, as a checked theorem rather than a placeholder. Concretely: exhibit a hypothetical instruction `oracleWord (dst src : Nat)` and prove `\u00ac \u2203 ps : List Prim, ps.length \u2264 primCap \u2227 interpretPrims ps = oracleSemantics` (R2), AND exhibit a `builderProgram` variant with a baked constant table and prove it violates R1's information gap. A's O13_oracle_rejected is currently the prose placeholder `<p writes the payload with no input read>` and is not a proposition; B's REQ-BLD-14 has (a) (b) (c) but no oracle-rejection row at all.",
  "R13 [PROCESS, BOTH]. Freeze order matters and neither contract states it: R1, R2, R5 and R12 are the rows on which every other row's meaning depends, and all four are cheap. They must be landed and blind-audited BEFORE any builder construction work begins, so that a later worker cannot discharge O9/REQ-BLD-07 honestly against a machine whose ISA has already been widened. This is the same failure geometry as A's own O3 warning ('the only obligation whose failure mode is silent') applied to the contract as a whole."
 ]
}