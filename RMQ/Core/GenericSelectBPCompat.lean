import RMQ.Core.GenericSelect.BPCompat

/-!
# Generic select BP compatibility barrel

Terminal import surface for `shape.bpCode`, `target := false` bridge facts.
The canonical `RMQ.Core.GenericSelect` root stays plain-bitvector generic.
-/
