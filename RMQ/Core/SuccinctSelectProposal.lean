import RMQ.Core.SuccinctSelect.CloseSelect

/-!
# Select-side sparse/dense relative-split close locator

Compatibility import root for the split C1-specific select-side close-select
layers. Public declarations keep the historical `RMQ.SuccinctSelectProposal`
namespace; implementation lives under `RMQ.Core.SuccinctSelect.CloseSelect.*`.
-/
