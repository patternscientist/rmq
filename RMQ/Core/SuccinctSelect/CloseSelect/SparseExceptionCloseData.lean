import RMQ.Core.SuccinctSelect.CloseSelect.SparseExceptionCloseData.Obstructions

/-!
# Sparse-exception close-select data

Compatibility import root for the split sparse-exception close-select data
layers. Public declarations keep the historical `RMQ.SuccinctSelect`
namespace.
-/
